// Version (the "Last updated" date) of the legal documents a user accepts at checkout.
// Bump this whenever /terms or /privacy changes so a recorded acceptance references the exact
// version that was shown. Both documents currently share the same date; if they ever diverge,
// split this into per-document constants and record both.
export const LEGAL_VERSION = "2026-07-20"
