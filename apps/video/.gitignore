node_modules/
package-lock.json
.remotion/
# Legacy Remotion output dir + any staging copies (the Remotion demo composition was removed — ADR-100;
# the demo mp4 is now a real recording assembled with ffmpeg, see tests/playwright/capture/home-clip-assemble.md).
out/
public/
