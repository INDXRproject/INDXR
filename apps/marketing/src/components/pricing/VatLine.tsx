// Skeleton component — visual polish in Claude Design rondje na alle Batch 1 pages

export function VatLine() {
  return (
    <p className="text-center text-sm text-[var(--fg-muted)] mt-6 max-w-xl mx-auto">
      All prices include VAT. Stripe issues a proper invoice on every purchase — VAT-deductible for businesses.
    </p>
  )
}
