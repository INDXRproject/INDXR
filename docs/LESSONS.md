# LESSONS.md

Terugkerende valkuilen voor INDXR.AI. Lees bij sessiestart. Append-only door CC bij elke correctie van Khidr op een patroon (niet one-off).

Format: `[YYYY-MM-DD] <gebied>: <wat fout ging> → <regel om herhaling te voorkomen>`

---

[2026-07-13] Supabase RPC-ACL: nieuwe public-functies krijgen automatisch EXECUTE voor `anon` + `authenticated` (expliciete grants, niet via PUBLIC). Een `SECURITY DEFINER`-functie die admin-data teruggeeft (bv. `admin_geld_summary`) was daardoor via `/rest/v1/rpc/<naam>` aanroepbaar door élke ingelogde/anonieme user, ondanks `REVOKE ALL ... FROM PUBLIC`. → Bij elke admin-/service-only `SECURITY DEFINER`-functie ALTIJD expliciet `REVOKE EXECUTE ... FROM anon, authenticated` (niet alleen FROM PUBLIC) + `GRANT ... TO service_role`, en verifieer met `has_function_privilege('anon', oid, 'EXECUTE')` = false. Draai `get_advisors(security)` na elke nieuwe DEFINER-functie.

[2026-07-02] stop-hook-conventie: `.claude/hooks/check-wiki.sh` werkt correct. "No stderr output" in de stop-hook-feedback is GEEN fout — de hook schrijft de check-melding naar stdout (exit 2), niet naar stderr. Dat is de bedoelde conventie: de runtime leest stdout bij exit 2 als feedback-bericht aan CC. Het bestand bestaat, is executable, en wordt correct uitgevoerd. Melding "No such file or directory" trad op als het bestand werkelijk ontbrak; na herstel is "No stderr output" de gewenste status.
[2026-07-03] bulk-ZIP-export: bestandsnamen in JSZip moeten gegarandeerd uniek zijn — JSZip overschrijft stil bij dubbele naam (laatste wint, geen error). Gebruik de volledige getitelde slug als primaire disambiguator: `slugify(title)` = lowercase, `[^a-z0-9]+→-`, leading/trailing `-` strippen — **geen slice, geen lengte-limiet, geen video_id in bestandsnaam**. Schema: `${slug}_${type}[_variant].${ext}` (bijv. `justice-ep-01_txt_timestamps.txt`, `surah-ar-rahman_rag_120s.json`). Voeg een teller-suffix (`_2`, `_3`) toe als fallback bij werkelijk identieke titels. Voeg altijd een archief-count = selectie-count-check toe en toon een warning bij mismatch. Geldt voor alle bulk-export-flows (TXT, MD, JSON, CSV, SRT, VTT, RAG).
[2026-07-02] pnpm-lockfile: elke package.json-wijziging (dependency toevoegen/verwijderen) vereist dat pnpm-lock.yaml in dezelfde commit geregenereerd + meegecommit wordt via `pnpm install` → Vercel faalt hard met ERR_PNPM_OUTDATED_LOCKFILE bij desync; lokale `pnpm build` vangt dit NIET — verifieer altijd met `pnpm install --frozen-lockfile` vóór push. Gemist bij commit 99c7099.
[2026-07-02] feedback-pattern: sonner/toast is verboden — auto-dismiss is niet toegestaan in deze codebase → gebruik altijd FeedbackCard (packages/shared/src/components/ui/FeedbackCard.tsx) voor inline persistente feedback. Copy-success → button-level useState bool (1.5s timeout, geen banner). Download-success → altijd stil (het bestand opent). Sidebar-berichten → compact inline banner (geen FeedbackCard-icoon). Financiële routes (checkout, credit-claim) → FeedbackCard met onDismiss, nooit auto-dismiss of success weglaten. RAG-bulk-success → persistent inline card met X-dismiss, geen setTimeout-close; de dialog sluit alleen via gebruikersactie (X-knop of Cancel).
[2026-07-03] flex-truncate: `truncate` (overflow:hidden/text-overflow:ellipsis) werkt niet op een flex-child zonder `min-w-0`. Flex-items hebben `min-width: auto` by default, waardoor ze niet onder hun content-breedte krimpen en het label naast hen buiten de container duwt. Altijd `min-w-0` toevoegen aan de truncerende flex-child naast `flex-1 truncate`.
[2026-07-03] grid-child-overflow: shadcn `DialogContent` (en `AlertDialogContent`) is `display: grid`. Grid-children hebben net als flex-items `min-width: auto` by default en krimpen niet onder hun inhoudsbreedte. De fix hoort in de **gedeelde primitive**, niet per callsite: voeg `grid-cols-[minmax(0,1fr)]` toe aan de base className van `DialogContent`. Dit begrenst de grid-track zelf tot de beschikbare breedte — alle huidige en toekomstige dialog-children erven dit automatisch. Absoluut gepositioneerde children (sluit-knop) zijn geen grid-items en worden niet geraakt. Per-callsite `min-w-0` op een wrapper is redundant als de primitive dit dekt en mag dan worden verwijderd. `min-w-0` op flex-children voor truncatie en `overflow-x-hidden` op scroll-containers blijven correct als diepere lagen.
[2026-07-02] credit-routes: RAG-export component (RagExportView) bood een gratis download-pad zonder credit-aftrek — bereikbaar via directe URL-manipulatie (?tab=developer) → elke credit-gerelateerde component of route vereist een expliciete server-side guard op het eerste-export-pad. Een component dat "gratis" aanneemt zonder de rag_exports-status te verifiëren is een lek. Defensief: render-guard in de page + component-level fallback.
[2026-05-04] arq: ack_late bestaat niet in arq 0.28.0 (Celery-concept) → bij crash-recovery design: ga uit van idempotency-vlaggen + watchdog cron, niet van library-features.
[2026-05-04] supabase: playlist-status enum is `'complete'` niet `'completed'` → wijk niet af van migratie-SQL als bron van waarheid voor status-strings.
[2026-05-04] supabase: `idempotency_keys` tabel is in ADR-019 beschreven maar nooit aangemaakt → verifieer tabel-bestaan via `information_schema.tables` voordat je ernaar verwijst in code.
[2026-05-04] yt-dlp: hergebruik van zelfde proxy-session bij retry helpt niet bij bot-detection → voor retries: nieuwe session-suffix.
[2026-05-04] sentry: serverless functions sturen geen envelope vóór process kill → altijd `await Sentry.flush(2000)` na captureException + `export const runtime = 'nodejs'` op API routes die capturen.
[2026-05-04] credits: nooit credits aftrekken via direct INSERT → altijd `deduct_credits_atomic` RPC of de geüpgrade `update_playlist_video_progress` RPC.
[2026-05-04] tiptap: `immediatelyRender: true` veroorzaakt SSR hydration mismatch → altijd `immediatelyRender: false`.
[2026-05-04] logging: uvicorn override't `logging.basicConfig` zonder `force=True`; Sentry SDK reset root logger ná init → na `sentry_sdk.init()` expliciet `logging.getLogger().setLevel(INFO)` zetten.
[2026-05-05] wiki-vs-code: known-issues.md claimde "rate limiting bewust uitgeschakeld" maar src/lib/ratelimit.ts deed wél echte Redis-calls totdat UPSTASH_* env vars op Vercel op Development scope werden gezet → 500K Upstash commands in 5 dagen mede-veroorzaakt door dit blinde-vlek. Verifieer wiki-claims over runtime-gedrag tegen bron-code, vooral bij infrastructuur-statements.
[2026-05-17] supabase-ssr: Gebruik getClaims() in middleware, niet getUser(). getClaims() returnt null bij geen sessie zonder retry-loop en zonder Supabase-netwerk-calls. getUser() maakt een server-side validatie-call en kan refresh-loops triggeren bij stale tokens (bewezen: 500K Upstash commands in 5 dagen, commit 22a0059). Voeg GEEN error-recovery cookie-clearing toe in middleware — clearAuthCookies() wist ook PKCE code-verifier (sb-*-auth-token-code-verifier), waardoor exchangeCodeForSession() faalt in OAuth/email-callback flows. Volg het officiële Supabase Next.js template letterlijk (vercel/next.js with-supabase).
[2026-05-17] supabase-ssr: Sluit auth callback routes uit van updateSession middleware via de matcher. Routes met exchangeCodeForSession() (bijv. /auth/callback) creëren zelf een sessie en hebben geen session-refresh nodig. updateSession() op een callback route kan de tijdelijke PKCE verifier-cookie verwijderen vóór de exchange → silent failure. Voeg auth/callback toe als uitzondering in de middleware matcher.
[2026-05-05] supabase: refresh_token_not_found in middleware veroorzaakt infinite refresh-loop in browser → blow-out van Vercel rate-limit Redis quota. [VERVANGEN door getClaims() fix 2026-05-17 — zie bovenstaande regels voor correcte aanpak]
[2026-05-05] next-server-actions: window.location.href na await serverAction() veroorzaakt "Error in input stream" TypeError omdat de browser de RSC action-response stream aborteert mid-transmission (Next.js GitHub #81377) → nooit window.location.href of router.push na een Server Action aanroep; gebruik redirect() in de Server Action zelf voor cross-host navigatie.
[2026-05-05] subdomain-split: redirect("/<auth-path>") in Server Components op app.indxr.ai veroorzaakt 307 → middleware 308 → cross-origin chain die RSC prefetch parser breekt → TypeError "Error in input stream". Server-side redirects MOETEN absolute marketing-URL gebruiken (NEXT_PUBLIC_MARKETING_URL) in subdomain-split setup, niet relatief.
[2026-05-04] subdomain-split: alle `<Link href="/<marketing-path>">` in components die op de app-host renderen (niet alleen shell-components zoals Header/Footer, ook alle children van dashboard pages zoals VideoTab, AudioTab, PlaylistTab, TranscriptViewer, TranscriptCard) veroorzaken Next.js prefetch-crash → TypeError: Error in input stream. Fix: vervang door `<a href={marketingHref(...)}>`. Inventaris-grep moet ALLE src/components/ + src/app/(app)/ scannen, niet alleen shells.
[2026-05-05] cross-host-links: audit cross-host links op drie patronen, niet alleen <Link>: (1) `<Link href="/pad">`, (2) `<a href="/pad">`, (3) `window.location.href = '/pad'`. Alle drie vereisen appHref()/marketingHref() wanneer de component op een andere host gerenderd kan worden.
[2026-05-05] env-vars: vermijd meerdere env-var-namen voor hetzelfde concept (NEXT_PUBLIC_SITE_URL naast NEXT_PUBLIC_APP_URL + NEXT_PUBLIC_MARKETING_URL is verwarrend). Eén concept = één var. Bij subdomain-split: NEXT_PUBLIC_APP_URL voor app-host, NEXT_PUBLIC_MARKETING_URL voor marketing-host.
[2026-05-05] vercel-limits: directe browser→Railway POST voor audio uploads omzeilt Vercel's 4.5MB request body limiet (AudioTab.tsx). Noem de env-var expliciet NEXT_PUBLIC_AUDIO_UPLOAD_URL zodat het doel helder is — niet NEXT_PUBLIC_PYTHON_BACKEND_URL hergebruiken voor dit specifieke geval.
[2026-05-05] git-workflow: CC heeft git commit uitgevoerd zonder expliciete toestemming → CC voert NOOIT git add/commit/push uit zonder expliciete toestemming per taak. Khidr geeft commit-toestemming in de taakprompt of in een vervolgbericht.
[2026-05-06] vercel-env-vars: Sensitive env vars kunnen NIET op alleen Development scope worden gezet — Vercel forceert minstens Production of Preview voor sensitive vars. De C.3.1 workaround (env vars op Development scope om noopLimiter te activeren in productie) is daarom niet herbruikbaar voor sensitive vars. Alternatief: vars volledig verwijderen om noopLimiter te activeren, of niet-sensitive maken.
[2026-05-06] vercel-env-vars: Vercel UI env var input accepteert GEEN quotes rond waardes. .env files gebruiken KEY="value" syntax maar in Vercel UI moet alleen de rauwe waarde, anders worden de quotes letterlijk deel van de string. Bij plak-uit-.env: quotes verwijderen.
[2026-05-06] ui-state: Twee parallelle UI-state mechanismen op hetzelfde concept (Shadcn useSidebar + custom localStorage voor sidebar breedte) zijn een recipe for desync — één knop doet visueel niets op desktop omdat hij de verkeerde state bestuurt. Kies één mechanism per UI-concept en verwijder de andere. Bij Shadcn-componenten met custom overrides: de custom state wint altijd; verwijder de Shadcn default trigger als die geen effect heeft.
[2026-05-07] shadcn-sidebar: Shadcn Sidebar `variant="inset"` (en alle non-collapsible="none" varianten) gebruikt `position: fixed; top: 4rem` — hardcoded 4rem header assumptie. Conflicteert met custom topbar heights. Gebruik `collapsible="none"` voor flex-flow integratie: sidebar zit dan in normale document flow, geen fixed positioning, hoogte bepaald door flex container. SidebarProvider moet OUTER wrapper blijven (context scope voor useSidebar hooks), layout-flex in geneste div erin.
[2026-05-07] react-context: Componenten die useContext() of custom hooks gebruiken (zoals Shadcn useSidebar()) MOETEN binnen de bijbehorende Provider rendered worden. Bij refactoring naar custom layout: SidebarProvider als outer wrapper laten, ook al stuurt hij visueel niet de hele layout. Provider = context scope, niet visuele container.
[2026-05-07] css-absolute-containment: Een `position: absolute` child van een `position: sticky` parent (bijv. Button inside sticky header) gebruikt de sticky parent als containing block. Maar zonder `overflow: hidden` op de sticky parent kan de absolute child buiten de button bounds verschijnen. Fix: altijd `relative overflow-hidden` op het directe parent element van een absolute-positioned child, niet alleen op een verre ancestor.
[2026-05-07] css-variables: Een CSS-variabele die zichzelf referentieert in een inline style (`style={{ "--x": "var(--x)" }}`) levert een guaranteed-invalid value op dat element. Child-elementen die `var(--x)` gebruiken krijgen 'invalid' terug en vallen terug op default (bijv. transparent voor kleuren). Patroon ontstaat bij mislukte pogingen om een Tailwind/Shadcn defaultkleur te overriden — gebruik in plaats daarvan een ANDERE CSS-variabele als bron, of een directe kleurwaarde.
[2026-05-17] cross-host-redirect: router.push('/pad') of router.push() in een component op een andere host dan het doelpad blijft op de huidige host → 404 op de ontbrekende route. Gebruik window.location.href = appHref('/...') (of marketingHref) voor cross-host navigatie. Next.js Link en router.push zijn altijd same-origin.
[2026-05-17] env-vars-vercel: BACKEND_API_SECRET mismatch tussen Vercel en Railway gaf 401 op alle backend API calls. Vercel UI accepteert geen quotes rond env values — letterlijke quotes worden opgeslagen als deel van de waarde. Bij elke env var: paste zonder aanhalingstekens, check scope (Production + Preview), redeploy zonder build cache om var op te halen. Var moet identiek zijn op alle Vercel projecten die de backend aanroepen (indxr-marketing + indxr-app). Na migratie naar nieuwe Vercel projecten altijd opnieuw verifiëren — env vars worden niet automatisch overgenomen.
[2026-05-17] supabase-recovery: PKCE-based recovery emails (password reset, magic link) moeten redirectTo instellen op een route die exchangeCodeForSession aanroept. Direct naar een protected app-pagina sturen werkt niet — de code vervalt en geeft otp_expired. Patroon: redirectTo = {marketing}/auth/callback?next=<encoded-final-target>; callback wisselt de code in en redirect dan naar final-target met hostname-validatie (voorkom open redirect).
[2026-05-17] upstash-arq: noopLimiter-pattern voor Upstash quota-mitigatie werkt op Vercel (env vars verwijderen → noopLimiter actief) maar NIET voor ARQ workers. ARQ roept bij init `pool.delete(self.health_check_key)` aan — zonder werkende Redis crasht de worker bij elke startup. Bij quota-incident: óf plan upgraden, óf worker offline nemen, niet "vergeten en doorgaan".
[2026-06-04] redis-serverless-vs-polling: Serverless per-commando Redis (Upstash) is structureel ongeschikt voor constant pollende workers. ARQ met `poll_delay=0.5s` + `health_check_interval=1s` genereert ~10.860 Redis-commando's/uur idle = ~7,84M/maand = 15,7× Upstash Free Tier (500K/maand) — zonder ook maar één job. Regel: gebruik container-Redis (Railway, geen kosten per commando) voor ARQ workers; gebruik Upstash alleen voor sporadische serverless calls (rate-limiter, cache). Het mismatch-probleem is niet configureerbaar weg via hogere poll_delay — health_check (1/s) domineert al bij poll_delay=5s.
[2026-06-27] playlist-shared-helper: bij playlist-jobs die een gedeelde helper aanroepen (bijv. do_assemblyai_transcription), verifieer dat cache-reads/guards in de helper zelf zitten en niet in een wrapper (run_whisper_job) die de playlist overslaat — anders mist één pad de optimalisatie stil zonder fout of log.
[2026-06-27] yt-dlp-tlang: `subtitleslangs` filtert `info['automatic_captions']` NIET in `extract_info(download=False)` mode — alle ~120 YouTube-vertaaltalen komen terug ongeacht de instelling. De enige betrouwbare manier om een native track te selecteren is: zoek sleutels die eindigen op `-orig` in `info['automatic_captions']` (bv. `ja-orig`) — die hebben nooit `tlang=` in de URL. Sleutels zonder `-orig` (bv. `en` voor een Japanse video) bevatten een machinevertaals-URL. Gebruik NOOIT de taalcode uit de YouTube Data API (`videos.list language`) als enige grondslag voor track-selectie: de API geeft `'en'` terug voor niet-Engelse video's (bewezen voor iKtPI8IMuOM, Japans). Verifieer altijd URL-niveau (`'tlang=' not in url`) als safety net.
[2026-06-04] tailwind-v4-monorepo: Tailwind v4 (@tailwindcss/postcss) scant NIET automatisch packages buiten de app-directory. In een monorepo met packages/shared/ moeten alle apps een `@source "../../../../packages/shared/src/**/*.{ts,tsx}";` directive toevoegen aan hun tokens.css (pad relatief ten opzichte van het CSS-bestand). Zonder dit ontbreken alle Tailwind utility classes die uitsluitend in shared components voorkomen (bg-accent, md:flex, md:hidden, text-fg-subtle, etc.) uit de CSS output — resulterend in onzichtbare UI elementen en kapotte responsive layouts.
[2026-06-30] supabase-migrations: 8-cijferige timestamp-prefixes (bijv. 20260302_) en SQL-Editor-wijzigingen buiten de CLI zijn onzichtbaar voor supabase_migrations.schema_migrations → schema-drift zonder foutmelding. Remedie: baseline-squash via Management API introspectie (pg_get_functiondef, pg_policies, information_schema, pg_constraint voor cross-schema FKs) → één baseline .sql in migrations/, tracking-reset via DELETE + INSERT op schema_migrations, pre-baseline bestanden naar migrations_archive/. Triggers ophalen apart via pg_trigger WHERE NOT tgisinternal (pg_get_triggerdef includeert auth-schema triggers zoals on_auth_user_created).
[2026-07-01] ui-feedback: geen toasts, ooit — ook niet in admin-only UI, ook niet als "netter maken" van een bevestiging. Toast = auto-dismiss via setTimeout = verboden. Altijd inline persistente success/error state die blijft staan tot de volgende actie of expliciete sluiting door de user.
[2026-06-30] supabase-migrations: migratiebestanden MOETEN een 14-cijferige YYYYMMDDHHMMSS-prefix hebben (zoals `20260630170359_messages_archived.sql`). 8-cijferig is verboden — die vallen buiten schema_migrations-tracking. Pas altijd toe via Supabase MCP apply_migration of Management API, nooit via `supabase db push` of SQL-Editor.
[2026-07-01] docs-vs-code: CLAUDE.md (en andere docs) kunnen beschrijven hoe de code eruitzag ten tijde van schrijven, niet hoe hij nu is. "Geen OKLCH (vervangen door hex)" bleek de live staat exact omgekeerd te zijn. Patroon: elk CLAUDE.md-claim over runtime-gedrag, token-namen, of library-gebruik verifiëren tegen de bronbestanden vóór je erop vertrouwt — zeker na redesign-sprints of grote refactors. Dit is het tweede blinde-vlek-patroon na de Upstash-wiki-drift (2026-05-05).
[2026-07-03] design-library-stijltaal: Library-redesign (stijl-anker voor de dashboard-shell) vestigde deze taal, over te nemen door volgende dashboard-pagina's: titel-gedreven rijen met `line-clamp-2` (nooit line-clamp-3), haarlijn-scheidingen (`divide-border-subtle`) i.p.v. zware per-rij card-borders, twee badge-families (bron = `bg-info-subtle text-info`, AI-afgeleide outputs = `bg-violet-subtle text-violet`) plus een neutrale `border-border`-collectiebadge, metadata (duur/woorden/datum) altijd rechts-uitgelijnd en gedempt (`text-fg-muted`) nooit concurrerend met de titel. Nieuwe tokens `--info`/`--violet` (+ `-subtle`/`-fg`) toegevoegd aan beide tokens.css-bestanden — volg het bestaande `success`/`warning`/`error`-patroon exact bij het toevoegen van nieuwe badge-families, gebruik de `-subtle` bg + basis-token als tekstkleur (niet de `-fg`-variant, die staat er als optie maar wordt in de praktijk niet gebruikt).
[2026-07-03] design-hexagon-bg-uitzondering: wiki/design/research/batch-3b-ux-aesthetic.md §5.4 classificeert een hexagon-achtergrondtextuur in de Library-lijst als Israf ("competes with content") — hexagon-achtergronden zijn daar beperkt tot reflectieve oppervlakken (login, 404, welcome, footer). Op expliciet verzoek van Khidr is dit voor de Library-body toch doorgevoerd, op zeer lage opaciteit (`opacity-[0.03] dark:opacity-[0.045]` — de feitelijke code-waarde in `DashboardBackdrop`, geverifieerd 2026-07-31) via `HexagonPattern` uit `packages/shared/src/components/icons/`. Behandel dit als bewuste, gedocumenteerde uitzondering (zie ook §5.4-notitie in de wiki zelf) — niet als precedent om hexagon-achtergronden stilzwijgend ook op andere werk-oppervlakken (Transcribe, Settings, Admin) toe te voegen zonder dezelfde expliciete afweging.
[2026-07-03] destructive-actions-confirmatie: elke destructieve actie (delete, vooral bulk-delete) MOET een bevestigingsdialoog hebben vóór uitvoering — ook als de bestaande component die nooit had. In TranscriptList.tsx had per-rij delete géén bevestiging (direct `onDelete(t.id)` op klik) en bulk-delete gebruikte een ongestylede `window.confirm()`. Beide zijn vervangen door de gedeelde `AlertDialog`-primitive (`packages/shared/src/components/ui/alert-dialog.tsx`) die titel (per-rij) of aantal (bulk) toont vóór bevestiging. Patroon voor nieuwe destructieve acties op andere pagina's: nooit een directe delete-call op een klik-handler zetten, altijd via een `deleteTarget`-state + AlertDialog, en `window.confirm()` nooit gebruiken (onstyled, inconsistent met design-system, makkelijk over het hoofd te zien tijdens redesigns omdat de functionaliteit "al bestond").
[2026-07-04] design-badge-hue-systeem: Library badge-kleuren volgen nu een OKLCH hue-familie-systeem (tokens.css "Badge families"). Regel bij toevoegen/wijzigen van badge-families: houd L en C CONSTANT per rol (text/base én -subtle bg), verschuif ALLEEN de hue tussen families zodat ze perceptueel gelijkwaardig en dark-mode-stabiel zijn. Families: auto-captions = sky (hue 230), AI-transcription = indigo (265), AI-summary = violet (300), RAG = teal (185) — minimaal ~35° hue-afstand. "Edited"-variant = ZELFDE hue, hogere L (`-soft`/`-edit` token). LET OP: op een lichte `-subtle` bg vermindert hogere text-L het contrast; in light mode botst "hogere L" met WCAG AA, dus draag het "lichter"-signaal deels via een lichtere bg en houd de text AA-veilig (geverifieerd: light min 4.75, dark min 6.73 op de eigen -subtle bg). Verifieer ELKE nieuwe familie/edited-paar numeriek (OKLCH→sRGB→WCAG) in beide thema's vóór merge; vergroot hue-afstand als twee families te dicht liggen (teal vs sky was het krapste). Badges renderen als `bg-{familie}-subtle text-{familie}` (basis-token als tekst, niet -fg). De oude `--info`/`--violet`-twee-familie-opzet is vervangen; `--info` verwijderd.
[2026-07-04] library-optimistic-mutaties: Library UI-mutaties die één rij aanraken (mark-as-read, en soortgelijke read/unread/toggle-acties) gebruiken het React 19 `useOptimistic` + `startTransition`-patroon, NOOIT een full-list-reload via `router.refresh()`, `revalidatePath()` of een `transcripts-updated`-event dat `fetchTranscripts()` opnieuw triggert. Reden: een refetch zet `loading=true` en herbouwt de lijst → zichtbare flits/reflow en scroll-reset voor een wijziging die maar één badge betreft. Correcte opzet (TranscriptList.tsx `handleMarkAsRead`): committed state in `useState` (`readIds`), overlay via `useOptimistic(readIds, (prev,id)=>new Set(prev).add(id))`; de optimistic setter (`addOptimisticRead`) MOET binnen de `startTransition`-async-callback worden aangeroepen anders flitst de badge terug; de canonicale mutatie (supabase `.update({viewed_at})`) blijft ongewijzigd draaien; alleen bij succes `setReadIds` committen, bij fout niets doen zodat de overlay vanzelf terugrolt. Verander bij zulke taken alleen de UI-update (reload→optimistic), niet de onderliggende mutatie, en dispatch géén `transcripts-updated` voor single-row reads.
[2026-07-04] youtube-data-api-playlist-paginatie: de YouTube Data API `playlistItems.list` retourneert MAX 50 items per pagina — voor playlists >50 video's MOET je met `nextPageToken` doorpagineren tot het token weg is (of tot een bovengrens; wij gebruiken 500, gelijk aan de yt-dlp-fallback `playlist_items: '1-500'`). Zonder loop haal je stil alleen de eerste 50 op. LET OP: ook `videos.list` (voor duur/captions-details) accepteert max 50 IDs per call — na paginatie moet je de video-ID's in chunks van 50 batchen, anders resolven alleen de eerste 50. Bijkomend anti-patroon dat hierdoor ontstond: een verzonnen "X unavailable"-telling als `total_count − opgehaald` (= puur de door de cap afgekapte rest) met misleidende hardcoded copy "private, members-only, or deleted" zonder enige echte check. Regel: een "unavailable"-telling moet berekend worden uit een échte per-video-resolutie (videos.list gaf geen details terug óf titel is "Private/Deleted video"), niet uit een aftreksom tegen een (mogelijk cap-opgeblazen of inconsistente) itemCount. Zie `backend/youtube_client.py:get_playlist_items` (retourneert nu `unavailable_count`) en `PlaylistManager.tsx` (leest `playlist.unavailable_count`).
[2026-07-04] home-dashboard-credits-bron: de Home/dashboard-pagina (`apps/app/src/app/dashboard/page.tsx`) MOET het credit-saldo uit dezelfde live bron halen als topbar en sidebar: `useAuth().credits` → RPC `get_user_credits` → `user_credits.credits`. Nooit een aparte/eigen RPC. Concreet ging het mis: Home was een server-component die een niet-bestaande RPC `get_credit_balance` aanriep, die faalde en via `?? 0` stil terugviel op 0 — terwijl topbar/sidebar wél het juiste saldo toonden. Twee lessen: (a) verzin geen tweede credits-leesbron; hergebruik `useAuth().credits` (client) zodat het getal consistent is én binnen een sessie meeververs t na aftrek; (b) een server-component rendert eenmalig per request en ververst niet — voor live-veranderende waarden (credits) hoort de weergave client-side via de gedeelde context, niet een one-shot server-render. Fix: klein client-component `HomeCreditsBalance` (`apps/app/src/components/dashboard/`). Raak nooit de credit-aftrek-RPC/`user_credits`-mutatie aan bij zo'n leesbron-correctie.
[2026-07-04] caption-timedtext-proxy-rotatie: caption/timedtext-downloads (yt-dlp/httpx VTT-fetch in `youtube_utils.py:extract_with_ytdlp`) MOETEN de proxy-sessie roteren per retry — net als de audio/Whisper-tak al doet (`transcription_pipeline.py` bouwt `[get_proxy_url(session_id=f"{sid}-r{i}") for i in range(1,4)]`, `audio_utils.py` kiest `proxy_urls[attempt-1]`). Eén sticky Decodo exit-IP vastpinnen over alle 3 de retries garandeert herhaalde 429 op schaal: als YouTube's timedtext-endpoint dat IP rate-limit, geeft élke retry op datzelfde IP weer 429. Concreet fout: `dl_proxy_url` werd één keer vóór de retry-loop berekend en hergebruikt; de 30s-deferred playlist-retry-pass (`worker.py`) gebruikte bovendien exact dezelfde per-video sessie als de mislukte eerste poging. Fix: per attempt `get_proxy_url(session_id=f"{session_id}-r{attempt}")`, en de retry-pass een `-retry`-suffix zodat die op een vers IP landt. Regel: elke geproxyde YouTube-download-retry (audio én captions) roteert de sessie; pin nooit één IP over retries. Combineer met async exponentiële backoff + jitter (nooit blocking `time.sleep` op de event-loop). NB: concurrency verlagen helpt hier NIET — de playlist-verwerking is al strikt sequentieel (`_enqueue_next`), dus de 429 komt van IP-hergebruik, niet van fan-out.
[2026-07-05] marketing-vs-transactionele-email-scheiding: marketing/broadcast-e-mail is een APART kanaal van transactionele support-mail. `profiles.marketing_unsubscribed` (broadcast opt-out) ≠ `profiles.email_notifications` (support-reply-toggle). Regel: een marketing-unsubscribe mag NOOIT transactionele mail (ticket-replies via `notifyUser`) breken, en omgekeerd. Concreet in de code: `sendBroadcastEmails`/de broadcast-send-route filtert op `marketing_unsubscribed`, `notifyUser` (support) checkt uitsluitend `email_notifications` — nooit kruisverwijzen. Geverifieerd: na een marketing-unsubscribe blijft `email_notifications` ongewijzigd `true`. Bij het toevoegen van nieuwe e-mailkanalen: geef elk kanaal zijn eigen opt-out-veld en check alleen dat veld. Bijkomend patroon uit deze feature: (a) niet elke auth-user heeft een `profiles`-rij (6 users vs 5 profiles) → de unsubscribe-route MOET upserten (`onConflict:'id'`), en send-time-checks behandelen een ontbrekende rij als "niet uitgeschreven"; (b) unsubscribe-token = HMAC-signed (userId ingebed + gesigneerd), geen radbare/omwisselbare user_id, geen opslag nodig; secret valt terug op `SUPABASE_SERVICE_ROLE_KEY` als `UNSUBSCRIBE_SECRET` niet gezet is; (c) `admin.auth.admin.listUsers` is hard `perPage:1000` → altijd pagineren voor >1000 users; (d) een broadcast-send-route MOET een expliciete `confirmCount`-gate hebben (409 bij mismatch) zodat er nooit verzonden wordt zonder bevestigde recipient-count.
[2026-07-05] broadcast-service-vs-marketing-classificatie: broadcast-e-mail kent twee WETTELIJKE types en de admin-composer classificeert ze expliciet — service (legitimate interest: storingen/account-mededelingen, gaat naar iedereen, GEEN opt-out-respect, GEEN unsubscribe-footer, en MAG GEEN promotionele inhoud bevatten — promotie in een service-mail maakt het juridisch marketing) vs marketing (consent: honoreert `marketing_unsubscribed`, verplichte unsubscribe-footer + `List-Unsubscribe`-header). KRITIEK fail-safe: de classificatie moet server-side defaulten naar "marketing" — alleen een expliciete `messageType === "service"` schakelt de unsubscribe-machinerie uit (`const isService = messageType === "service"` in `api/admin/broadcast/route.ts`); elke ontbrekende/onverwachte waarde valt terug op marketing zodat een opt-out nooit per ongeluk omzeild wordt. De param loopt door drie lagen: composer (`messageType`-button-group) → send-route (`isService` stuurt filter-skip + `skippedUnsubscribed=0`) → `sendBroadcastEmails({includeUnsubscribe})` (bij service géén footer/header, `unsubscribeUrl` optioneel weggelaten). De in-app-message-insert is voor beide types identiek — alleen het e-mailkanaal verschilt. Bouwt hierop voort: [[marketing-vs-transactionele-email-scheiding]].
[2026-07-05] broadcast-email-html-template: broadcast-e-mail (`sendBroadcastEmails` in `apps/app/src/lib/mail.ts`) gebruikt een branded, e-mail-VEILIGE HTML-template — NIET web-HTML. E-mailclients (Gmail/Outlook/Apple Mail) strippen `<style>`-blokken, negeren flexbox/grid, droppen custom fonts en ondersteunen GEEN OKLCH. Regels: (a) table-based layout (`<table role="presentation" cellpadding=0 cellspacing=0 border=0>`), nooit flexbox/grid; (b) ALLE styling inline (`style="..."` per element), geen `<style>` of externe/token-CSS; (c) web-safe font-stack met fallback (`-apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`) — IBM Plex bestaat niet in e-mail; (d) merk-kleuren als HEX (OKLCH uit tokens.css vooraf omgezet: accent `#d79628`, bg `#fcfaf7`, surface `#ffffff`, fg `#27231f`, border `#dbd7d2`, fg-muted `#77726d`, accent-fg-link `#643400`); (e) max-width ~600px gecentreerd; (f) logo via ABSOLUTE URL (`${appUrl}/logo/indxr-wordmark-black-transparent.png`) — relatieve paden resolven niet in e-mail; light-variant want e-mail heeft geen dark-mode-garantie; (g) ALTIJD een plain-text-fallback (`text`) náást `html` meesturen (spam-filters/clients vereisen dat). TYPE-AFHANKELIJKE footer via de bestaande `includeUnsubscribe`-param (niet opnieuw classificeren): marketing = "Unsubscribe" als TEKSTLINK (rauwe token-URL achter het woord, nooit zichtbaar) + `List-Unsubscribe`-header; service = privacy-policy-link, GEEN unsubscribe, GEEN `List-Unsubscribe`-header. User-input (subject/body) HTML-escapen. Raak bij template-werk nooit de recipient-filtering, de `marketing_unsubscribed`-skip, de route-classificatie of de HMAC-unsubscribe-token aan — alleen de body-opmaak. Zie [[broadcast-service-vs-marketing-classificatie]] en [[marketing-vs-transactionele-email-scheiding]].
[2026-07-05] arq-jobtimeout-en-interrupted-state: TWEE lessen uit de "playlist toont Complete terwijl 'ie draait"-bug. (1) ARQ `job_timeout` is een instelbare DEFAULT (300s), GEEN plafond — je overschrijft 'm globaal op `WorkerSettings.job_timeout` of per functie (per-functie wint van de worker-default), met een waarde die de ZWAARSTE ENKELE taak moet dekken (bij een playlist: de langste enkele video, niet de som — het is een keten van per-video-jobs). In deze codebase staat `job_timeout=7200` (2u) globaal (worker.py `WorkerSettings`), wat de langste video ruim dekt (gemeten: max ~36 min/video). Timeout was hier dus NIET de oorzaak — meet de echte config vóór je 'm als oorzaak aanwijst. (2) `'interrupted'` is een TRANSIENTE, HERSTELBARE status, GEEN eindtoestand. Het poll-endpoint (`main.py`, `HEARTBEAT_STALE_SECS=300`) markeert een job `'interrupted'` bij een stale heartbeat (worker-restart of lange stall); de worker-watchdog (`watchdog_interrupted_jobs`, elke 2 min) re-enqueuet 'm binnen ~2 min (crash-recovery, `watchdog_attempts` telt op). Regel: behandel `'interrupted'` NOOIT als terminal in de frontend — anders stopt de UI met pollen en rendert 'ie "Complete" terwijl de backend nog draait (en de watchdog 'm hervat). Alleen `'complete'` en `'error'` zijn terminal (`error` = watchdog Pass 2 na uitgeputte retries). Concreet was de bug: `useJobStatus.ts` `TERMINAL`-set bevatte `'interrupted'` → poll stopte + `_handlePlaylistComplete` vuurde; en de PlaylistTab mount-resume gooide een `'interrupted'` job weg i.p.v. hervatten. Bijkomend anti-patroon: de completion-copy toonde `total = Object.keys(videoStatuses).length` (lokaal geziene count, "17 of 18") i.p.v. `total_videos` (63) — bij een vroegtijdige render geeft dat een misleidend totaal; opgelost doordat de UI nu pas bij échte `'complete'` rendert (dan bevat video_results alle 63). Keten-continuïteit bij CancelledError: `process_playlist_video` (`except Exception`) vangt GEEN `asyncio.CancelledError` (worker-restart/cancel) — de in-process enqueue-van-de-volgende sterft dan, maar de watchdog vangt dit robuust op (bewezen: job ee6e7a81 hervatte van 21→63 met `watchdog_attempts=1`). Heartbeat is adequaat: `_run_with_heartbeat` tikt elke 60s tijdens ZOWEL audio-download als AssemblyAI-transcriptie (< 300s drempel). Zie [[caption-timedtext-proxy-rotatie]].
[2026-07-05] email-darkmode-colorscheme-en-logo: transactionele/broadcast-e-mail vereist een DOCUMENT-wrapper met color-scheme-declaraties + een dark-mode-veilig logo — nooit een zwart-transparant logo op een wit table-fragment. Twee gekoppelde regels. (1) COLOR-SCHEME: wikkel de verstuurde HTML in `<!doctype html><html><head>…</head><body>…</body></html>` en zet in de `<head>` `<meta name="color-scheme" content="light dark">` + `<meta name="supported-color-schemes" content="light dark">`, plus `color-scheme:light dark;` op body-niveau. Deze vertellen Gmail/Apple Mail dat dark mode is afgehandeld en dempen agressieve volledige inversie. Een kaal `<table>`-fragment (geen `<head>`) heeft geen plek voor deze meta's → clients inverteren ongeremd. (2) LOGO: afbeeldingen worden NOOIT mee-geïnverteerd door clients, dus een transparant logo leest maar op één achtergrond. Oplossing zonder media-query-swap: WITTE wordmark (`indxr-wordmark-white-transparent.png`) op een GENUINE-DONKERE header-cel `background-color:#141414` (NIET puur `#000000` — Apple Mail auto-flipt puur zwart). Wit-op-#141414 leest in zowel light als dark mode omdat clients écht-donkere achtergronden met rust laten. Laat de donkere balk de card-hoekradius volgen (`border-top-left/right-radius` gelijk aan de card) zodat het een intentionele header is, geen los blok. Concreet: `apps/app/src/lib/mail.ts` `renderBroadcastEmailHtml`. Media-query image-swap bewust buiten scope (fragiel over clients). Beide template-varianten (broadcast met unsubscribe-footer + service met privacy-link) delen dezelfde head/logo-constructie — alleen de footer verschilt. De `List-Unsubscribe`-header blijft losstaand in de send-payload (alleen bij `includeUnsubscribe && unsubscribeUrl`). Zie [[broadcast-email-html-template]].
[2026-07-05] playlist-resume-hydratatie: bij terugkeren naar een lopende playlist-job (navigeer weg → terug) verdween de per-video-lijst — alleen de progress-balk/teller bleef. OORZAAK: de lijst-rows renderen uit PlaylistManager's INTERNE `playlist`-state (`{title, entries}`, gated door `{playlist && …}` op regel 566 + `playlist?.entries?.map` op 632); die state is vluchtig en gaat verloren bij remount/navigatie. De `videoStatuses`-prop (per-video-status-badges) wórdt wél gehydrateerd uit DB `video_results`, maar zonder `playlist.entries` zijn er geen rows om die badges aan te hangen → lege lijst, terwijl de balk/teller (aparte render, gedreven door `videoStatuses`-counts + elapsedSeconds) blijft. REGEL: een resume-path moet de VOLLEDIGE UI-state herstellen, niet alleen de teller. Split naar bron: (a) dynamische per-video-STATE (statussen) hydrateren uit DB (`video_results`) — verandert live, moet autoritatief uit de DB komen; (b) statische display-data (titel/thumbnail per video) die NIET in de DB-job-rij staat (`video_metadata` is leeg; `video_results` bevat alleen `{status, transcript_id/error_type}`) → persisteren bij job-start (sessionStorage `entries`) en op resume terugzetten in de component-state. Concreet: PlaylistTab slaat `entries` op in sessionStorage bij start, leest ze op resume en geeft ze via nieuwe prop `resumePlaylist` aan PlaylistManager, dat zijn `playlist`-state seedt (`setPlaylist(prev => prev ?? …)` + `hasExtracted=true`). Toekomstige robuustheid (cross-device/andere tab): vul `video_metadata` (backend accepteert `{video_id:{title,duration,thumbnail}}`, main.py:208/1059) bij job-start zodat de entries ook puur uit de DB kunnen komen — vereist wijziging aan de start-payload/Next.js-route (buiten de resume-path-scope, apart oppakken). Zie [[arq-jobtimeout-en-interrupted-state]].
[2026-07-05] resume-uit-db-en-lijst-windowen: twee regels voor resume/lange-lijst-render. (1) sessionStorage telt ÓÓK als vluchtige client-state — een resume-path moet display-data uit de DB hydrateren, niet uit een sessionStorage-cache. Concreet playlist: de per-video display-data (titel, duur) hoort in de DB-job-rij via `video_metadata` ({video_id:{title,duration}}), meegestuurd in de start-payload; op resume bouw je de entry-lijst uit `job.video_metadata` + `job.video_ids` (die je toch al ophaalt voor de `video_results`-statussen). GEEN sessionStorage-entry-cache. LET OP de doorgifte-keten: de backend PERSISTEERT `video_metadata` bij job-start (main.py insert), maar de Next.js start-route Zod-schema STRIPT onbekende velden (`safeParse` → `result.data`) — een nieuw veld moet expliciet aan het schema worden toegevoegd, anders bereikt het de backend nooit. Zod v4: `z.record(z.string(), valueSchema)` (2 args), niet `z.record(valueSchema)`. (2) Lange lijsten WINDOWEN, niet volledig renderen: een 200-500-video-playlist mag nooit alle rows tegelijk in de DOM zetten — render standaard ~25 met een "Load More" (+25) en een "Showing X of Y"-indicator; de balk/teller toont het échte totaal (voortgang over alle N), de lijst is gewindowd. Geldt live én bij resume. In `PlaylistManager` bestond `visibleCount`/`loadMore` al; `availableCount = playlist?.entries?.length` zodat windowing automatisch ook bij resume werkt zodra `playlist.entries` gehydrateerd is. Bijkomend: playlist-lijst is nu titel-gedreven (thumbnails uit de row verwijderd — sneller, consistent met Library-stijl). Zie [[playlist-resume-hydratatie]].
[2026-07-06] credit-transactions-sign-conventie: debits in `credit_transactions` hebben ÉÉN consistente sign-conventie — **positief amount onder `type='debit'`**; de `type`-kolom draagt de richting, NIET het teken. Schrijf NOOIT een debit met negatief amount weg. Fout die dit blootlegde: `deduct_credits_atomic` (whisper) schreef debits positief, maar `update_playlist_video_progress` (caption) schreef `-p_amount` (negatief) — beide `type='debit'`. Gevolg: `SUM(credit_transactions)` reconcilieerde NIET naar `user_credits.credits` (whisper- en caption-debits hieven elkaar deels op), en de admin-metric "Credits Consumed" (`SUM WHERE type='debit'`) was structureel onderschat (met 2× de som van de negatieve rijen). De LIVE balans klopte wel — beide paden doen `credits = credits - p_amount` op `user_credits`; alleen het LOG en de afgeleide metrics waren fout. Ook een display-bug: `{type==='credit'?'+':'-'}{amount}` rendert een negatief debit-bedrag als "--1". Regel bij elke nieuwe credit-aftrek-route: teken = altijd positief, richting via `type`; en verifieer met de reconciliatie-invariant `user_credits.credits == SUM(type=credit) - SUM(type=debit)` per user. `user_credits.credits` is en blijft de gezaghebbende balans (4 RPC's onder `FOR UPDATE`); `credit_transactions` is een audit-log, geen balans-bron. Opgelost 2026-07-06 via `20260706172045_fix_caption_debit_sign` (RPC → `+p_amount`) + `20260706172114_backfill_debit_sign` (627 negatieve rijen → `ABS`); 6/6 users reconcilieerden diff=0 na de backfill.
[2026-07-06] additieve-kolom-op-hete-rpc-tabel: een NIEUWE, ge-CONSTRAINEDE kolom toevoegen aan een tabel waar bestaande RPC's/INSERT-paden al naar schrijven MOET non-breaking zijn — anders breekt elke bestaande INSERT die de nieuwe kolom niet zet. Regels: (a) kolom NULLABLE, geen NOT NULL (bestaande paden zetten 'm niet); (b) een CHECK-constraint moet NULL EXPLICIET toestaan (`CHECK (kind IS NULL OR kind IN (...))`), nooit `CHECK (kind IN (...))` — die laatste faalt op elke insert zonder kind; (c) een UNIQUE-index bedoeld voor toekomstige idempotentie maak je PARTIEEL met een `WHERE <nieuwe-kolom> IS NOT NULL`, zodat legacy-rijen (kolom NULL) erbuiten vallen en er geen collision ontstaat vóór de nieuwe code de kolom vult. Backfill van de klassificatie-kolom (bijv. `kind`) mag, maar backfill de JOIN-sleutel (`job_id`/`playlist_id`) van legacy-rijen JUIST NIET uit metadata — dat trekt ze de partiële UNIQUE-index in met collision-risico. Concreet toegepast bij ADR-050 credit-reservering-fundering (`20260706190140`, `20260706190216`): `kind`/`job_id`/`playlist_id` op `credit_transactions` nullable + `CHECK (kind IS NULL OR ...)` + twee partiële UNIQUE-indexen — de live RPC's (`deduct_credits_atomic`, `add_credits`, `update_playlist_video_progress`) zetten geen `kind` en blijven daardoor werken. Verifieer non-breaking vóór apply: zou een INSERT-vorm van de bestaande RPC's nog aan de nieuwe CHECK voldoen? Zie [[credit-transactions-sign-conventie]].
[2026-07-06] reserveren-idempotent-insert-first: een reserverings-/idempotente-aftrek-RPC bouw je als **insert-first onder FOR UPDATE**, NIET check-balance-first. Volgorde: (1) `FOR UPDATE` op `user_credits`; (2) probeer de reservering-rij te inserten met `ON CONFLICT DO NOTHING RETURNING id` (partiële UNIQUE `(job_id,kind)`/`(playlist_id,kind)` vangt de retry); (3) **geen** rij terug = al gereserveerd → return idempotent success ZONDER aftrek; (4) wél een nieuwe rij → pas dan saldocheck; onvoldoende → de zojuist ingevoegde rij weer DELETE'n en schoon hard-falen (niets afgetrokken). WAAROM insert-first en niet check-first: bij een idempotente retry ná een geslaagde eerste reservering is de balans al verlaagd — een check-balance-first zou de retry ten onrechte op `insufficient_credits` laten vallen. De conflict-detectie moet dus vóór de saldocheck. `reserve_credits` (`20260706200207`) doet dit; bewezen met een concurrent-overspend-test (2 jobs, samen > saldo → exact 1 slaagt, balans nooit negatief) + idempotentie-asserts voor job_id én playlist_id. **Rollout van financieel-kritieke multi-fase credit-changes: build dark, activate together** — reserve is gebouwd + bedraad achter `CREDIT_RESERVATION_ENABLED` (default OFF) zodat het live gedrag nul verandert (geen dubbele aftrek met de nog-bestaande per-video-aftrek); de flag gaat pas AAN samen met settle+refund, niet los. Zie [[credit-transactions-sign-conventie]] en [[additieve-kolom-op-hete-rpc-tabel]].
[2026-07-06] reserve-settle-refund-drie-fasen: de gedrags-fase van ADR-050 (settle+refund) heeft drie harde regels. (1) SETTLE IS BALANS-NEUTRAAL: bij een gereserveerde job/playlist is de balans al bij reserve bewogen; settle registreert alléén het WERKELIJKE verbruik als `kind='settlement'`/`type='debit'`-rij ZONDER `user_credits` te muteren — het is de som-bron voor de refund, geen balans-mutatie. Gevolg voor de reconciliatie-invariant: nieuwe settlements (herkenbaar aan `job_id`/`playlist_id` NOT NULL) worden UITGESLOTEN uit `balans == Σ(credit) − Σ(debit)` — anders tel je verbruik dubbel (reserve daalt de balans al). Legacy-settlements (fase-1-backfill, `job_id`/`playlist_id` NULL) blijven wél balans-affecterend; na de launch-reset (taak 1.26) vereenvoudigt de uitsluiting tot `WHERE kind <> 'settlement'`. En: de admin-metric "Credits Consumed" telt `SUM WHERE kind='settlement'`, NIET `SUM WHERE type='debit'` — anders tel je reservering + settlement dubbel (beide `type='debit'`) zodra de flag AAN gaat; "Credits Purchased" telt `type='credit'` EXCL. `kind='refund'` (NULL-kind blijft meetellen) zodat refund-credits de aankoop-metric niet inflateren. (2) REFUND IS ÉÉN NETTO-POST aan het eind: `refund = credits_reserved − Σ(settlements)`; positief → credit; negatief (verbruik > gereserveerd) → best-effort debit GECAPT op saldo (`LEAST(-refund, balance)`) + `RAISE WARNING`, NOOIT `RAISE EXCEPTION` — een al-gedraaide transcriptie mag niet alsnog falen (ADR-050 §4). Reason zegt bij negatief "bijbetaald", niet "teruggestort". Idempotent via insert-first (`ON CONFLICT (.,'refund') DO NOTHING` VÓÓR de balans-mutatie, onder `FOR UPDATE`). (3) BRANCH OP `credits_reserved > 0` (per-job/playlist), NIET op de globale flag: een in-flight job houdt het model van zijn eigen start, ook als de flag midden in zijn leven flipt → géén window met dubbele óf nul aftrek. De flag (`CREDIT_RESERVATION_ENABLED`) gate ALLEEN of NIEUWE jobs reserveren; de oude directe aftrek blijft als `else`-tak (flag-OFF/ongereserveerd = rollback-veilig, byte-identiek aan `20260706172045`). Caption-settlement-idempotentie = applicatielogica (`v_already_done` per-video-guard onder `FOR UPDATE`), GEEN index-backstop; whisper-settlement via de bestaande `(job_id,'settlement')` UNIQUE; de `(playlist_id,kind)` UNIQUE moest daarom `kind <> 'settlement'` uitsluiten (M4) want settlements zijn meervoudig per playlist. Gebouwd in `20260706205451/205619/205835/205918` + pipeline/worker; flag OFF bij commit — activering is een aparte 1-regel-follow-up ná prod-verificatie. Zie [[reserveren-idempotent-insert-first]].
[2026-07-07] recovery-pad-boek-geld-vóór-terminal-claim: op een financieel-kritiek recovery-/crash-pad mag je de status NOOIT terminal claimen vóór de geld-boeking. Volgorde MOET zijn: (1) muteer het geld EERST (idempotent), (2) CHECK de returnwaarde, (3) claim de status pas terminal bij BEWEZEN succes. Waarom: bij claim-vóór-boeking staat de job na een transient failure (522/timeout) op de boeking al terminal → wordt nooit meer geselecteerd (recovery-queries filteren op de niet-terminale status) → de refund wordt nooit opnieuw geprobeerd = STIL, PERMANENT geldverlies. Idempotentie beschermt tegen dúbbel, niet tegen núl — dus de reorder heeft een idempotente mutatie nodig (partiële UNIQUE `(job_id/playlist_id,'refund')`; voor een niet-idempotente helper zoals `add_credits` een idempotente variant bouwen, hier `refund_credits_flat`). Faalt de boeking → laat de status niet-terminaal zodat de volgende cyclus retry't, én alarmeer (error-Sentry). Concreet: de watchdog `Pass 2`/`Pass 2b` (`worker.py`) claimde `interrupted→error` en refundde daarná; gefixt naar refund-vóór-claim via `_refund_then_claim_job`/`_refund_then_claim_playlist`. BREDER patroon (op meerdere plekken fout te maken): een GENEGEERDE returnwaarde van `refund_credits`/`add_credits`/`refund_credits_flat` op ELK pad = stil verlies — check 'm altijd. Hetzelfde antipatroon zat in `run_whisper_reservation_aware` op het whisper-success-pad (refund genegeerd, géén watchdog-vangnet want `transcript_id` gezet) → daar minimaal luid alarmeren (geen retry-pad). En: een transient SELECT-522 op een recovery-lijst-query is NIET high-priority — muteert niets, retry't vanzelf → degradeer naar warning (`scope.set_level("warning")`, sentry-sdk default = error); reserveer error-level voor de boeking-failure. RPC-tests dekken de dispatch/cron-orchestratie NIET — test de faalmodus op het pad zelf (stub de mutatie → failure, assert niet-terminaal + geen boeking; dan success → precies één boeking, terminal; retry → idempotent). **Twee lagen die je op een terminaal refund-pad allebei nodig hebt** (een pad ZONDER watchdog-vangnet, d.w.z. `transcript_id` gezet / `status='complete'` → buiten Pass 2/2b): (a) een **bounded idempotente retry** op de refund-call zelf (2-3× korte backoff vóór het alarm) — een enkele error-Sentry zónder retry laat een transient 522 alsnog stil verliezen; refund_credits is idempotent via `(.,'refund')`, dus herproberen dubbel-refundt nooit. (b) een **reconciliatie-sweep** als structureel net: retry dekt de transient NIET wanneer de worker crasht TUSSEN de terminal-status-set en de refund-retries (job is dan al terminaal → buiten elk crash-pad). Een watchdog-pass met een **anti-join** (`terminale status + credits_reserved>0 + GEEN (job/playlist_id,'refund')-rij`, via een SQL-functie want PostgREST kan geen NOT EXISTS) vindt precies die gemiste refunds en boekt ze idempotent — **zonder de status te muteren** (de job is al terminaal en correct; er valt niets te claimen, anders dan Pass 2/2b). De anti-join is meteen de idempotentie (verdwijnt zodra de refund-rij bestaat) en moet ge-`LIMIT`-d zijn zodat een achterstand de cron niet laat overlopen. Concreet: `refund_with_retry` (`transcription_pipeline.py`) + watchdog `Pass 2c` / `watchdog_unrefunded_reserved` (`worker.py`, migratie `20260707102113`). Zie [[reserve-settle-refund-drie-fasen]] en [[reserve-vóór-dispatch-splitsing-alle-paden-bedraden]].
[2026-07-07] reserve-vóór-dispatch-splitsing-alle-paden-bedraden: `reserve_credits` draait in `main.py` (`/api/transcribe/whisper`) VÓÓR de `if source_type == "youtube"`-splitsing, dus het geldt voor ÉLK dispatch-pad dat daarna de transcriptie-pipeline aanroept. Gevolg: elk zo'n pad MOET reservation-aware zijn (pipeline `reservation_mode=True` → oude aftrek onderdrukt + settle) én een refund-hook hebben — een DIRECTE `do_assemblyai_transcription`-aanroep zonder die bedrading = bij flag ON reserve + oude per-video-aftrek samen = DUBBELE afrekening, en de reservering wordt nooit teruggeboekt. Concreet gemist bij de eerste fase-2-bouw: alleen `worker.run_whisper_job` (ARQ-pad) was bedraad; het **upload-pad** (`main.py`, `source_type != youtube`, `asyncio.create_task` — permanent, geen fallback) en de **arq-loze youtube-fallback** riepen de pipeline direct aan zonder `reservation_mode`/refund → prod-bereikbare dubbele aftrek voor elke upload-transcriptie. Fix: één gedeelde dispatch-primitief `run_whisper_reservation_aware` (`transcription_pipeline.py`) die credits_deducted+credits_reserved van de eigen job-rij leest, `reservation_mode` afleidt, de pipeline draait en ná afloop (success ÉN failure) `refund_credits(job_id)` roept; alle drie de standalone-paden (worker, upload, fallback) gaan er nu doorheen zodat de bedrading niet per call-site kan driften. Playlist-whisper gebruikt 'm NIET (refundt op playlist-niveau). REGELS: (a) reserve vóór een branch → verifieer ELKE tak die daarna geld uitgeeft; (b) meerdere call-sites van hetzelfde financieel-kritieke pad = één gedeelde primitief, niet gekopieerde bedrading (kopie #2 werd hier vergeten); (c) RPC-tests dekken de `main.py`-dispatch NIET — voeg een e2e-test op het dispatch-pad zelf toe (gestubde pipeline die settle simuleert) die bewijst dat balans exact één keer beweegt. Zie [[reserve-settle-refund-drie-fasen]].
[2026-07-07] reserve-bedrag-realistisch-vóór-de-gate-betekenis-heeft: een overspend-/quota-gate is alleen echt als het GERESERVEERDE bedrag realistisch is vóór de gate draait. Een reserve die stil terugvalt op het minimum (hier `ceil(0/60)`→1 credit) is een LEGE gate: de 402 checkt tegen 1, en het werkelijke verbruik wordt pas ná de dure operatie via settle/refund verrekend → een gebruiker met te weinig credits krijgt het meerdere gratis (bijbetaald-refund gecapt op saldo, `LEAST(-refund, balance)`, RAISE WARNING nooit EXCEPTION). Concreet: het **upload-pad** (`main.py /api/transcribe/whisper`) reserveerde 1 credit omdat de audio-duur pas ín de pipeline geprobed werd (ná reserve); YouTube reserveerde wél correct want de browser stuurt de duur (metadata) vóór reserve mee. REGELS: (a) stel het reserve-bedrag **server-side + onomzeilbaar** vast VÓÓR reserve — NOOIT uit een client-waarde (bij een directe JWT-upload is die volledig client-gecontroleerd → heropent de gate), en NOOIT stil naar het minimum terugvallen; (b) kun je de exacte kosten nog niet meten, **overschat royaal** (bij een gefaalde ffprobe: schat uit bestandsgrootte met een bewust lage bitrate-aanname) — overschatten is veilig want settle+refund corrigeren naar de echte duur; onderschatten heropent de gate; (c) de meting die je vóór reserve doet is voor de GATE, niet voor de afrekening — settle blijft de waarheid (probe in de pipeline op de echte duur), dus geef een geslaagde vóór-reserve-probe door om dubbel proben te vermijden maar laat een fallback-schatting NOOIT als settle-waarde tellen. Concreet: `estimate_upload_reserve_cost` (`audio_utils.py`) + probe-vóór-reserve in `main.py` (temp-bestand één keer geschreven, hergebruikt door de pipeline; `known_duration_seconds`-passthrough). Zie [[reserve-vóór-dispatch-splitsing-alle-paden-bedraden]] en [[reserve-settle-refund-drie-fasen]].
[2026-07-08] completion-receipt-één-component-stilte-bij-succes: financiële/status-feedback die per scherm ad-hoc gebouwd wordt driftt onvermijdelijk uit elkaar (drie completion-banners hadden drie verschillende groentinten + verschillende copy + de playlist toonde géén totaal-credits). REGEL: bouw zulke feedback als ÉÉN variant-gedreven, herbruikbaar inline component (`packages/shared/src/components/ui/CompletionReceipt.tsx`, gemodelleerd op `FeedbackCard`), niet per call-site. Ontwerpprincipe "stilte bij succes, uitleg bij afwijking": een schone job = één regel (alleen wat betaald is), géén reserve/refund-ruis als er niks terugkwam; pas bij een afwijking (refund, skip, overschatting) toon je de transparantie. Data-regel (financieel-kritiek maar READ-ONLY): lees de cijfers uit de gestructureerde `credit_transactions.metadata` van de refund-rij (`{reserved, consumed, refunded, ...}`) + de settlement-rijen — NOOIT de (Nederlandse) reason-string parsen, en verzin geen tweede balans-bron; de UI muteert nooit credits. Onderscheid scherp: refund-door-mislukking (playlist met skips → normale reserved→used→refunded-transparantie) is NIET hetzelfde als overschatting (upload-fallback waar de duur-schatting te hoog was → geruststellings-strook); trigger de strook semantisch (`kind='upload' && reserved > used`), niet op een credit-drempel. Balans-refresh hoort erbij: `refreshCredits()` moet óók bij job-START draaien (de reservering is server-side al gecommit vóór `job_id` terugkomt), niet alleen bij completion — anders loopt de topbar achter. Zie [[reserve-settle-refund-drie-fasen]] en [[reserve-bedrag-realistisch-vóór-de-gate-betekenis-heeft]].

[2026-07-08] playlist-retry-collection-scoped-receipt: een playlist-retry (per-video én "Retry all") draait als een APARTE playlist-job (`POST /api/playlist/extract` met een subset) met een eigen `playlist_id` maar dezelfde `collection_id` — GEEN interne retry-pass. Gevolg 1 (weergave): een receipt/aggregatie die op één `playlist_id` leest, bevriest op de eerste-run-snapshot ("10 transcribed · 2 skipped — 30 refunded") en ziet de retry-settlements (onder een ander `playlist_id`) nooit; de eerste-run-`video_results` blijft óók bevroren (die 2 video's staan er nog als error in). Een "her-fetch op dezelfde id" lost dit dus NIET op. FIX: aggregeer read-only over `collection_id` (alle collectie-jobs mergen, per video wint success van error, settlements/refunds sommeren) + een `refreshToken`-param die her-fetcht bij retry-completion (het anker-id verandert niet). Gevolg 2 (verrekening — die WÉL klopt): omdat elke retry-job hetzelfde reserve-pad (`_compute_playlist_reservation` + `reserve_credits`) hergebruikt en de worker met identieke formule settelt (reserve==settle per job), en de refund uitgesteld is tot ná de retry-pass, blijft de ledger consistent — geen dubbele charge, geen over-refund (bewezen tegen de echte ledger van collectie de42c5d4: reserved 154, used 153, net refund 1, 12/0). NUANCE (geen ledger-bug, wél revenue): de free-tier (`idx < 3` gratis captions) is index-gebaseerd en reset per job → een retry-subset krijgt z'n eerste ≤3 caption-video's opnieuw gratis; consistent met de bestaande per-video-retry, bewust zo gelaten (Policy K) om de settle-logica niet aan te raken. Retry-all = per-video-retry met een array via één gedeelde `_startRetryJob` — géén apart reserve-pad. Zie [[reserve-settle-refund-drie-fasen]] en [[completion-receipt-één-component-stilte-bij-succes]].
[2026-07-08] active-job-filter-twee-implementaties-sync: de "telt lopende jobs" filter bestaat op TWEE plekken — backend `_count_active_jobs` (`main.py`, service-role, expliciet op `user_id`, voor de concurrency-cap VÓÓR reserve) en frontend `ActiveJobsIndicator` (browser-client onder RLS, voor de weergave). Beide MOETEN dezelfde definitie gebruiken: niet-terminale statussen `transcription_jobs∈{pending,downloading,transcribing,saving}` + `playlist_extraction_jobs∈{running,retry_pending}`, ÉN de versheidsfilter `created_at <30m OF last_heartbeat_at <10m` (mirrort de dedup-query — sluit zombie/stale jobs uit, bv. de april-crashes). `interrupted` telt NIET mee (watchdog-herstelstaat). Bij wijziging: pas BEIDE aan. Cap-plaatsing is financieel-kritiek: de check MOET vóór de job-insert én `reserve_credits` (een geweigerde job mag nooit reserveren) — whisper: ná dedup / vóór insert; playlist: vóór insert. WAAROM niet één gedeelde bron: frontend leest onder RLS (auth.uid), backend met service-role op expliciete user_id — een gedeelde SQL-functie zou beide contexten niet schoon dekken; twee kleine implementaties + deze sync-regel is eenvoudiger. Oude bug: `ActiveJobsIndicator` telde sessionStorage-sleutels (één per job-type) → twee gelijktijdige same-type jobs werden als "1" geteld.

[2026-07-09] running-status-onzichtbaar-voor-recovery: elke NIET-terminale status waarin een job kan blijven hangen MOET door minstens één watchdog-pass gequery'd worden — anders is die status een permanente-lek-val. Concreet: een playlist_extraction_jobs-rij op `status='running'` waarvan de ARQ-keten stierf was onzichtbaar voor ELK recovery-pad (poll-endpoint flipt running→interrupted alléén bij aanwezige heartbeat `main.py:1257`; géén watchdog-pass query'de 'running'; Pass 1b one-shot op attempts=0) → 3 jobs permanent stuck incl. een 71-dagen zombie, reservering nooit gerefund. REGELS: (1) DETECTEER stuck jobs op een VOORTGANG-signaal (`last_progress_at`, fallback `created_at`), NOOIT op heartbeat-AANWEZIGHEID — een vers-gestarte job heeft legitiem NULL heartbeat en mag niet gereapt worden; gebruik heartbeat-VERSHEID hooguit als PROTECTIEVE guard (verse heartbeat = levende worker = niet rapen), niet als trigger. (2) refund op een reap = refund-VÓÓR-terminal-claim + `(playlist_id,'refund')`-idempotentie + CAS op de bron-status; skip refund bij reserved 0/NULL (pre-reservering zombie). (3) het money-loss-window bij refund-dan-claim is DICHT zolang de heartbeat-guard een levende worker uitsluit (die zou ná de refund nog kunnen settelen → huisverlies) — combineer met een per-stap-timeout zodat een healthy worker ALTIJD een verse heartbeat heeft. (4) een sequentiële keten heeft een per-stap wall-clock-timeout nodig (`asyncio.wait_for` op de yt-dlp-EXTRACTIE, NIET op de AssemblyAI-poll — een legitiem trage whisper mag niet gekilld) — anders blokkeert één hang de hele keten tot de ARQ `job_timeout` (2u). Zie [[recovery-pad-boek-geld-vóór-terminal-claim]] en [[reserve-settle-refund-drie-fasen]]; ADR-051.

[2026-07-09] discount-signaal-moet-reserve-EN-settle-bereiken (mirror-invariant): elke prijs-korting die per-video verrekend wordt, wordt op TWEE plekken berekend — de reservering vooraf (`_compute_playlist_reservation`, `main.py`) en de settle per video (`worker.py`, `is_free`). Die twee MOETEN exact spiegelen (reserve==settle per video), anders ontstaat een refund-mismatch (over-/under-refund = huisverlies of user-schade). Concreet (Policy K→S fix): de gratis-3 (`idx<3`) is index-gebaseerd en reset per job → een frontend-retry (aparte playlist-job met een subset) kreeg z'n eerste ≤3 caption-video's opnieuw gratis. FIX: een `is_retry`-signaal moet BEIDE kanten bereiken — kolom op de job-rij (`playlist_extraction_jobs.is_retry`), gezet vanuit `PlaylistExtractRequest.is_retry`, gelezen door zowel de reserve-functie als BEIDE worker-settle-passes (`process_playlist_video` én `process_playlist_retries`), zodat `is_free = idx<3 AND NOT is_retry`. Let op de twee proxy-hops die een nieuw veld STRIPPEN als je 't niet declareert: de Next.js zod-allowlist (`route.ts`) én het Pydantic-model — een veld dat je niet in beide zet, komt nooit aan. Auto-retry binnen dezelfde job (retry_pending/Pass 1b, `is_retry=false`) behoudt de originele index → geen dubbele belasting. Bewijs met een reserve==settle-property-test over meerdere vormen (`backend/test_retry_free_tier.py`). Zie [[playlist-retry-collection-scoped-receipt]] en [[reserve-settle-refund-drie-fasen]].

[2026-07-09] fk-introspectie-via-pg_constraint-niet-information_schema: `information_schema` (constraint_column_usage e.d.) mist FK's die naar een ANDER schema verwijzen (public → `auth.users`) en gaf vals "geen FK's" terug. Authoritatief is `pg_constraint` (`contype='f'`, `confrelid='auth.users'::regclass`, `confdeltype` voor de ON DELETE-regel). GECORRIGEERD 2026-07-09 (verving een eerdere foute lezing): de public-tabellen HEBBEN wél FK's naar `auth.users`, de MEESTE met ON DELETE **CASCADE** (profiles, credit_transactions, user_credits, collections, transcription_jobs, playlist_extraction_jobs, playlist_jobs, saved_videos, messages, support_tickets). Nog ÉÉN uitzondering na 2026-07-09: **`usage_logs` = SET NULL** (rij blijft, user_id wordt NULL). (`transcripts` was NO ACTION — een delete op een user mét transcripts blokkeerde — maar is 2026-07-09 op **CASCADE** gezet, migratie `20260709160000_transcripts_fk_cascade`.) REGEL: voor een user-delete kun je nu op CASCADE leunen voor vrijwel alles; hooguit `usage_logs` (SET NULL) laat genulde rijen achter. Verifieer ná afloop met een orphan-anti-join (`NOT EXISTS (SELECT 1 FROM auth.users …)`) dat het 0 is. Let op: niet elke user heeft een `profiles`/`user_credits`-rij (script-aangemaakte auth-users via de admin-API omzeilen de profiel-trigger). Geldt ook voor de 1.26 pre-launch-reset.

[2026-07-09] receipt-net-final-niet-bruto-cumulatief: een playlist-receipt dat over meerdere retry-rondes aggregeert MOET de netto-eindstand tonen, niet de bruto-som per ronde. Bug: de counts (transcribed/skipped) kwamen uit merged video_results (net-final, success-wint), maar 'used'/'refunded' waren Σ per-job (bruto-cumulatief) → "8 not used" bij maar 4 skips, want de reserve→refund→re-reserve churn van geretryde video's zat erin (een video gerefund in ronde 1 en her-verrekend in ronde 2 telde in beide). REGEL: leid ELK receipt-cijfer af uit de MERGED per-video eindstaat: `used` = Σ per-video settlement van video's die uiteindelijk slaagden (een video settelt één keer); `not-used` = Σ would-be-credits van uiteindelijk-overgeslagen video's (0 als free-tier). Dan telt charged+not-used op met transcribed+skipped en is de churn onzichtbaar. Weergave-only, raakt de ledger niet (die klopt: Σreserved==Σused+Σrefunded). Free-tier voor een skip: caption idx<3 in de ORIGINELE (niet-retry) run → rate 0; whisper skip → ceil(dur/60) (mirror calculate_credit_cost). Pure functie `packages/shared/src/hooks/receiptAggregation.ts`, testbaar via `node --experimental-strip-types`. Zie [[playlist-retry-collection-scoped-receipt]] en [[discount-signaal-moet-reserve-EN-settle-bereiken]].

[2026-07-09] playlist-progress-teller-transition-aware: `update_playlist_video_progress` telde een video die op retry error→success gaat DUBBEL (completed+1 zonder failed−1), want de idempotentie-guard `v_already_done` vuurt alleen bij gelijke status → `failed` overcount (An-Najm job1: 7 i.p.v. 6). REGEL: een niet-idempotente statusovergang moet de OUDE bucket verlaten én de nieuwe vullen: bij prev='error' & new='success' → failed−1 & completed+1 (symmetrisch success→error), met `GREATEST(0,…)` tegen underflow op bestaande scheve tellers. De receipt gebruikt deze teller NIET (die leest video_results), maar 'failed' moet kloppen voor admin/interne consistentie. Migratie herberekent bestaande TERMINALE jobs uit video_results (terminal-only i.v.m. de FOR UPDATE-lock van in-flight jobs).

[2026-07-09] retryable-outcome-hergebruik-bestaande-slug: een nieuw retrybaar error-type toevoegen? Map het op een BESTAANDE retryable slug die al door élke retry-gate bedraad is, i.p.v. een nieuwe slug. Een status als `timeout` zit op ~13 plekken: backend `_classify_download_error`, twee Sentry-exclusion-sets (pipeline:420 + worker:474), de RPC `v_has_retryable` (bepaalt `retry_pending`), de worker auto-retry-filter (worker:531), en frontend VideoStatus-type, allDone-check, botOrTimeout-count, retry-filter, retry-knop-label, progress-bar-set, AlertCircle, badge, én `mapBackendStatus`. Een NIEUWE slug moet op ALL die plekken toegevoegd (+ een RPC-migratie) of de feature is half-bedraad (bv. wel een knop maar geen auto-retry, of telt niet als "done"). Concreet (Fix A): connection/network-fouten → hergebruik `timeout` = 1 wijziging (keywords in de classifier) i.p.v. ~13 + migratie voor een `connection_error`-slug; de badge zei toevallig al "Connection timeout". Alleen als downstream-gedrag écht moet verschillen (andere backoff/afhandeling) is een nieuwe slug het waard. Verifieer bij zo'n keuze met een grep op de bestaande slug hoeveel plekken 'm noemen.
[2026-07-10] git-workflow: de oude regel "CC commit/pusht NOOIT zonder expliciete toestemming" ([2026-05-05]) is VERVANGEN → norm sinds 2026-07-10 (geautoriseerd door Khidr, CLAUDE.md "Na elke voltooide taak" punt 3): CC commit en pusht zelf na elke voltooide, geverifieerde taak (build groen + verificatie), met Co-Authored-By-footer. Alleen bewezen werk pushen; een falende taak niet.
[2026-07-12] caption-native-anchoring-geen-taalvoorkeur: de captie-cascade moet ALTIJD de originele/native track leveren, NOOIT een vertaling, en NOOIT een taal prefereren/hardcoden. `lang_pref` (YouTube Data API `defaultAudioLanguage`) is onbetrouwbaar en mag de trackkeuze niet sturen. De 2026-06-27 tlang-fix dekte alleen `extract_with_ytdlp` en had twee gaten die `Bm1RhjcdJek` (Napoleon, Engelse audio) een Albanees transcript lieten geven: (1) manuele subs zijn NIET inherent native — een video kan tientallen menselijke community-VERTALINGEN dragen; de oude Priority-1 iterereerde `['en'] + list(manual_subs.keys())` en viel bij ontbrekende bare-`en`-sleutel (het Engelse spoor heet `en-GB`) terug op `manual_subs.keys()[0]` = de eerste willekeurige taal (`sq`); (2) geen `-orig`-track (geen native-ASR-marker) → Priority-2 ving niks. FIX: anker op de NATIVE taal via twee betrouwbare signalen — yt-dlp `info['language']` (audio-taal, bv. `en-GB`) én de `-orig`-sleutel (structurele native-ASR-marker) — en selecteer ALLEEN een track waarvan de base-taalcode (`_base_lang`: `en-GB`→`en`) de native matcht: P1 manueel-native, P2 `-orig`-ASR-native, P3 non-orig auto-native zonder `tlang=`, anders `no_captions`. Stap 1 (`youtube-transcript-api`) idem: lees de generated (`is_generated=True`) ASR-track = native, kies manueel-native of native-ASR, nooit `.translate()`; geen ASR-track → `return None` → laat de yt-dlp-cascade (met `info['language']`) beslissen. Base-code-match dekt regionale varianten (`pt-BR`/`en-US`). Bij zowel single-video (`main.py`) als playlist (`worker.py`) — gedeelde helpers. Geverifieerd tegen echte repro's (Napoleon→en, Arabisch→ar native, Japans→ja native). Zie [[caption-timedtext-proxy-rotatie]] en ai-pipeline.md.
[2026-07-12] security-definer-read-rpc-cross-user: een `SECURITY DEFINER` read-RPC die een `p_user_id`-parameter accepteert EN aan `authenticated`/`anon` is gegrant, is een cross-user privacy-lek — de definer bypasst RLS, dus élke caller leest élke user-id die hij meegeeft. Bewezen op `get_user_credits(p_user_id)`: ingelogde user A las 1339 cr van user B via directe `rpc('get_user_credits', B-id)`. REGEL: een SECURITY DEFINER read-RPC mag de meegegeven user-id NIET vertrouwen voor niet-service callers — forceer binnen de functie `v_target := auth.uid()` voor authenticated en negeer de parameter; laat het `p_user_id`-pad alleen open voor `service_role` (detecteerbaar als `auth.uid() IS NULL`, want de backend-service_role-client draagt geen JWT). Revoke `anon`+`PUBLIC` EXECUTE als er geen anon-caller is (verifieer: staan alle frontend-callsites achter `if (user)`?). `auth.uid()` blijft correct binnen SECURITY DEFINER (leest `request.jwt.claims`, niet de executing role). Dit is de READ-tegenhanger van de MUTATE-lockdown in `20260711170300_lock_credit_rpcs` ([[auth-and-security]] RPC-tabel): mutate-RPC's → service_role-only; read-RPC met user-param → auth.uid()-forcering. Nieuwe SECURITY DEFINER-RPC met een user-id-param = altijd deze audit vóór grant. Test-recept: `SET LOCAL role authenticated` + `set_config('request.jwt.claims', json met sub=A)` in een `BEGIN…ROLLBACK`, roep de RPC met B's id aan, verwacht A's data. Migratie `20260712204359_get_user_credits_own_only`.
[2026-07-12] caption-cache-lagen-purge-én-self-heal: een caption-fix die lokaal werkt maar in PRODUCTIE de oude output blijft geven = bijna altijd een STALE CACHE, niet de code. De caption-extractie heeft DRIE lagen vóór de cascade (`main.py:/api/extract/youtube`): (1) Redis `caption:{video_id}`, (2) `master_transcripts` (Supabase-row + R2-JSON, language-aware op `normalised_lang` uit de YouTube Data API), (3) de cascade zelf. REGELS: (a) Redis legen is NIET genoeg — de master-hit **backfilt Redis opnieuw** (`main.py` ~349-359), dus een vergiftigde master-row her-vergiftigt Redis bij de volgende request; purge ALTIJD beide lagen (Upstash `DEL caption:{id}` via REST + `DELETE FROM master_transcripts WHERE video_id=… AND language=… AND transcription_model=…`). (b) De master-row is language-keyed op `normalised_lang` (Data API), NIET op de werkelijke transcript-taal → een pre-fix bug die Albanees onder `language='en'` opsloeg wordt geserveerd zodra Data API `'en'` teruggeeft (niet-deterministisch: bij quota-fail → `normalised_lang=None` → master-read overgeslagen → cascade → correct; verklaart "soms wel soms niet"). (c) De caption master-write was **insert-only** (`force_refresh=False`) → 409 duplicate-key → een correcte her-extractie kon de vergiftigde row NOOIT overschrijven (= waarom retry/redeploy niet hielp) én de 90-dagen-refresh werkte nooit (`fetched_from_provider_at` bleef staan → verlopen row her-extraheert eeuwig). FIX: caption-write `force_refresh=True` (UPSERT) in ZOWEL `main.py` als `worker.py` → correcte extractie self-heal't de row + refresh't de timestamp; de write vuurt alleen bij cache-MISS dus overschrijven met verse content is per definitie gewenst. Bij het landen van een caption-content/taal-fix: purge de master-rows (+ Redis) van de getroffen video's, niet alleen Redis. Bewijs LIVE via de ECHTE endpoint (`indxr.ai/api/extract`) + Railway-logs (`native manual subtitle`/`native ASR`/`master_cache write OK`), nooit een lokaal testscript. Zie [[caption-native-anchoring-geen-taalvoorkeur]] en [[caption-timedtext-proxy-rotatie]].
[2026-07-12] extractresponse-language_detected-is-bool: `ExtractResponse.language_detected` is `Optional[bool]` (True=runtime-gedetecteerd via lingua, False=bekend uit bron `info['language']`), GEEN taalcode. Het master-cache-HIT-pad (`main.py`) zette het op `mc.get("language")` (een string als 'ar'/'ja') in zowel de hit-response als de Redis-backfill → Pydantic weigerde de string → elke caption-master-hit met lege/afwezige Redis gaf 400 op de HELE request (intermittent: alleen als dat pad geraakt werd; opgedoken op Arabisch `jKz9GLqhuPo`). Pre-existing sinds b666048 (2026-05-01). REGEL: op een cache-hit-pad dat een response-model (Pydantic) opbouwt, moet ELK veld het gedeclareerde type hebben — een string in een bool-veld 400't stil pas op de hit-code-pad, niet op de miss/write-pad, dus lokale miss-tests zien het niet. De malformed-cache-guard (`CACHED_CAPTION_REQUIRED_KEYS`) checkt alleen KEY-aanwezigheid, geen TYPE — vertrouw daar niet op voor type-veiligheid. Fix: cache-hit zet `language_detected=False` (taal is bekend uit cache, niet gedetecteerd). String-vergiftigde Redis-entries self-healen via fall-through: de gefaalde Redis-hit wordt gecatcht → valt door naar de nu-correcte master-read die Redis correct herbevult.
[2026-07-13] gratis-grant-canoniek-email-dedup: elke gratis per-account credit-grant (welkomst-credits, en straks het geplande referral-programma) is misbruikbaar via e-mailaliassen — Gmail negeert alles na `+` (`naam+x@`) én puntjes (`na.am@` = `naam@`), en `googlemail.com`==`gmail.com`; Supabase Auth ziet elk als een los account → oneindig gratis grants. Een per-account-guard (`welcome_reward_claimed`) stopt dit NIET. REGEL: dedup zulke grants op een **canoniek e-mailadres** via `public.normalize_email(text)` (strip `+tag`; voor gmail/googlemail puntjes uit local-part + domein-canon; lowercase) — niet op het rauwe adres of user_id. Doe de dedup op **grant-niveau** in de RPC (het chokepoint voor álle callers), niet als signup-block: dat breekt geen bestaande accounts en geen legitieme `+addressing`-gebruikers (die mogen inloggen + gratis features, krijgen alleen de grant niet twee keer). Race-veilig zonder schema-kolom via `pg_advisory_xact_lock(hashtext('...:'||canon))` vóór de EXISTS-check; de RPC is SECURITY DEFINER (owner=postgres) dus mag `auth.users.email` lezen. Test reversibel met `BEGIN; INSERT INTO auth.users(id,email)…; UPDATE profiles SET welcome_reward_claimed=false; SELECT claim_welcome_reward(…); … ROLLBACK;` (alleen `id` is verplicht in auth.users; 3 INSERT-triggers vuren maar rollen terug). EERLIJKE GRENS: normalisatie stopt de alias-truc, NIET tien écht verschillende mailadressen — inherent aan gratis-instap zonder betaalmuur; zwaardere laag (device-fingerprint/betaalmethode, ADR-024) pas bij bewezen abuse. Migratie `20260712220428`. Zie [[credit-system]] en auth-and-security.md.

[2026-07-14] money-model-aggregaat-nooit-twee-rollen: een dag-aggregaat-teller (`daily_cost_counters`) kan géén intern/extern- of free/paid-split dragen en mag NOOIT tegelijk als OPEX-regel én als COR-basis dienen. De caption-COR werd geschat als `verbruikte_caption_credits × (dagteller_bytes/dagteller_count)` terwijl diezelfde dagteller óók als free-funnel-OPEX telde → dubbeltelling + de schatting werd op álle credits toegepast incl. cache-hits (0 egress). REGEL: alles wat in het money-model gesplitst moet worden (intern/extern, free/paid, cache-hit/miss) hoort op een **per-event rij** met snapshots (`usage_logs`: `is_internal_at_time`, `had_paid_at_time`, `credits_used`, `cache_hit`, `proxy_bytes`), niet op een aggregaat; laat de aggregaat-teller uitsluitend over voor het écht niet-splitbare deel (anonieme captions). COR = gemeten (`credits_used>0`), niet geschat; funnel-OPEX = `credits_used=0`; cache-hits schrijven `proxy_bytes=0` zodat ze $0 tellen. Elke gemeten kost-eenheid landt in EXACT één P&L-regel — verifieer met een sluit-test (Σ buckets == onafhankelijk totaal, 0 overlap/gap). Kost-egress ook capturen op MISLUKTE operaties (hang de bytes aan de exception) en over ALLE retry-pogingen sommeren (een partial download trok al echte proxy-egress). Migraties `20260713222920`/`20260713223907`, ADR-057. Zie [[credit-system]].

[2026-07-14] health-teller-staleness-guard-én-null-partitie: een "in flight / actieve jobs"-teller moet een STALENESS-GUARD hebben (genuinely-active = vers aangemaakt <30m OF verse heartbeat <10m), anders telt een dode/zombie-job (bv. een legacy 'downloading' die nooit afrondde) eeuwig mee — precies wat de Operations-tab deed ("1 in flight" sinds de tab live ging, terwijl de job uit april stamde). De frontend `ActiveJobsIndicator`/`main.py`-dedup hadden de guard al; de nieuwe `admin_operations_summary`-RPC niet → houd ELKE nieuwe actieve-job-teller in sync met die definitie. TWEEDE valkuil (bij het bouwen van de guard): een `(created_at > X OR last_heartbeat_at > Y)`-predikaat met NULLABLE `last_heartbeat_at` levert bij NULL geen FALSE maar NULL → de rij valt uit ZOWEL de "fresh"- als de "NOT fresh"-FILTER (verdwijnt uit beide totalen; `total` klopte niet). REGEL: COALESCE de nullable timestamp naar `-infinity` (of expliciet `col IS NOT NULL AND col > Y`) zodat het predikaat altijd TRUE/FALSE is en `fresh`/`stuck` de actieve jobs VOLLEDIG partitioneren. Toon stuck-jobs apart (niet stilzwijgend wegfilteren — een stuck job is een operationele anomalie). Termineer een echt-dode job alleen als 'ie financieel schoon is (`credits_reserved=credits_refunded` AND NOT `credits_deducted`) — de watchdog bezit de reserved-jobs (ADR-050). Migraties `20260714125449`/`20260714125540`.
[2026-07-14] stripe-checkout: de checkout-route (`apps/app/src/app/api/stripe/checkout/route.ts`) bouwt line_items met **inline `price_data` + inline `product_data`** (unit_amount = `pkg.priceEur*100`, product ad-hoc uit `name`/`description`/`images`) — géén `price:`-ID en géén `product:`-referentie. Gevolg: de Stripe **Dashboard/Catalog-producten** (`stripeProductId` = `prod_...`) en **lookup keys** (`stripeLookupKey`) in `pricing.ts` worden NERGENS in de code gelezen en hebben **nul runtime-effect** op betaling of credit-grant. Prijs komt 100% uit `pricing.ts`; credits komen uit `session.metadata.credits`. Wijzig prijs/credits dus in `pricing.ts` (redeploy = live), niet in het Stripe Dashboard. De `prod_`/`lookup_key`-velden zijn puur een administratieve mirror — bij afwijking is er geen betaalbug, alleen een documentatie-drift. Ad-hoc products verschijnen wél als losse producten in Stripe-reporting (niet gekoppeld aan de 4 Catalog-producten).
[2026-07-15] pricing-twee-oppervlakken: pricing wordt op TWEE plekken gerenderd — marketing `indxr.ai/pricing` én app `app.indxr.ai/dashboard/billing` — beide gedreven uit `packages/shared/src/lib/pricing.ts` (PACKAGES + `prominent`/`mostPopular`) en sinds 2026-07-15 via één gedeelde presentatie `packages/shared/src/components/pricing/PricingTiers.tsx` (actie als `renderCta`-prop: app=checkout-fetch, marketing=auth-aware navigatie). Een redesign/wijziging op ÉÉN oppervlak is per definitie incompleet — verander ze SAMEN en verifieer beide. Bijkomende val: de Stripe-checkout-route bestaat alléén op de app-host; een marketing-knop mag GEEN relatieve `fetch('/api/stripe/checkout')` doen (404 op marketing-host) én geen cross-origin fetch (Supabase auth-cookie is `SameSite=Lax` → reist niet mee op cross-origin fetch, alleen op top-level navigatie). Marketing→checkout MOET via top-level navigatie naar `app.indxr.ai/dashboard/billing?checkout=<plan>`.
[2026-07-15] Stripe: hardcoded payment_method_types in de Checkout Session overschrijft de Dashboard payment method configuration → gebruik dynamic payment methods; test altijd met een non-card methode (iDEAL), want Apple Pay/Link komen ook door een kaart-only config heen.
[2026-07-15] rpc-regressie-embed: als een RPC (`_geld_scope`) rauw geëmbed wordt in een andere RPC-output (`admin_geld_summary`), dan breekt élke toegevoegde output-key een byte-identieke regressie-diff. Bij een range-refactor: houd de KEY-SET identiek en laat elke waarde bij defaults (-infinity/+infinity) samenvallen met de all-time-uitkomst (FLOWS op [from,to), STOCKS/recognitie cumulatief-<to, `recognized_revenue` = cum_to−cum_from → bij from=-inf = cum_to). Nieuwe velden (stripe_fee, honest vat_computed) horen dan in de NIEUWE RPC (`admin_finance_summary`), niet in de geëmbede. Leg de baseline-output vóór de refactor vast en bewijs `n = baseline` met jsonb-`=`.
[2026-07-15] net-profit-goodwill-dubbeltelling: `granted_delivery_cost` (goodwill) is het gratis-aandeel van de VOLLE gemeten COR (`cor_ai/cap/sum/rag` = alle verbruik, betaald + gratis) — het is een REPORTING-split, GEEN extra kostenpost. Net profit = `revenue_delivered − volle_COR − funnels − stripe_fee`. Goodwill NOOIT los aftrekken náást de COR-termen (deed de eerste snapshot-versie → net te laag). Als je goodwill als aparte OPEX-regel toont: dan moet COR-tegen-omzet de purchased-share zijn (niet de volle COR), zodat `cor_against_revenue + goodwill = volle_COR`.
[2026-07-15] amsterdam-dag-grain: dag-grain-aggregaties in een periode-RPC (anon-funnel uit `daily_cost_counters`, entered-accrual, dag-telling) moeten de `[from,to)`-timestamptz eerst `AT TIME ZONE 'Europe/Amsterdam'` naar een datum brengen — NIET `timestamptz::date` (dat gebruikt de sessie/UTC-tz). Anders selecteert de tab een andere dag dan de snapshot (die op `snapshot_date` = Amsterdam-dag attribueert) en faalt de sluit-test `snapshot == live` op de external-rij (de ~2u UTC↔Amsterdam-shift).
[2026-07-15] stripe-fee-details: boek Stripe's tarievenlijst NOOIT na in code (geen hardcoded 1,5%/€0,25/0,4% — rates verouderen + verschillen per methode/herkomst). Lees `balance_transaction.fee_details` (self-describing type+amount+currency); `balance_transaction.fee` (totaal) is leidend. De fee is soms op checkout-moment nog niet settled → altijd een reconcile-pad (`PaymentIntent→charge→balance_transaction`) naast de best-effort webhook-capture. Stripe-fee = measured OPEX op VERKOOPDATUM, nooit COR. De on-demand invoicing-fee zit NIET in `fee_details` (ADR-053 `paid_out_of_band` → geen charge) → apart gefactureerd → ENTERED-regel, niet stilzwijgend €0.
[2026-07-15] mcp-execute-sql-laatste-statement: `mcp__supabase__execute_sql` retourneert alleen het resultaat van het LAATSTE statement in een multi-statement-string. Zet de SELECT die je wil zien als laatste (of splits). En: een data-modifying CTE (`WITH ins AS (INSERT...), del AS (DELETE ... WHERE id IN (SELECT id FROM ins))`) ziet de zojuist-geïnserteerde rij NIET (alle CTE's delen dezelfde snapshot) → de DELETE verwijdert 0 rijen. Ruim testrijen op met een apart DELETE-statement.
[2026-07-15] auth-funnel: een post-auth redirect-doel (bv. checkout-intent) moet door de HELE auth-flow gethread worden, niet alleen de laatste stap. De onboarding-gate is de valkuil: `loginAction` én `auth/callback` sturen users met `onboarding_completed=false` naar `/onboarding` VÓÓR ze `next`/`redirectTo` honoreren, en onboarding-completion ging hardcoded naar `/dashboard` → doel viel stil weg. Threading-checklist: (1) email/password login → `/onboarding?next=`; (2) `signupAction` `emailRedirectTo=/auth/callback?next=` (e-mailverificatie staat AAN → `mailer_autoconfirm=false`, de link is de enige drager); (3) `loginWithGoogleAction` OAuth `redirectTo=/auth/callback?next=` (form heeft een hidden `next`-input nodig); (4) `auth/callback` onboarding-incompleet → `/onboarding?next=`; (5) onboarding-completion honoreert `next`; (6) login/signup-pagina's dragen `next` door via de onderlinge links (signup-link, "Log in"-link). Centraliseer de open-redirect-guard in `packages/shared/src/lib/safe-redirect.ts` (alleen `app.indxr.ai`/localhost) i.p.v. per-callsite dupliceren. Verifieer met een VERS onboarding-incompleet account (admin.createUser `email_confirm:true`, `onboarding_completed=false`) — een ge-onboard account test de gate niet. LET OP bij Playwright-asserts: match op `new URL(u).pathname`, niet op de volle URL — een `next`-query die "dashboard"/"billing" bevat matcht anders vals-positief.
[2026-07-15] recognitie-cumulatief-clawback: bereken omzet-recognitie NOOIT uit cumulatieve totalen (`LEAST(purchased, GREATEST(0, consumed − granted))`). Dat herrekent achteraf en kan het VERLEDEN wijzigen: een goodwill-grant NÁ aankoop+verbruik verlaagt met terugwerkende kracht de al-erkende omzet (retroactieve clawback zonder refund). Khidr grant structureel aan betalende klanten → dit raakt de echte economie. Doe recognitie CHRONOLOGISCH: speel de event-stream op `created_at`-volgorde af (credits vóór debits bij gelijke tijd), granted-first, met FIFO purchase-lots die elk hun eigen `€/credit` dragen (`_recognize_asof`). Invariant: `recognized + deferred = purchased_net`. Een event met `created_at ≥ p_to` valt buiten `_recognize_asof(..., p_to)` → bevroren snapshots verschuiven niet. Bewijs ALTIJD reversibel (BEGIN/ROLLBACK) vóór én ná: koop→verbruik→recognized, dan grant, toon dat recognized BLIJFT (niet daalt). Zie ADR-061.
[2026-07-15] recognitie-cross-user-pooling: aggregaat-formules die per-user-logica nabootsen zijn fout zodra er meerdere users zijn — en een test met ÉÉN user kan het niet betrappen (bij één user is gepoolde == per-user, en de invariant `recognized+deferred=purchased_net` houdt ook bij cross-user-pooling stand). Granted-first is per-PORTEMONNEE: user A's gratis credits mogen nooit user B's verbruik compenseren. `_recognize_asof(p_users uuid[])` voegde eerst alle users in één event-stream met één gedeeld `granted_bal` → A's ongebruikte grant at B's erkende omzet op (recognized €15→€14,06) en vergiftigde `purchased_share` → COR-splitsing (against_revenue vs goodwill). Fix: buiten-loop PER USER (eigen granted_bal + eigen FIFO-lots), pas daarna sommeren over de scope. Test financiële recognitie ALTIJD met ≥2 users met verschillende profielen (één die alleen grant+niet-verbruikt, één die koopt+verbruikt) — reversibel, tegen de echte scope-RPC. Controleer expliciet dat purchased_share óók per-user blijft (als recognitie per-user rekent maar share nog uit aggregaten komt, is de COR-splitsing alsnog vergiftigd).
[2026-07-15] auth-verificatie: SSR-bewijs dat een redirect-URL correct wordt opgebouwd is geen bewijs dat hij werkt → test de e-mailtak met admin generateLink en klik de echte link, niet met een admin-aangemaakt account via de login-tak.
[2026-07-15] auth-verificatie-methode: `admin.generateLink(type:signup)` produceert een NON-PKCE (implicit-flow) link → tokens in de URL-fragment, GEEN `?code=`; die valt door de `/auth/callback` (die `?code=` verwacht) en landt op `/dashboard` → lijkt vals-positief op een bug. Echte @supabase/ssr-signups zijn PKCE (`auth.flow_state.code_challenge_method='s256'`), dus de link redirect mét `?code=`. Faithful test: doe een ECHTE form-signup in de browser (zet de code_verifier-cookie), lees de `confirmation_token` uit `auth.users` via de Management-API SQL-endpoint (PostgREST kan `auth`-schema niet lezen), bouw `.../auth/v1/verify?token=<token>&type=signup&redirect_to=<…>` en klik in DEZELFDE browsercontext. Uitkomst 2026-07-15: `next` overleeft → `/onboarding?next=` → billing → Stripe (funnel GROEN). Supabase redirect-allowlist `https://indxr.ai/**` accepteert query-strings (de `**` matcht `?next=…`), dus geen extra allowlist-entry nodig.
[2026-07-15] signup-test-domein: Supabase's PUBLIC signup (anon `signUp`) weigert `@indxr-test.com` met "Email address invalid" (geen MX/geldig domein). Gebruik `@indxr.ai` (eigen domein, bounce is onschadelijk) of een echt domein met MX voor E2E-signup-tests; of maak accounts admin-side aan (`auth.admin.createUser` met `email_confirm:true` accepteert `@indxr-test.com`, maar dan test je de login-tak, niet de e-mailtak). Let op de ingebouwde mailer-limiet `rate_limit_email_sent=2`/uur — bij E2E-signup-tests tijdelijk ophogen via Management-API `/config/auth` en daarna terugzetten.
[2026-07-15] sql-null-boolean-filter: een `FILTER (WHERE NOT cond)`-teller slaat rijen waar `cond` NULL is STIL over (NULL is niet true en niet false). Bij `cond = (metadata->>'k' = 'x')` is dat NULL zodra de key ontbreekt → de teller mist precies de gevallen die je wilde vangen (hier: sales zonder gemeten BTW telden als 0 i.p.v. 2). Maak de expressie altijd een echte boolean met COALESCE: `COALESCE(metadata->>'k','') = 'x'`. Verifieer een teller/flag ALTIJD tegen data waarvan je het antwoord kent (2 historische sales → verwacht count=2), niet alleen tegen "draait zonder error".
[2026-07-15] stripe-valuta-bron: houd P&L-bedragen in ÉÉN valuta uit ÉÉN bron = de settlement-valuta (EUR) uit de balance_transaction (amount/net/fee zijn al omgerekend). amount_paid/currency uit de Checkout Session zijn PRESENTMENT (klant-valuta, bij Adaptive Pricing bv. USD) → alleen als informatie bewaren, nooit in een P&L-som mengen. VAT in EUR = amount_tax(presentment) × balance_transaction.exchange_rate. Zolang alles EUR is (exchange_rate=1, settlement=amount_paid) is de bug onzichtbaar; bewijs 'm daarom met een GESIMULEERDE non-EUR sale (settlement 9,20 / presentment 10,00) en check dat charged=9,20 (niet 10,00) en charged−vat=revenue_ex_vat sluit.
[2026-07-15] stripe-automatic-tax-checkout: automatic_tax staat NIET automatisch aan op een Checkout Session omdat het op de factuurroute aan staat — het zijn losse API-calls. Zet op de sessie expliciet automatic_tax:{enabled:true} + line-item tax_behavior:'inclusive' + product_data.tax_code (vertrouw niet op de account-default 'inferred_by_currency': die sluit BTW UIT bij USD/CAD). Werkt samen met Adaptive Pricing (tax op integratievaluta, dán omreken). Zonder dit is session.total_details.amount_tax = 0 en staat álle omzet ~21% te hoog. "BTW = 0" ≠ "BTW onbekend": leg tax_status='complete' vast als bewijs dat er echt gemeten is (ook een legitieme 0 bij US/verlegd), en markeer de rest expliciet als onbekend.
[2026-07-15] één-bron-hiërarchie-per-grootheid: definieer de bron-hiërarchie van een financiële grootheid (hier BTW: tax_status='complete' → invoice_tax → unknown) in ÉÉN functie die élke lezer aanroept. Bug die dit blootlegde: `measured` accepteerde invoice_tax terwijl vat_eur/net_lot alleen amount_tax lazen → een sale met invoice_tax=0.61 en amount_tax=0 werd measured=true (waarschuwing weg) maar netto BTW-inclusief → omzet 21% te hoog. Zodra twee lezers dezelfde grootheid uit verschillende bronnen afleiden, lopen ze uiteen. Refactor: `_sale_vat(m)->{vat,status}`, aangeroepen door _geld_scope (vat+measured+vat_by_country) én _recognize_asof (net_lot). Bewijs met synthetische sales die precies op de discrepantie mikken (A gemeten, B alleen-invoice, C gemeten-0).
[2026-07-15] gemeten-nul-is-geen-ontbrekende-meting: een gemeten 0 (US/verlegd: geen EU-BTW verschuldigd) is een VOLLEDIGE meting, geen ontbrekende. Leg de meet-STATUS apart vast van de WAARDE: `_sale_vat` geeft `{vat:0, status:'measured'}` bij tax_status='complete', niet `{vat:null}`. Rapporteer omzet als de som over measured-sales, met een aparte regel (count+gross) voor unknown — tel unknown nooit stil bij een land/bucket op. Anti-patroon: `bool_or(has_tax)` (key bestaat) als "vat_computed" gebruiken terwijl een aparte teller de sale als onbekend rapporteert — die twee kunnen niet uit dezelfde bron komen; kies de status-bron en verwijder de andere (hier: dood `vat_known` verwijderd).
[2026-07-15] skip-conditie-hoort-bij-de-taak: een skip-conditie moet de vraag stellen die bij de taak hoort — "heeft deze sale al veld X?" — niet "is er ooit iets aan deze sale gedaan?". De reconcile-route skipte een sale zodra `fee_details` bestond; de BTW/land-backfill erachter (customer_country/invoice_tax) draaide daardoor nooit → beide oude sales geskipt, backfill deed niets terwijl de code er wél was. Derde keer dat een guard/expressie stil het verkeerde doet (NULL-boolean FILTER, measured-vs-vat_eur, nu deze). Fix: bepaal per veld of het ontbreekt (needFees/needSession/needInvoice) en rapporteer per sale wat wél/niet is bijgewerkt. Verifieer een backfill ALTIJD tegen de teller die hij hoort te verlagen (vat_unmeasured 2→0), niet tegen "draaide zonder error".
[2026-07-15] per-user-aggregaat-NULL-COALESCE: bij het per-user maken van een scope-aggregaat (COR-share, ADR-063) verbergt de scope-versie een NULL-bug die de per-user-versie blootlegt. `sum(duration)*rate + sum(bytes)*rate` is NULL zodra één term all-NULL is (hier: 80 productie-jobs met proxy_bytes=NULL); de scope-query overleefde omdat die `COALESCE(sum(),0) INTO var` deed, maar de nieuwe per-user-subquery zette de expressie rechtstreeks in een SELECT-lijst → die user viel stil uit de reconciliatie (€10,92 duur-kost verdween naar goodwill). Regel: wrap ELKE `sum()` in een per-user/per-groep COR-expressie in `COALESCE(...,0)`, en verifieer met de reconciliatie-identiteit (Σ per-user against + goodwill == scope-totale COR) — een measured_ar+goodwill die niet op cor.total uitkomt is het symptoom. Vierde keer dat een NULL/guard stil het verkeerde doet.
[2026-07-15] deferred-count-echte-som-niet-blended: leid "credits/fee outstanding" NOOIT af door een €-saldo te delen door een blended gemiddelde (`deferred_balance / (purchased_net/purchased_cr)`) — bij meerdere tiers in één scope is dat gemiddelde geen echte prijs en de teruggerekende count verzonnen. De FIFO-lus kent het werkelijke restant per lot: geef `Σ lot_rem` (en `Σ lot_rem × fee_pc`) direct terug. Bewijs met TWEE tiers (Try €0,0378/cr + Plus €0,0199/cr): echte som 700 vs blended 641,7 = 8,3% fout die bij één tier onzichtbaar is (elk gemiddelde = elke prijs). Zelfde les geldt voor elke per-lot grootheid (fee, VAT): bereken 'm in de lus, reken 'm niet terug.
[2026-07-15] finance-scope-filters: in een scope-parametrische reporting-RPC (admin_finance_summary: external vs internal via profiles.is_internal) MOET elke aggregaat-subquery consistent op scope filteren. De Radar successful-screen-telling telde credit_transactions ZONDER profiles-join → interne testsales lekten als "successful screens" in de externe scope. Regel: elke SELECT die per scope draait, joint profiles en filtert is_internal (of gebruikt is_internal_at_time); een aggregaat zonder scope-filter in een scope-loop is per definitie een lek. Verifieer met een pre/post-diff van het volledige tegengestelde-scope-block.
[2026-07-15] finance-round-only-at-render: afronden hoort ALLEEN in de weergave, nooit in bron/berekening. (a) est_future_gross uit afgeronde displaywaarden (1,99−0,17−0,22=1,60) wijkt af van de volle-precisie-bron (1,99−0,1757−0,2208=1,59) — reken in de bron met onafgeronde tussenwaarden, rond pas bij render. (b) een €-cel die naar 0,00 afrondt terwijl de waarde >0 is (bv. €0,0000125/credit) moet "<€0,01" tonen, geen "0,00"/"0,0000" (suggereert nul waar het verwaarloosbaar-maar-niet-nul is). (c) een aanname-nul (RAG-COR hard 0) is iets anders dan een gemeten/afgeronde nul → toon het als expliciete aanname met redenering in de UI-hint.
[2026-07-16] finance-verify-DML-CTE-snapshot: een `WITH del AS (DELETE ... RETURNING) SELECT count(*) FROM zelfde_tabel` liegt — de outer SELECT ziet de MVCC-snapshot van vóór het statement, dus `rows_after` toont het oude aantal terwijl `del` wél verwijderde. Verifieer een DELETE/UPDATE ALTIJD met een APARTE, volgende query (of gebruik alleen `RETURNING`-count), nooit met een lees in hetzelfde data-modifying statement. Idem: de Supabase MCP `execute_sql` geeft bij meerdere statements alleen het resultaat van het LAATSTE terug — split verificatie-SELECTs of combineer ze in één SELECT met subqueries.
[2026-07-16] synthetische-testusers-trigger: `INSERT INTO auth.users (id)` vuurt `handle_new_user` (+ acquisition/message-triggers) die `profiles` én `user_credits` automatisch aanmaken. Een expliciete `INSERT INTO profiles` daarna geeft `duplicate key` → hele transactie rollt terug. Regel voor synthetische finance-tests: insert alleen `auth.users`, dan `UPDATE` de door de trigger gemaakte `profiles`/`user_credits` (is_internal, library_bytes). Opruimen via `DELETE FROM auth.users` → cascade ruimt profiles/user_credits/credit_transactions/daily_library_bytes (FK ON DELETE CASCADE). auth.users vereist enkel `id`.
[2026-07-16] storage-cor-account-vrije-tier-per-user-share: de R2-gratis-tier (10 GB) is ACCOUNT-niveau, niet per user — attribueer de totale storage-COR (`max(0, totaal_GB − 10) × tarief`) daarna per user naar byte-aandeel (`user_bytes/total_bytes`), en split elk slice against-revenue (× share) vs goodwill (× 1−share), net als ai/cap/sum (F1). €0-test bewijst niets (flat == per-user bij 0); bewijs met ≥2 synthetische users met TEGENGESTELDE profielen (A 15GB share0 → volledig goodwill, B 1GB share1 → volledig against). Zelfde klasse als één-user/één-tier/één-periode: een test die niet kán falen test niets.
[2026-07-16] meting-vóór-formule: als een formule data nodig heeft die niet bestaat (storage-COR wil de bytes-stand ín een verleden periode, maar er is alleen stand-nu), bouw eerst de MÉTING (insert-only dag-serie `daily_library_bytes`, per nacht geschreven) en laat de formule terugvallen op stand-nu MÉT een zichtbare `*_approx`-vlag in de UI tot de serie het venster dekt — nooit stil doen alsof stand-nu de historie is. Dekking = serie-min-dag ≤ venster-begin; anders approx=true.
[2026-07-16] commit-alle-bestanden-verifieer-git-log: een `git add a b c d` waarin ÉÉN pathspec niet matcht (bestond nog niet) faalt ATOMISCH — géén van de bestanden wordt gestaged, en de daaropvolgende `git commit` commit dan alleen wat al gestaged was. Gevolg deze sessie: de F3-code (3 functie-migraties + FinanceView/financeTypes) werd via MCP op de DB toegepast maar NOOIT gecommit/gepusht; alleen 1 tabel-migratie + LOG zaten in de commit. De DB was correct, de repo miste de functie-defs (disaster-recovery-gat). Regel: na een commit die migraties/DB-wijzigingen bevat, `git show --stat <hash>` of `git status` controleren dat ALLE bedoelde bestanden erin zitten — een groene apply_migration ≠ in git. Stage bij voorkeur per bestand of check de exit-code van `git add`.
[2026-07-16] gemeten-COR-dienst-niet-als-OPEX: een dienst die al per gebruik in COR gemeten wordt (AssemblyAI/DeepSeek/Decodo/R2 via proxy_bytes/tokens/library_bytes) mag NIET óók als volle entered-OPEX-regel worden ingevoerd → dubbeltelling. Bouw een guard/waarschuwing bij invoer. Let op de omgekeerde valkuil: de meting hoeft niet dekkend te zijn (Decodo: 6/188 jobs dragen bytes, error-jobs 0, non-job-verkeer ongemeten) → COR is dan een ONDERGRENS; alleen het gat (factuur − gemeten) hoort eventueel als entered, nooit de volle factuur. Bepaal per gemeten dienst of de meting dekkend is vóór je concludeert "zit al in COR, klaar".
[2026-07-17] env-var-deploy-target: een env-var in de lokale `.env` bewijst NIETS over de deploy-target. In F17 aangenomen "DEEPSEEK_API_KEY staat al op de worker (summary-flow)" — fout: de summary-flow draait op de API-service, de worker-service miste de key, dus de nachtelijke `fetch_service_metrics` faalde met "DEEPSEEK_API_KEY not set". Regel: verifieer per service (`railway variables -s <service>`) welke vars er ECHT staan vóór je concludeert dat een credential aanwezig is. Railway-services (api / worker / Redis) delen geen env.
[2026-07-17] externe-API-contract-verifiëren: raad het auth-schema en de request-body van een externe API NOOIT — verifieer tegen de officiële docs én met een echte call vóór je ship't. F17 Decodo-integratie had DRIE aannames fout tegelijk: (a) `Authorization: Bearer <key>` terwijl Decodo de RAUWE key wil (`Authorization: <key>`, geen prefix → anders 401 "Invalid Api key"), (b) body miste het verplichte `proxyType: "residential_proxies"`, (c) datums moesten `Y-m-d H:i:s` zijn, niet `Y-m-d` (→ 400 "Invalid data provided"). Ook de response-parser zocht `date/day/timestamp` terwijl de dag-rijen `key` heten (`{"key":"...","rx_bytes":...,"tx_bytes":...}`). Een 401/400 in een best-effort nachtjob is stil — bewijs de happy-path met één echte request (`railway run -s worker -- python -c ...` tegen de live key) vóór je "gewired ✓" schrijft.
[2026-07-17] api-contract-is-hypothese-tot-echte-call: een API-contract uit de docs is een HYPOTHESE tot je één echte call hebt gedaan. In F17 gingen auth-vorm (`Bearer` ipv rauwe key), verplichte velden (`proxyType`), datumformaat (`Y-m-d H:i:s` ipv `Y-m-d`) én responsestructuur (dag-veld heet `key`, top `{metadata,data}`) alle VIER fout — geen daarvan was betrouwbaar uit de documentatie af te leiden (docs waren vaag/onvolledig). Bel de API in de ONDERZOEKSFASE (`railway run -s <service> -- python -c` tegen de live key), niet pas als de nachtjob stil in productie faalt. Bewijs de happy-path (HTTP 200 + verwacht getal in de tabel) vóór je "gewired ✓" schrijft.
[2026-07-17] reconciliatie-venster-alignment: bij het vergelijken van twee bronnen die per dag verschillende dekking hebben (bv. Decodo billed = alleen gefetchte dagen; onze meting = elke dag), MOET je over dezelfde dagen vergelijken — anders krijg je een vals negatief/positief gat. Meet niet de volle gekozen periode tegen een deel-gedekte bron. Maak "dag gedekt" expliciet (schrijf een 0-rij per gefetchte dag, zodat "rij bestaat" = "gedekt" i.p.v. ambigu met "geen data"), en sluit de INCOMPLETE huidige dag uit (async-aggregatie loopt achter → measured>billed). Test-regel: verifieer ELKE venster-vergelijking met ≥2 vensters — één binnen de dekking én één die erbuiten valt. Een test op alleen het gunstige (gelijk-gedekte) venster mist de bug volledig (F17 reconciliatie leek te kloppen op 14–18 jul maar gaf −0,025 GB op "This month").
[2026-07-17] monorepo-asset-serve-dir + verifieer-vóór-je-claimt: in een Next.js-monorepo resolvet `/asset.png` naar de public/-dir van de DRAAIENDE app (`apps/app/public/`), NIET naar repo-root `public/`. Ik rapporteerde vier taken lang "public/credit-coin.png untracked → 404 in productie" op basis van (a) `HexagonCreditIcon` gebruikt `src="/credit-coin.png"` en (b) repo-root `public/credit-coin.png` was untracked — zonder ooit `apps/app/public/` te checken (waar het bestand AL getrackt stond sinds `e0e7e29`) of een productie-fetch te doen. `curl -I https://app.indxr.ai/credit-coin.png` = **200**: het icoon was nooit stuk. De les is dubbel: (1) een bewering over productie ("404") is een HYPOTHESE tot je 'm met een productie-fetch bewijst — `curl -I` had de fout meteen betrapt; een lokale render of een git-status-aanname bewijst niets. (2) `/asset` in een monorepo = de public/ van díé app; check de geserveerde dir (`apps/<app>/public/`), niet alleen repo-root. Bonus-valkuil: "untracked, niet van deze taak" onderaan een rapport laten staan is geen bevinding maar uitstel — het kostte vier taken en de bevinding zelf was fout.
[2026-07-17] ophaalvenster-is-een-aanname: een ophaalvenster (hoeveel dagen/uren terug je een externe bron opvraagt) is een AANNAME en hoort een gemeten reden te hebben. Het 3-daagse Decodo-venster was een gekozen getal zonder onderbouwing; de gemeten delay is ~1s zonder revisie in 3u, dus een watermark met 1 dag lookback is zowel correcter als zelfherstellend. Standaardpatroon (high-water mark): watermark = MAX(verwerkte dag), vraag vanaf watermark − lookback tot nu, upsert (idempotent op de dag-sleutel), en de watermark schuift pas mee ná een geslaagde load. Effect: een uitval kan geen permanent gat maken (de volgende run haalt alles op). Zelfde patroon geldt voor snapshot_finance_day (catch-up van gemiste nachten). Cap de span zodat één run na een extreme uitval geen API-limiet raakt en niet timeout't.
[2026-07-18] posthog-js-headless: PostHog-js dropt ALLE capture-events in een headless/geautomatiseerde browser — `_is_bot()` is true (UA bevat `HeadlessChrome`, plus `navigator.webdriver`), events worden gedropt vóór `before_send` draait (console: "Refusing to render web experiment since the viewer is a likely bot"). `/flags/` en config laden nog wél, dus posthog lijkt te werken, maar `capture()` returnt `undefined` en er gaat niets naar `/e/`. UA-spoofing + `--disable-blink-features=AutomationControlled` hielp niet betrouwbaar. → Voor privacy-/config-verificatie van PostHog: verifieer (a) `window.posthog.config` (persistence/api_host/disable_session_recording), (b) roep de geconfigureerde `config.before_send`-functie DIRECT aan met een sample-event om IP-nulling te bewijzen, en (c) check cookies/localStorage/sessionStorage voor cookieless — NIET vertrouwen op een live event-payload over het netwerk in headless. Voor een echte payload: non-headless browser met echte UA + `navigator.webdriver=false`.
[2026-07-18] posthog-eu-fallback: een lege/undefined `NEXT_PUBLIC_POSTHOG_HOST` valt STIL terug op de SDK-default `https://us.i.posthog.com` — geverifieerd in zowel posthog-js (`api_host:"https://us.i.posthog.com"` in DEFAULT_CONFIG) als posthog-node (`host ??`-default). Dat is een GDPR-transfer-risico: EU-data lekt naar US zonder foutmelding zodra de env-var ergens ontbreekt. → Geef `api_host`/`host` ALTIJD een expliciete EU-fallback (`process.env.NEXT_PUBLIC_POSTHOG_HOST || 'https://eu.i.posthog.com'`) in PostHogProvider én de posthog-node-init in de Stripe-webhook; vertrouw niet op alleen de env-var. Zelfde klasse geldt voor andere data-residency-SDK's: AssemblyAI's `aai.settings.base_url` default = `https://api.assemblyai.com` (US) → expliciet `https://api.eu.assemblyai.com` zetten voor EU-residency.
[2026-07-19] subverwerkers-EU: gebruik NOOIT een niet-EU/DPA-loze subverwerker voor de verwerking van persoonsgegevens (transcript-tekst kan PII bevatten). DeepSeek (China) biedt geen DPA en geen SCC's → onrechtmatig voor EU-persoonsgegevens (Garante-ban, EDPB). Regel: elke nieuwe AI-/verwerkings-subverwerker moet (a) EU-residency of een geldig transfermechanisme (SCC's/adequaatheid) én (b) een DPA hebben, geverifieerd vóór integratie. AI-summary verhuisd naar de AssemblyAI EU LLM Gateway (ADR-068). Let bij LLM-gateways op in-region vs global routing: EU-residency ("model_region" NIET op global) kost hier ~10% meer dan de global-prijs — reken met de in-region prijs.
[2026-07-19] server-side-telemetrie-hardening: een client-side telemetrie-fix dekt de server-side init NIET automatisch. De browser-PostHog was al EU/cookieless/geen-PII, maar de Python-backend had drie eigen PostHog-inits met hardgecodeerde US-host (`app.posthog.com`) die de EU-env negeerden — én de backend-Sentry had geen PII-scrub. Regel: bij elke telemetrie-hardening (PostHog/Sentry) ALLE init-plekken langs — client, Next-server/edge, én de Python-backend (`grep -rn "sentry_sdk.init\|posthog.host"`). Server-side telemetrie heeft dezelfde EU-host (expliciete EU-fallback, nooit de SDK-US-default) + geen-PII (Sentry `send_default_pii=False`+`before_send`-scrub, PostHog `disable_geoip`) nodig als client-side. Hardgecodeerde host = env-wijziging heeft geen effect.
[2026-07-22] cost-model: een statisch provider-tarief is fout zodra de provider meerdere modellen met eigen prijzen draait. AssemblyAI STT draaide op één rate ($0,21/uur) terwijl Universal-2=$0,15 en Universal-3(.5) Pro=$0,21/uur — Universal-2-runs werden te duur geboekt. → Reken COR PER EFFECTIEF MODEL (het vastgelegde `transcription_jobs.assemblyai_model`, niet het aangevraagde), per-run/per-user, nooit scope-gemiddeld; zet de per-model tarieven in cost_config (USD, FX bij query) met één rate-helper. Legacy runs zonder model → gedocumenteerde fallback-rate (niet stil nul/gemiddelde). Model-ids gebruiken streepjes: `universal-3-5-pro` (NIET `universal-3.5-pro` → API-fout). `speech_models` is een TAAL-ROUTER (beste model dat de taal native dekt), geen error-fallback. Zie ADR-070.
[2026-07-22] assemblyai-chain: een `speech_models`-lid waarvan de talen een deelverzameling zijn van een eerder-genoemd lid, kan door de taal-router NOOIT geselecteerd worden (dode tak). `universal-3-pro` (6 talen) stond vóór `universal-2` maar ná `universal-3-5-pro` (18 talen, superset) → onbereikbaar. → Verifieer bij elke chain-wijziging dat élk lid bereikbaar is (geen superset ervoor); volg AssemblyAI's eigen aanbeveling (3.5 Pro + Universal-2) en houd `language_detection=True` aan zodat de router per taal kan kiezen. COR-tarief van een verwijderd model in cost_config laten staan voor historische runs (ADR-070/071).
[2026-07-22] credit-safety: elke nieuwe limiet/afwijzing op een betaald pad (duur-cap, playlist-cap) MOET vóór `reserve_credits`/`deduct_credits_atomic` zitten, nooit erna — anders kan een user credits verliezen aan een job die toch geweigerd wordt. Plaats de check direct nadat de kostbepalende grootheid (duur / aantal video's) bekend is, en bewijs de volgorde (code + live 422-test met saldo-vóór=saldo-ná). Raak daarbij `_sale_vat`/`vat_by_country`/omzet-recognitie niet aan (audit-chain 31/0/0).
[2026-07-23] redirects: elke herstructurering voegde redirect-regels toe zónder de bestaande tabel als geheel te herzien → een nieuwe regel (`/docs/help/faq → /docs/faq`, ADR-073) botste met een oude uit een vorige laag (`/docs/faq → /docs/help/faq`, mei-hernesting) = infinite redirect-loop in productie; en `/docs/how-to`/`/docs/troubleshooting` werden 2-hop-ketens omdat hun tussenbestemming later verwijderd werd. → Regels bij ELKE structuurwijziging: (1) een pagina die nu een echte route is mag NOOIT een redirect-bron zijn — verwijder zo'n bron. (2) Na een route-verwijdering: check of die route een DESTINATION van bestaande redirects was en her-target die naar het nieuwe eindpunt (voorkom ketens). (3) Loop de HELE `next.config.ts`-redirecttabel na als graaf (bron→bestemming), niet alleen de zojuist toegevoegde regels: 0 loops, 0 ketens >1 hop, elke bestemming bestaat, geen sitemap-URL is een redirect-bron. Statische graaf-audit + live curl-crawl vóór het als-klaar markeren.
[2026-07-22] credit-display-source: het bedrag dat de gebruiker vóór een playlist-job ziet kwam uit een ándere formule dan wat gereserveerd/afgerekend wordt. De availability-route (`apps/*/src/app/api/check-playlist-availability/route.ts:80`) schat needs_whisper-kosten als `ceil(minuten/8)`; dat lekt via `estimatedCredits` in `PlaylistAvailabilitySummary` (summary-kaart "X credits required", per-video "N credits", de "Extract — N credits"-knop, "after extraction") én in de preflight-check (`PlaylistTab.tsx:419`). De reservering/afrekening gebruikt `ceil(minuten/60)` = 1 credit/min (`backend/main.py:1340-1344` `_compute_playlist_reservation`, `credit_manager.calculate_credit_cost`). Voor niet-caption-video's die de gebruiker niet toggelt (de recompute naar `/60` gebeurt alleen bij toggle, PlaylistAvailabilitySummary.tsx:94/107) toont de UI dus ~8× te weinig; de backend reserveert het echte bedrag. → Een bedrag dat de gebruiker vóór een betaalde actie ziet MOET uit dezelfde bron/formule komen als het gereserveerde/afgerekende bedrag. Nooit een losse "schatting" in een preview-route; render uit de gedeelde kostfunctie (of laat de UI het altijd zelf uit duur berekenen met exact dezelfde `ceil(dur/60)`). Verifieer met een echte job: getoond == gereserveerd (saldo-vóór − saldo-ná).
[2026-07-23] wiki-is-afgeleide: product-truth.md §3 stelde stellig "**Geen** ZIP/bulk multi-file format-export voor users" — maar die export bestaat wél (library-multiselect → ZIP met één bestand per transcript, `TranscriptList.tsx:403 handleBulkRagExecute` + `:469-499 handleBatchDownload`). De artikelen die de wiki geloofden dreigden fout gecorrigeerd te worden; verificatie tegen de code redde het (jszip is een echte dependency en wordt gebruikt). → Regel: de **wiki is een afgeleide** die kan verouderen; de **code is de bron van waarheid**. Bij een conflict tussen wiki en code wint de code, en dan wordt de wiki bijgewerkt (met `bestand:regel`-verwijzing) — niet andersom. Toets een wiki-"feit" dat je in content gaat verwerken altijd eerst tegen de code (grep de feature/dependency); een stellig "geen X" in de wiki is een rode vlag om te verifiëren, niet om te vertrouwen.
[2026-07-23] content-vs-productgedrag: de oude `/docs/quickstart` (door Claude gegenereerd) beweerde dat INDXR ná een captions-check AI-transcriptie "aanbiedt" — een pre-check die volgens ADR-076 niet bestaat (de keuze is een schakelaar vóór extractie, er wordt niets vooraf gecontroleerd) — en beschreef alleen de gratis captions-route terwijl AI-transcriptie de hoofdroute is. → Contentpagina's die productgedrag beschrijven MOETEN vóór publicatie getoetst worden tegen `product-truth.md` én de relevante ADR's, en de UI-labels/knop-overgangen letterlijk tegen de code (VideoTab/TranscriptCard). Beschrijf geen flow uit aanname; verifieer knop-tekst, gating en credit-gedrag in de bron.
[2026-07-23] docs-conceptpagina-overlap: een conceptpagina die meer dan één onderwerp behandelt (how-indxr-works raakt accuracy/formats/credits/library; faq raakt vrijwel alles) dubbelt onvermijdelijk met de pagina's die die onderwerpen bezitten. → Houd zo'n pagina kort en verwijzend: per onderwerp één alinea + link naar de bezittende pagina, nooit een tweede volledige uitleg. Bij twijfel over de grens (wat bezit deze pagina, wat herhaalt ze niet) geldt de bezit-tabel in `docs/wiki/content/docs-page-contract.md`, niet je eigen inschatting.
[2026-07-23] AI-beeldgeneratie: Leonardo Content Reference op High legt de fijne textuurstructuur van het bronbeeld over het hele frame, ook op lucht en glas -> gebruik Content Reference nooit boven Low voor fotorealistische scenes; compositie afdwingen via framing-ankers in de prompt.
[2026-07-23] AI-beeldgeneratie: Leonardo free plan geeft geen IP-eigendom (Leonardo behoudt eeuwigdurende rechten, generaties zijn publiek) -> commerciele merkassets alleen genereren op een betaald plan met Private Mode aan.
[2026-07-23] Werkwijze: twee gelijktijdige CC-sessies op dezelfde working tree leidden ertoe dat werk van sessie A in de commit van sessie B belandde via `git add -A` -> altijd expliciete paden stagen, nooit `git add -A`, en bij aanvang van een taak `git status` controleren op een schone tree.
[2026-07-24] i18n/backend: gebruikersgerichte strings werden in het Nederlands weggeschreven naar `credit_transactions.reason` (Python-default "AI transcriptie settlement" in credit_manager.py, en de refund-RPC `refund_credits` met "Gereserveerd … → verbruikt … → … teruggestort (…/… mislukt)") terwijl de hele UI Engels is. → Elke string die in de UI kan belanden moet Engels zijn, óók in Postgres-RPC's en de Python-backend. Render user-facing labels bij voorkeur uit gestructureerde `metadata` (het datacontract), niet uit een vrije-tekst `reason`; dan corrigeert de frontend bestaande én nieuwe rijen zonder de audit-log te muteren, en lekt de reserveringsmechaniek niet naar de gebruiker.
[2026-07-26] design/hexagon: de pattern-map in `system.md §5` ("Waar GEEN patterns") is BINDEND, en de Transcribe-werkzone valt daar expliciet buiten — dit is nu twee keer fout gegaan (patroon live over de werkbank). → Voeg het honeycomb-patroon nooit toe aan een werkoppervlak (Transcribe-form, editor, settings, data-tables, werkende dashboardpagina's). Toegestaan alleen op de §5-tabel-surfaces (marketing/auth/empty states/404/footer/docs-landing). Library is de enige vastgelegde werk-uitzondering (LESSONS 2026-07-03 + ADR-079). Toets elke nieuwe patroon-plaatsing expliciet tegen §5 vóór je hem toevoegt.
[2026-07-26] design/keuze-vs-navigatie: een keuze-control (bv. methode-keuze) mag NOOIT dezelfde vorm hebben als de navigatie erboven. Een segmented control onder de mode-strip (ook een segmented control) las als twee rijen tabs — niemand zag dat het een keuze was of welke actief was. → Een keuze krijgt een eigen visuele vorm (radiogroep met zichtbare radio-indicator + tint op de selectie), niet de segmented-control-vorm die aan de navigatie is voorbehouden. Toets elke nieuwe control tegen wat er direct boven/onder staat.
[2026-07-26] design/kosten-nadruk: de kosten van een betaalde actie zijn nooit het kleinste of grijste element op het scherm. De job-prijs stond als muted 12px tussen andere UI, terwijl het het getal is dat de gebruiker geld kost. → "Wat kost dit" = het grootste getal in het kostenblok (`Total` ~20px semibold `--fg-strong`); "kan ik dat betalen" = secundair maar leesbaar (`--fg-subtle`, niet `--fg-muted`); bij tekort geen dode disabled-knop maar echte uitwegen (Deselect / Buy credits). LET OP: `--fg-secondary` bestaat niet in tokens.css — gebruik `--fg-subtle` (prominenter dan `--fg-muted`).
[2026-07-26] design/gedeeltelijk-succes: een gedeeltelijk geslaagde run krijgt geen succeskleur en geen feestelijke kop. Het playlist-afrondingsscherm tintte de hele kaart groen en riep "Extraction Complete!" terwijl 4 van 21 video's mislukten. → Neutrale kop + neutrale kaart zolang er iets mislukt is ("17 of 21 videos transcribed"); groen vinkje en "All N transcribed" alleen bij 100%. Geel is helemaal uit de flow (stond op de betaalde AI-functie en las als waarschuwing); groen = gratis/nieuw/volledig gelukt, rood = onbeschikbaar/fout.
[2026-07-26] design/hexagon-layout: de `HexagonPattern` lag layout-breed in `apps/app/src/app/dashboard/layout.tsx` — dus over ÉLKE dashboardpagina (Home/Transcribe/Messages/Library) — terwijl §5 het patroon alleen op empty states/marketing/auth toestaat. Een gedeelde layout is precies de verkeerde plek: het patroon wordt dan stilzwijgend meegenomen naar de volgende werkende pagina. → Patroon is PER-PAGINA opt-in via `DashboardBackdrop`, nooit vanuit een gedeelde layout, en géén pathname-conditie in de layout (gaat bij de volgende route weer fout). Werkoppervlak = geen patroon (ADR-079).
[2026-07-26] error-capture: de download-tak persisteerde error_type wél, maar 13 andere terminale error-branches in `transcription_pipeline.py` (credit-check, insufficient, deduction, members_only, validation, duration, compression, **api_error/AssemblyAI**, no_speech, internal_error) riepen `_update_job(status="error", error_message=...)` ZONDER `error_type=` → kolom bleef NULL → Operations-dashboard toonde alles als "uncategorized" ondanks een rijke ~11-slug-taxonomie die de return-dict wél berekende. Regel: bij een terminale fout ALTIJD `error_type` meegeven aan de DB-write, niet alleen aan de return-dict. De return voedt de caller; de DB-kolom voedt dashboards/analytics — beide moeten het label dragen. Idem de watchdog-reaper-passes in `worker.py` (status='error' zonder error_type → nu stuck_pending/worker_crashed). Nog open: caption-failures in `usage_logs` dragen geen error_type (aparte tabel, RPC-uitbreiding nodig) en `main.py`'s inline caption-classifier is een stale kopie van `_classify_download_error` (drift-risico).
[2026-07-26] error-classificatie: 'timeout' was een te brede vangbak — connectie/TLS (EOF/SSL/reset), proxy-tunnel-fouten en HTTP-5xx vielen er allemaal in, en 'timeout' is óók het retry-signaal. Gevolg: de failure-reason breakdown liegt (bij inspectie waren 4/4 bestaande timeout-rijen iets anders: TLS, 5xx, partial_write, proxy). Regel: elke foutsoort een eigen expliciete code; de catch-all is 'extraction_error' (honest unknown), NOOIT een specifieke retryable slug. Splits je een retryable slug, werk dan ALLE retry-gates bij en houd ze in sync: worker retry-set (`worker.py`) + RPC `v_has_retryable` IN-list (`update_playlist_video_progress`) + frontend `errorCopy.ts` (ADR-080; alleen toevoegen, niet hernoemen).
[2026-07-26] keyword-classifier: bare cijfercodes ('504','500','429') matchen spurieus als substring in byte-tellingen/URLs — "6504400 bytes read" bevat "504" → werd 'timeout'. Gebruik de tekstuele vorm ('gateway timeout','service unavailable','internal server error') of 'http error 50x' mét context; zet specifieke takken (partial_write, proxy_error) vóór de generieke/numerieke.
[2026-07-26] concurrente-sessies/build-lock: raak NOOIT een `.next/lock` (of enige build-lock) aan die een andere sessie vasthoudt. Twee gelijktijdige `next build`-processen op dezelfde `.next/` klobberen elkaars temp-manifests (`ENOENT ... _buildManifest.js.tmp...`) en je kunt niet zien wiens build je sloopt. Regel: is de lock bezet, dan WACHT je tot 'ie vrij is, óf je verifieert alleen met `tsc --noEmit` (raakt `.next/` niet). Nooit de lock verwijderen om je eigen build erdoor te duwen. (Naast de bestaande git-workflow-regels over gelijktijdige sessies.)
[2026-07-26] error-taxonomie: `extraction_error` is DE enige download-fout-catch-all (honest "unknown", fault=unknown). Zet er nooit een tweede catch-all naast (bv. een aparte `unknown`-slug) — twee vangbakken is slechter dan één, en hernoemen breekt de errorCopy.ts-keys (ADR-080). Elke herkenbare foutsoort krijgt een eigen expliciete code; al het ongemapte valt in `extraction_error`. Vast te leggen in de metrics-ADR.
[2026-07-26] design/foutkaart-body: een foutkaart toont NOOIT een backend- of provider-string als hoofdtekst. De ErrorCard-neutrale-fallback gebruikte `ctx.fallbackMessage` (= de rauwe `error.message`) als body → een yt-dlp-stacktrace mét "report an issue"-GitHub-URL verscheen als gebruikerstekst. → De body is altijd eigen copy (per code, of een generieke fallback); de rauwe string hoogstens als kleine muted `code` onderaan. Zorg dat de error-setter de backend-`error_type`/`code` MEEDRAAGT (niet alleen de message-string) zodat de kaart op getailorde copy landt i.p.v. de generieke fallback. Credit-mededelingen bij betaalde fouten uit data lezen (`credits_refunded`), niet uit een aanname over reserve-and-hold — zwijgen als de waarde er niet is.
[2026-07-26] css/sticky-overflow: `overflow-hidden` van een kaart weghalen om `position: sticky` te laten werken legt clipping bloot die dat overflow elders opving (rijen die over de rechterrand liepen, ronde hoeken die kinderen niet meer klippen). → Regel de afkapping bij de KINDEREN — `min-w-0` op de flex-container + `truncate`/`line-clamp` op de titel, en `rounded-t-xl` op de header en `rounded` op de eerste/laatste rij — niet met een overflow op de ouder. En: een JSX-comment `{/* */}` mag niet direct na `return (` staan (parser ziet twee expressies) — zet 'm als `//`-comment vóór de return.
[2026-07-27] design/actiebalk-conditionele-knoppen: een actiebalk met conditionele knoppen wordt getest met de MAXIMALE set acties, niet met de gebruikelijke — de zeldzame combinatie (bv. Library-selectie mét [new]-badge → extra "Mark as read" naast Download + Delete + clear) is precies degene die niemand ziet breken. Ontwerp de balk op de volledige set, geef 'm een harde max-breedte, en collapse labels naar icon-only (mét aria-label) op smal i.p.v. te vertrouwen op ml-auto/wrap die de laatste knop eruit duwt.
[2026-07-27] build/dual-runtime-module: een module die ZOWEL gebundeld wordt (Next/tsc) ALS door een node-`--experimental-strip-types`-test wordt geladen, moet z'n VALUE-imports met expliciete `.ts`-extensie schrijven — node's ESM-loader resolvet extensieloos (en `.js`→`.ts`) NIET (getest: beide `ERR_MODULE_NOT_FOUND`). Zet dan `allowImportingTsExtensions: true` in de tsconfig(s) die het bestand typechecken (mag alleen met `noEmit`/`emitDeclarationOnly`; hier staat noEmit overal aan). Type-only imports ontsnappen omdat strip-types ze verwijdert. Alleen dat ene bestand krijgt de `.ts`-extensie; de rest houdt de extensieloze bundler-norm.
[2026-07-27] download/threads: asyncio.wait_for rond een asyncio.to_thread(...) kapt alleen de wachtende coroutine, NIET de onderliggende thread — die loopt door en trok bij een 76-min video de volledige 82 MB ná het afbreken (verspilde proxy-egress, weggegooid). Regel: een blocking download in een thread die je wilt kunnen afbreken, moet een IN-download afbreek-hook hebben (yt-dlp: DownloadCancelled uit een progress_hook stopt de download echt); een buiten-af timeout is alleen een coarse backstop, geen echte cancel.
[2026-07-31] ui/rij-vs-bulk-context: één actiemenu dat zowel een enkele rij als een selectie bedient levert onzin op ("Rename 15 bestanden", "Watch on YouTube" over een bulk). Regel: rij-acties en bulk-acties zijn TWEE aparte menu's/oppervlakken die niets delen op de action-laag. Rename/Watch/Copy/Open horen alleen in het rij-menu; bulk kent alleen set-veilige acties (Move/Export-ZIP/Mark-read/Delete). Een rij-actie zonder actieve selectie betreft dat éne item (nooit 0, nooit de selectie). Gedeelde sub-UI (bv. Move-to-collection) mag hergebruikt worden zolang die op een expliciete id-set opereert die de aanroeper meegeeft (rij → `[id]`, bulk → `selectedIds`).
[2026-08-01] youtube-embed-privacy: de app is cookieless-by-design (bewust géén cookie-consent-banner — `PostHogProvider` persistence:'memory', privacy-facts.md). Een gewone `youtube.com/embed`-iframe zet echter wél Google/DoubleClick-cookies bij laden → dat ondermijnt de no-banner-positie stil. Regel: elke YouTube-embed via **`youtube-nocookie.com`** (IFrame Player API: `host:'https://www.youtube-nocookie.com'`) én **lazy** (pas laden bij expliciete user-actie "open video", niets van YouTube tot dan, geen cookie tot playback). Geverifieerd: `TranscriptViewer` was de enige embed in de repo. Bij een nieuwe embed elders: dezelfde nocookie+lazy-regel, anders inconsistentie.
[2026-08-01] db-kolom-elke-writer: een nieuwe kolom die een afgeleide/UI-signaal draagt (bv. `edited_content_updated_at` → stale-summary-melding) moet in **ÉLK** schrijfpad van de bronkolom worden gezet — mis er één en de feature is stil kapot + je houdt een dode kolom over (precies het rommel-type uit de source-map). Regel: vóór je de feature bouwt, `grep -rn "update({ *<broncol>" ` alle writers op en zet de afgeleide kolom in állemaal (hier: `TranscriptViewer.handleSave`→`now()` én `dashboard/transcribe/page.tsx` reset→`null`). Twee writers gevonden, beide gezet.
[2026-08-01] label-hernoemen-≠-enum: bij een productbrede tekst-hernoeming (bv. "Auto-captions" → "YouTube captions") verander je **alleen zichtbare strings**. De DB-enum (`processing_method = 'youtube_captions'`), method-keys, dedup-sleutels en Playwright role-keys blijven — die hernoemen breekt bestaande rijen + de backend-branch. Twee valkuilen: (1) **prose ≠ label** — een zin die uitlegt wát automatische ondertiteling ís houdt "automatic captions" (correct Engels); alleen INDXR's methode/optie/badge wordt hernoemd (per-plek oordeel, niet blind zoek-vervangen). (2) **export-metadata zit dieper dan de UI-knop** — de string die de gebruiker in een gedownload bestand ziet komt uit een generator (`formatTranscript.ts` → `generateCsv`/`generateMarkdown` `transcript_source`), en die wordt alleen gevuld als de caller `extractionMethod` meegeeft (free-tool wél, sommige library-exports niet). Verifieer bij export-tekst dus welke generator + welke caller het echt oplevert, niet alleen de knop. Zie [[086-youtube-captions-rename]].
[2026-08-01] overflow-trigger-met-één-kind: een ⋯/"More"-trigger (DropdownMenu/Sheet) die geopend maar **één** actie toont, is een UX-smell — hij belooft meer dan hij levert. Kwam voor in de mobiele bulkbalk (⋯ → alleen "Move to collection") én in `AiSummaryView` (Export-dropdown → alleen "Text File (.txt)"). Regel: rendert een menu maar één actie, maak er een **directe gelabelde knop** van; een ⋯ mag alleen terugkomen als hij écht meerdere (of conditioneel meerdere) acties heeft. Bij twijfel over "elders herhaald?": grep `MoreHorizontal`/`MoreVertical` + `aria-label="More"` en tel de kinderen per menu. (Let op de rand: `TranscriptViewer`'s ⋯ degradeert in edited-modus zonder video tot alleen "Delete" — nog acceptabel maar houd 't in de gaten.)
[2026-08-01] uploads-hebben-geen-video_id: geüploade audiofiles krijgen `transcripts.video_id = NULL` (bron: `assemblyai`, geverifieerd 5 rijen); YouTube-transcripten hebben altijd een 11-tekens id. Elke YouTube-specifieke UI (nocookie-speler, "Watch on YouTube", "Watch video"-collapse, seek-op-timestamp, `watch?v=…`-hrefs) moet daarom gegate zijn op `!!video_id` — anders krijg je een zwart speler-vierkant + `watch?v=null`-links bij uploads. Regel: `const hasVideo = !!videoId;` en gate álle video-affordances; laat positie-timestamps staan als **inerte** markers (`.ts-static`, `pointer-events:none`) i.p.v. dode links. `page.tsx` mag geen `watch?v=${video_id}` bouwen als `video_id` null is.
[2026-08-02] sitemap-lastmod: `sitemap.ts` stempelde alle 49 URL's met de buildtijd (`Date.now()`) als `<lastmod>` — een uniforme buildstempel is geen neutrale default maar maakt het signaal onbruikbaar: Google gebruikt `<lastmod>` alleen als die verifieerbaar overeenkomt met de échte laatste contentwijziging, dus een build-datum op álles traint crawlers om het veld te negeren. → `<lastmod>` moet per pagina een echte contentwijziging weerspiegelen. Seed uit git-historie en leg de datums vast in een ingecheckt data-bestand (`sitemap-lastmod.ts`) — NIET op buildtijd uit git afleiden (Vercel cloont ondiep → git onbetrouwbaar). Route zonder bekende contentdatum: géén `<lastmod>`, nooit terugvallen op builddatum/`Date.now()`. `priority` + `changefreq` weglaten (Google negeert ze). Onderhoudsregel: datum alleen bumpen bij inhoudelijke wijziging (tekst/feiten/structuur), niet bij styling/refactor/deps/canonical-toevoeging.
[2026-08-02] proxy-sessie-determinisme: een DETERMINISTISCHE Decodo-proxysessie voor een YouTube-extractiepad pint het sticky exit-IP en amplificeert bot_detection. `main.py` gebruikte `session_id = video_id[-8:]` voor de caption-cascade → elke request (én elke "Try again", die de extractie 1-op-1 herdraait) kreeg hetzelfde sticky IP voor de sessie-TTL; zodra YouTube dat IP bot-flagt, faalt élke retry identiek tot de TTL verloopt. Live bewezen: een vaste sid pinde één video 7 runs op één IP. Regel: geef élke extractie-REQUEST een verse random sessie (`secrets.token_hex(4)`), zodat retries/Try-again gegarandeerd een ander exit-IP verkennen; laat een client-side "Try again" NOOIT dezelfde sessie forceren. Het audio-pad deed dit al goed (per-job `job_id[:8]`). Captions zijn NIET IP-locked (de VTT-download gebruikt al een ander `-r`-IP dan `extract_info`), dus randomiseren is veilig. NB: dit is een IP-loterij/wapenwedloop — rotatie verhoogt de dag-slaging niet dramatisch, het verwijdert de failure-amplificatie; reken op restfalen (~5%). PO-tokens/bgutil niet terughalen (ADR-027). Zie known-issues.md (2026-08-02).
[2026-08-03] capture-machine-determinisme: bij het uitbreiden van de docs-screenshot-machine (`tests/playwright/capture/`) twee valkuilen die stille of trage runs geven. (1) **Dedup onderschept de Extract-klik**: zodra het capture-account de fixture-video al in z'n bibliotheek heeft, toont de UI "you already have this transcript" i.p.v. te extraheren → gestubde ErrorCard/progress-captures krijgen nooit hun kaart. Fix: gestubde captures gebruiken een **dummy video-id** (de stub negeert tóch welke video), en maak het account schoon vóór de live-captures die het transcript vers moeten aanmaken. (2) **Login-per-test haalt de timeout**: gebruik één gedeelde `storageState` (global-setup mint de sessie + consent + thema één keer) i.p.v. `loginAs` per test — scheelt van >10 min naar ~3 min. `capture-state.json` bevat een sessietoken → gitignoren. Anker captures op zichtbare TEKST of ROL, nooit op vormklassen (`div.rounded-2xl`): een gewijzigde afronding mag geen ander beeld schieten.
[2026-08-02] playwright-root-config-pnpm: een Playwright-config in de repo-root importeert `@playwright/test`, maar in deze pnpm-monorepo staat dat pakket NIET in `node_modules/.bin` of `node_modules/@playwright` van de root (het is gehoist naar `node_modules/.pnpm/node_modules`). `npx playwright test` in de root pakt de `playwright`-CORE-CLI (die heeft geen `test`-subcommando → "unknown command 'test'"). Draai root-configs met `NODE_PATH=node_modules/.pnpm/node_modules node node_modules/.pnpm/@playwright+test@<ver>/node_modules/@playwright/test/cli.js test --config=<cfg>`. Isoleer een niet-standaard suite (bv. een screenshot-capture-machine) via een eigen config + eigen `testDir` zodat de default-`playwright test` (andere `testDir`) hem nooit oppakt (geverifieerd met `--list`). Bijkomend: de **lokale** app-server (`next start :3001`) kan de extractie-backend niet bereiken (`/api/extract` → 503) — Playwright-flows die het echte backend nodig hebben draaien tegen `app.indxr.ai`; `page.route`-gestubde flows draaien overal. Cookie-login = Supabase-sessie minten + `sb-*`-cookies injecteren (host-scoped), niet de UI-loginform (PKCE-timing flaky).
[2026-08-02] tag-die-cookies-zet: een third-party tag die een cookie plaatst (Google Ads `_gcl_au`, en soortgelijke) mag NOOIT los van (a) een consent-banner/Consent Mode-gate én (b) de bijgewerkte privacytekst (subverwerker + cookietabel) deployen. Los deployen maakt de live /privacy-claim ("cookieless / no tracking cookies") onwaar op het moment tussen de twee deploys. Regel: tag + consent + privacytekst = één commit/één deploy. Consent Mode v2 in **BASIC** mode (niet Advanced): laad `gtag.js` pas ná toestemming zodat er nul requests/pings naar Google gaan vóór consent — toetsbaar (headless: 0 requests naar googletagmanager/googleadservices/google-analytics + 0 `_gcl_*` vóór Accept). Cross-subdomein consent: localStorage is per-origin → sync via een strikt-noodzakelijk `.indxr.ai`-cookie dat ALLEEN de keuze draagt (geen identifier, anders niet meer strikt-noodzakelijk). Intrekken moet de al gezette `_gcl_*` actief wissen — anders is het geen intrekking. Zie ADR-087.
[2026-08-02] keyword-data-interpretatie: Bing Webmaster Tools "impressies" zijn GEEN maandelijks zoekvolume — het is hoe vaak een term in een Bing-resultaat is gezien, en Bing heeft ~3–4% marktaandeel. NOOIT vergelijken met Google Keyword Planner of Trends; alleen onderling. Bing's related-keyword-algoritme waaiert lexicaal uit vanaf de zaadterm en blijft in de kop hangen → onze long-tail-clusters (playlist/bulk/rag/srt/markdown/obsidian/csv/json) zijn er **niet meetbaar** (meetgrens, geen afwezige vraag) — valideer die via Keyword Planner + het search terms report zodra de campagne draait, concludeer niet uit een lege Bing-uitvraag. Bijvangst-correctie op `writing-standard §B`: Bing ≠ de ChatGPT-index (OpenAI draait sinds eind 2024 een eigen OAI-SearchBot; Bing voedt Copilot/Edge). Vol rapport: `business/keyword-demand-2026-08.md`.
[2026-08-02] robots.txt-named-groups: een named `User-agent`-groep erft NIETS van de `*`-groep. Per RFC 9309 / Google's spec gehoorzaamt een crawler exact ÉÉN groep — zijn meest-specifieke match — en de `*`-groep geldt alleen "if no matching group exists". `apps/marketing/public/robots.txt` had 13 named groepen met enkel `Allow: /` (Googlebot, Bingbot, GPTBot, ClaudeBot, OAI-SearchBot, ChatGPT-User, Claude-SearchBot, Claude-User, PerplexityBot, Perplexity-User, Google-Extended, CCBot, anthropic-ai) → elk van die crawlers zag alléén `Allow: /` en negeerde de vier `Disallow`-regels (/api, /dashboard, /admin, /library). Een groep met alleen `Allow: /` geeft die crawler dus MÉÉR rechten dan `*`, niet dezelfde. → Maak een named group ALLEEN aan als hij de volledige disallow-set herhaalt (zoals de Meta-ExternalAgent-blokgroep), of maak hem niet aan; laat het beleid door `*` dragen en documenteer de "allowed by choice"-rationale als commentaar, niet als lege allow-groepen. (`anthropic-ai` verwijderd — token afgeschaft; live Claude-tokens = ClaudeBot/Claude-User/Claude-SearchBot, alle onder `*`.)
[2026-08-02] legal-pagina-lastmod: legal-pagina's (/privacy, /terms) zijn geen gewone contentpagina's — hun sitemap-`<lastmod>` moet de legal-versie tracken, niet cosmetische wording. Een productbrede hernoeming (bv. 'auto-captions'→'YouTube captions') die alleen terminologie raakt mag hun lastmod NIET bumpen → expliciet overslaan (terug naar de publish/LEGAL_VERSION-datum). Een WÉL inhoudelijke wijziging (nieuwe disclosure/recht/plicht) moet drie dingen samen bumpen: zichtbare "Last updated"-datum, `LEGAL_VERSION` (`packages/shared/src/lib/legal.ts`, per checkout vastgelegd in `terms_acceptances`, ADR-069), én sitemap-lastmod. Divergentie = aanvaardingen verwijzen naar een versie die de koper niet zag. Een inhoudelijke wijziging aan /privacy of /terms is de TRIGGER; die drie moeten meteen mee. (Concreet: /privacy kreeg op 2026-08-01 een echte no-cookie-speler-disclosure die alléén de tekst raakte — zichtbare datum + `LEGAL_VERSION` bleven 2026-07-20 → het bewijsspoor in `terms_acceptances` liep stil uit de pas. Rechtgetrokken 2026-08-02: `LEGAL_VERSION`→2026-08-01 (nu expliciet een BUNDELVERSIE = laatste wijziging aan één van beide docs), /privacy "Last updated"→2026-08-01; /terms blijft 2026-07-20 want ongewijzigd. DB-check: 0 aanvaardingen met `accepted_at ≥ 2026-08-01`, dus geen retroactieve schade — de 2 bestaande rijen (07-20/07-23) zagen wél de 07-20-tekst.)
[2026-07-31] i18n/dir-auto: door gebruikers aangeleverde titels (transcript-titels, collectie-namen) bevatten Arabisch/Hebreeuws en renderen fout (leestekens/cijfers aan de verkeerde kant) zonder `dir="auto"`. Regel: zet `dir="auto"` op ELK element dat een user-supplied titel toont — lijstrij, rename-input, badge, menu-item, sheet-titel, filterveld — niet alleen de hoofdtitel. `dir="auto"` laat de browser per string de richting bepalen; het is gratis en correct voor gemengde LTR/RTL content. Geverifieerd met een Arabische titel die door de `transcripts_list`-view heen round-tript.
[2026-08-03] single-source/hand-getypte-telling: een drop-zone die 7 formaten toonde terwijl de backend er 9 accepteert (`MPEG`/`MPGA` weggevallen) is het klassieke hand-getypte-drift-symptoom. Regel: één bron voor zo'n set (`packages/shared/src/lib/uploadFormats.ts`, lockstep met `backend/audio_utils.py SUPPORTED_FORMATS`), waaruit ZOWEL de UI (`accept`-attr, client-side `validTypes`-guard, drop-zone-label) ALS élke prose-plek rendert (`accept=`, count-word via `spellCount`, oxford-prose-helper). Grep ná de refactor BREED op de oude enumeratie én op count-woorden ("seven"/"nine" bij "format/upload") — óók in rendered reference-tabellen (`/docs/reference/limits` had de volledige 9-lijst handmatig; nu `UPLOAD_EXTENSIONS.join(", ")`). Backend blijft de autoriteit — de shared-constante moet exact z'n set spiegelen, niet andersom.
[2026-08-03] docs/content-mirrors-zijn-niet-allemaal-mirrors: `docs/content/*.md` werd in de wiki als "static mirrors van rendered content" beschreven, maar 6 van de 26 (`ARTIKEL-alternative-*`, `ARTIKEL-youtube-transcript-generator`) hebben GEEN gerenderde route (`/alternative/*`, `/youtube-transcript-generator` bestaan niet, worden nergens gelinkt) → dat zijn wees-drafts voor nooit-gebouwde pagina's, de SOLE source. Regel: vóór je een "mirror"-map blanco opruimt, map ELK bestand op z'n gerenderde equivalent (route-tree: `find … -name page.tsx`). Bestand mét rendered-pagina → verwijderen (rendered `.tsx` is de bron; git bewaart historie). Bestand ZONDER route → sole source, niet blind verwijderen — surface het als content-debt en laat de mens beslissen (bouwen of schrappen). Een niet-gerenderde tweede waarheid die je wél mag verwijderen ≠ een unieke draft die je NIET mag verwijderen; het verschil is puur "bestaat de rendered pagina".
[2026-08-03] dead-column-gate: een feature-gate (`isPremium` → rate-limiter-bypass in `/api/extract`) las `user_credits.total_credits_purchased` — een kolom die NOOIT wordt geschreven (0 voor álle rijen, live-geverifieerd; CLAUDE.md lijst 'm zelfs expliciet als dode orphan). Gevolg: de "wie ooit kocht omzeilt de limiter"-regel vuurde nóóit, stil. Regel: een boolean/gate afgeleid van een kolom is alleen betrouwbaar als díe kolom aantoonbaar gevuld wordt — vóór je erop bouwt, `SELECT count(*) FILTER (WHERE <col> ...)` op de live tabel om te bewijzen dat er ooit een non-default in staat. Het gezaghebbende "heeft ooit gekocht"-signaal is een `credit_transactions`-rij met `kind='purchase'` (alleen door de Stripe-webhook gezet), niet een teller-kolom. Zie [[project_session_2026_07_24_admin_kpi_audit]] (has_ever_purchased-gap).
[2026-08-03] ssr-comment-grep: bij het verifiëren van gerenderde Next/React-HTML met grep splitst React SSR aangrenzende `{expr}` + tekst met een `<!-- -->`-marker → een grep op de letterlijke "100 MB, free" of "{n} downloads" mist, terwijl de content er wél staat. Regel: strip eerst de markers (`sed 's/<!-- -->//g'`) of match op een fragment dat gegarandeerd in één tekstnode zit ("MB, free", "base + bought"). Anders concludeer je ten onrechte dat een render kapot is.
[2026-08-03] gefabriceerd-schema-in-voorbeelden: een met-de-hand-getypt "voorbeeld" van machine-output (RAG-JSON-chunk met `source_url`) had een veld dat de echte generator NOOIT uitstuurt, en dezelfde verzonnen schema's stonden verspreid over homepage + artikelen. Plausibel-maar-fout is het gevaarlijkst: niemand controleert een voorbeeld dat er echt uitziet. Regel: een voorbeeld van generator-/API-output NOOIT met de hand typen — draai de ECHTE generator op ECHTE data (hier: `formatTranscript` op de fixture-transcript uit de DB), knip in met `…`, verzin geen velden/waarden, en leg de herkomst vast in een databestand (`homeExportSamples.ts`-header + product-truth) zodat een latere sessie het 1-op-1 kan reproduceren. Het echte RAG-veld is `deep_link`/`chunk_id`/`token_count_estimate`/`total_chunks`, niet `source_url`. Zie [[content-sitemap]] (was óók stale hierop).
[2026-08-03] audit-doc-veroudert: een content-audit/kaart (`content-sitemap.md`, snapshot 2026-07-18) beschreef claims die er allang niet meer zijn (99.4%, 67 talen, lege privacy/terms) — "een kaart die de werkelijkheid niet meer beschrijft is erger dan geen kaart." Regel: dateer zulke snapshot-docs, en werk ze bij tegen de CODE (grep de claim: `99.4` → 0 hits = weg), niet tegen de vorige versie van de kaart of tegen aannames. Laat volatiele tellingen (formaten/credits/talen/opslag) expliciet naar de live bron verwijzen (`pricing.ts`/`exportFormats.ts`/`storage.ts`/product-truth) i.p.v. ze in de kaart te herhalen; markeer wat NIET her-geauditeerd is zodat een lezer weet wat te vertrouwen.
