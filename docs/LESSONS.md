# LESSONS.md

Terugkerende valkuilen voor INDXR.AI. Lees bij sessiestart. Append-only door CC bij elke correctie van Khidr op een patroon (niet one-off).

Format: `[YYYY-MM-DD] <gebied>: <wat fout ging> → <regel om herhaling te voorkomen>`

---

[2026-07-02] stop-hook-conventie: `.claude/hooks/check-wiki.sh` werkt correct. "No stderr output" in de stop-hook-feedback is GEEN fout — de hook schrijft de check-melding naar stdout (exit 2), niet naar stderr. Dat is de bedoelde conventie: de runtime leest stdout bij exit 2 als feedback-bericht aan CC. Het bestand bestaat, is executable, en wordt correct uitgevoerd. Melding "No such file or directory" trad op als het bestand werkelijk ontbrak; na herstel is "No stderr output" de gewenste status.
[2026-07-03] bulk-ZIP-export: bestandsnamen in JSZip moeten gegarandeerd uniek zijn — JSZip overschrijft stil bij dubbele naam (laatste wint, geen error). Gebruik de volledige getitelde slug als primaire disambiguator: `slugify(title)` = lowercase, `[^a-z0-9]+→-`, leading/trailing `-` strippen — **geen slice, geen lengte-limiet, geen video_id in bestandsnaam**. Schema: `${slug}_${type}[_variant].${ext}` (bijv. `justice-ep-01_txt_timestamps.txt`, `surah-ar-rahman_rag_120s.json`). Voeg een teller-suffix (`_2`, `_3`) toe als fallback bij werkelijk identieke titels. Voeg altijd een archief-count = selectie-count-check toe en toon een warning bij mismatch. Geldt voor alle bulk-export-flows (TXT, MD, JSON, CSV, SRT, VTT, RAG).
[2026-07-02] pnpm-lockfile: elke package.json-wijziging (dependency toevoegen/verwijderen) vereist dat pnpm-lock.yaml in dezelfde commit geregenereerd + meegecommit wordt via `pnpm install` → Vercel faalt hard met ERR_PNPM_OUTDATED_LOCKFILE bij desync; lokale `pnpm build` vangt dit NIET — verifieer altijd met `pnpm install --frozen-lockfile` vóór push. Gemist bij commit 99c7099.
[2026-07-02] feedback-pattern: sonner/toast is verboden — auto-dismiss is niet toegestaan in deze codebase → gebruik altijd FeedbackCard (packages/shared/src/components/ui/FeedbackCard.tsx) voor inline persistente feedback. Copy-success → button-level useState bool (1.5s timeout, geen banner). Download-success → altijd stil (het bestand opent). Sidebar-berichten → compact inline banner (geen FeedbackCard-icoon). Financiële routes (checkout, credit-claim) → FeedbackCard met onDismiss, nooit auto-dismiss of success weglaten. RAG-bulk-success → persistent inline card met X-dismiss, geen setTimeout-close; de dialog sluit alleen via gebruikersactie (X-knop of Cancel).
[2026-07-03] flex-truncate: `truncate` (overflow:hidden/text-overflow:ellipsis) werkt niet op een flex-child zonder `min-w-0`. Flex-items hebben `min-width: auto` by default, waardoor ze niet onder hun content-breedte krimpen en het label naast hen buiten de container duwt. Altijd `min-w-0` toevoegen aan de truncerende flex-child naast `flex-1 truncate`.
[2026-07-03] grid-child-overflow: shadcn `DialogContent` (en `AlertDialogContent`) is `display: grid`. Grid-children hebben net als flex-items `min-width: auto` by default en krimpen niet onder hun inhoudsbreedte. De fix hoort in de **gedeelde primitive**, niet per callsite: voeg `grid-cols-[minmax(0,1fr)]` toe aan de base className van `DialogContent`. Dit begrenst de grid-track zelf tot de beschikbare breedte — alle huidige en toekomstige dialog-children erven dit automatisch. Absoluut gepositioneerde children (sluit-knop) zijn geen grid-items en worden niet geraakt. Per-callsite `min-w-0` op een wrapper is redundant als de primitive dit dekt en mag dan worden verwijderd. `min-w-0` op flex-children voor truncatie en `overflow-x-hidden` op scroll-containers blijven correct als diepere lagen.
[2026-07-02] credit-routes: RAG-export component (RagExportView) bood een gratis download-pad zonder credit-aftrek — bereikbaar via directe URL-manipulatie (?tab=developer) → elke credit-gerelateerde component of route vereist een expliciete server-side guard op het eerste-export-pad. Een component dat "gratis" aanneemt zonder de rag_exports-status te verifiëren is een lek. Defensief: render-guard in de page + component-level fallback.
[2026-05-04] arq: ack_late bestaat niet in arq 0.28.0 (Celery-concept) → bij crash-recovery design: ga uit van idempotency-vlaggen + watchdog cron, niet van library-features.
[2026-05-04] supabase: playlist-status enum is `'complete'` niet `'completed'` → wijk niet af van migratie-SQL als bron van waarheid voor status-strings.
[2026-05-04] supabase: `idempotency_keys` tabel is in ADR-019 beschreven maar nooit aangemaakt → verifieer tabel-bestaan via `information_schema.tables` voordat je ernaar verwijst in code.
[2026-05-04] yt-dlp: hergebruik van zelfde proxy-session bij retry helpt niet bij bot-detection → voor retries: nieuwe session-suffix.
[2026-05-04] sentry: serverless functions sturen geen envelope vóór process kill → altijd `await Sentry.flush(2000)` na captureException + `export const runtime = 'nodejs'` op API routes die capturen.
[2026-05-04] credits: nooit credits aftrekken via direct INSERT → altijd `deduct_credits_atomic` RPC of de geüpgrade `update_playlist_video_progress` RPC.
[2026-05-04] tiptap: `immediatelyRender: true` veroorzaakt SSR hydration mismatch → altijd `immediatelyRender: false`.
[2026-05-04] logging: uvicorn override't `logging.basicConfig` zonder `force=True`; Sentry SDK reset root logger ná init → na `sentry_sdk.init()` expliciet `logging.getLogger().setLevel(INFO)` zetten.
[2026-05-05] wiki-vs-code: known-issues.md claimde "rate limiting bewust uitgeschakeld" maar src/lib/ratelimit.ts deed wél echte Redis-calls totdat UPSTASH_* env vars op Vercel op Development scope werden gezet → 500K Upstash commands in 5 dagen mede-veroorzaakt door dit blinde-vlek. Verifieer wiki-claims over runtime-gedrag tegen bron-code, vooral bij infrastructuur-statements.
[2026-05-17] supabase-ssr: Gebruik getClaims() in middleware, niet getUser(). getClaims() returnt null bij geen sessie zonder retry-loop en zonder Supabase-netwerk-calls. getUser() maakt een server-side validatie-call en kan refresh-loops triggeren bij stale tokens (bewezen: 500K Upstash commands in 5 dagen, commit 22a0059). Voeg GEEN error-recovery cookie-clearing toe in middleware — clearAuthCookies() wist ook PKCE code-verifier (sb-*-auth-token-code-verifier), waardoor exchangeCodeForSession() faalt in OAuth/email-callback flows. Volg het officiële Supabase Next.js template letterlijk (vercel/next.js with-supabase).
[2026-05-17] supabase-ssr: Sluit auth callback routes uit van updateSession middleware via de matcher. Routes met exchangeCodeForSession() (bijv. /auth/callback) creëren zelf een sessie en hebben geen session-refresh nodig. updateSession() op een callback route kan de tijdelijke PKCE verifier-cookie verwijderen vóór de exchange → silent failure. Voeg auth/callback toe als uitzondering in de middleware matcher.
[2026-05-05] supabase: refresh_token_not_found in middleware veroorzaakt infinite refresh-loop in browser → blow-out van Vercel rate-limit Redis quota. [VERVANGEN door getClaims() fix 2026-05-17 — zie bovenstaande regels voor correcte aanpak]
[2026-05-05] next-server-actions: window.location.href na await serverAction() veroorzaakt "Error in input stream" TypeError omdat de browser de RSC action-response stream aborteert mid-transmission (Next.js GitHub #81377) → nooit window.location.href of router.push na een Server Action aanroep; gebruik redirect() in de Server Action zelf voor cross-host navigatie.
[2026-05-05] subdomain-split: redirect("/<auth-path>") in Server Components op app.indxr.ai veroorzaakt 307 → middleware 308 → cross-origin chain die RSC prefetch parser breekt → TypeError "Error in input stream". Server-side redirects MOETEN absolute marketing-URL gebruiken (NEXT_PUBLIC_MARKETING_URL) in subdomain-split setup, niet relatief.
[2026-05-04] subdomain-split: alle `<Link href="/<marketing-path>">` in components die op de app-host renderen (niet alleen shell-components zoals Header/Footer, ook alle children van dashboard pages zoals VideoTab, AudioTab, PlaylistTab, TranscriptViewer, TranscriptCard) veroorzaken Next.js prefetch-crash → TypeError: Error in input stream. Fix: vervang door `<a href={marketingHref(...)}>`. Inventaris-grep moet ALLE src/components/ + src/app/(app)/ scannen, niet alleen shells.
[2026-05-05] cross-host-links: audit cross-host links op drie patronen, niet alleen <Link>: (1) `<Link href="/pad">`, (2) `<a href="/pad">`, (3) `window.location.href = '/pad'`. Alle drie vereisen appHref()/marketingHref() wanneer de component op een andere host gerenderd kan worden.
[2026-05-05] env-vars: vermijd meerdere env-var-namen voor hetzelfde concept (NEXT_PUBLIC_SITE_URL naast NEXT_PUBLIC_APP_URL + NEXT_PUBLIC_MARKETING_URL is verwarrend). Eén concept = één var. Bij subdomain-split: NEXT_PUBLIC_APP_URL voor app-host, NEXT_PUBLIC_MARKETING_URL voor marketing-host.
[2026-05-05] vercel-limits: directe browser→Railway POST voor audio uploads omzeilt Vercel's 4.5MB request body limiet (AudioTab.tsx). Noem de env-var expliciet NEXT_PUBLIC_AUDIO_UPLOAD_URL zodat het doel helder is — niet NEXT_PUBLIC_PYTHON_BACKEND_URL hergebruiken voor dit specifieke geval.
[2026-05-05] git-workflow: CC heeft git commit uitgevoerd zonder expliciete toestemming → CC voert NOOIT git add/commit/push uit zonder expliciete toestemming per taak. Khidr geeft commit-toestemming in de taakprompt of in een vervolgbericht.
[2026-05-06] vercel-env-vars: Sensitive env vars kunnen NIET op alleen Development scope worden gezet — Vercel forceert minstens Production of Preview voor sensitive vars. De C.3.1 workaround (env vars op Development scope om noopLimiter te activeren in productie) is daarom niet herbruikbaar voor sensitive vars. Alternatief: vars volledig verwijderen om noopLimiter te activeren, of niet-sensitive maken.
[2026-05-06] vercel-env-vars: Vercel UI env var input accepteert GEEN quotes rond waardes. .env files gebruiken KEY="value" syntax maar in Vercel UI moet alleen de rauwe waarde, anders worden de quotes letterlijk deel van de string. Bij plak-uit-.env: quotes verwijderen.
[2026-05-06] ui-state: Twee parallelle UI-state mechanismen op hetzelfde concept (Shadcn useSidebar + custom localStorage voor sidebar breedte) zijn een recipe for desync — één knop doet visueel niets op desktop omdat hij de verkeerde state bestuurt. Kies één mechanism per UI-concept en verwijder de andere. Bij Shadcn-componenten met custom overrides: de custom state wint altijd; verwijder de Shadcn default trigger als die geen effect heeft.
[2026-05-07] shadcn-sidebar: Shadcn Sidebar `variant="inset"` (en alle non-collapsible="none" varianten) gebruikt `position: fixed; top: 4rem` — hardcoded 4rem header assumptie. Conflicteert met custom topbar heights. Gebruik `collapsible="none"` voor flex-flow integratie: sidebar zit dan in normale document flow, geen fixed positioning, hoogte bepaald door flex container. SidebarProvider moet OUTER wrapper blijven (context scope voor useSidebar hooks), layout-flex in geneste div erin.
[2026-05-07] react-context: Componenten die useContext() of custom hooks gebruiken (zoals Shadcn useSidebar()) MOETEN binnen de bijbehorende Provider rendered worden. Bij refactoring naar custom layout: SidebarProvider als outer wrapper laten, ook al stuurt hij visueel niet de hele layout. Provider = context scope, niet visuele container.
[2026-05-07] css-absolute-containment: Een `position: absolute` child van een `position: sticky` parent (bijv. Button inside sticky header) gebruikt de sticky parent als containing block. Maar zonder `overflow: hidden` op de sticky parent kan de absolute child buiten de button bounds verschijnen. Fix: altijd `relative overflow-hidden` op het directe parent element van een absolute-positioned child, niet alleen op een verre ancestor.
[2026-05-07] css-variables: Een CSS-variabele die zichzelf referentieert in een inline style (`style={{ "--x": "var(--x)" }}`) levert een guaranteed-invalid value op dat element. Child-elementen die `var(--x)` gebruiken krijgen 'invalid' terug en vallen terug op default (bijv. transparent voor kleuren). Patroon ontstaat bij mislukte pogingen om een Tailwind/Shadcn defaultkleur te overriden — gebruik in plaats daarvan een ANDERE CSS-variabele als bron, of een directe kleurwaarde.
[2026-05-17] cross-host-redirect: router.push('/pad') of router.push() in een component op een andere host dan het doelpad blijft op de huidige host → 404 op de ontbrekende route. Gebruik window.location.href = appHref('/...') (of marketingHref) voor cross-host navigatie. Next.js Link en router.push zijn altijd same-origin.
[2026-05-17] env-vars-vercel: BACKEND_API_SECRET mismatch tussen Vercel en Railway gaf 401 op alle backend API calls. Vercel UI accepteert geen quotes rond env values — letterlijke quotes worden opgeslagen als deel van de waarde. Bij elke env var: paste zonder aanhalingstekens, check scope (Production + Preview), redeploy zonder build cache om var op te halen. Var moet identiek zijn op alle Vercel projecten die de backend aanroepen (indxr-marketing + indxr-app). Na migratie naar nieuwe Vercel projecten altijd opnieuw verifiëren — env vars worden niet automatisch overgenomen.
[2026-05-17] supabase-recovery: PKCE-based recovery emails (password reset, magic link) moeten redirectTo instellen op een route die exchangeCodeForSession aanroept. Direct naar een protected app-pagina sturen werkt niet — de code vervalt en geeft otp_expired. Patroon: redirectTo = {marketing}/auth/callback?next=<encoded-final-target>; callback wisselt de code in en redirect dan naar final-target met hostname-validatie (voorkom open redirect).
[2026-05-17] upstash-arq: noopLimiter-pattern voor Upstash quota-mitigatie werkt op Vercel (env vars verwijderen → noopLimiter actief) maar NIET voor ARQ workers. ARQ roept bij init `pool.delete(self.health_check_key)` aan — zonder werkende Redis crasht de worker bij elke startup. Bij quota-incident: óf plan upgraden, óf worker offline nemen, niet "vergeten en doorgaan".
[2026-06-04] redis-serverless-vs-polling: Serverless per-commando Redis (Upstash) is structureel ongeschikt voor constant pollende workers. ARQ met `poll_delay=0.5s` + `health_check_interval=1s` genereert ~10.860 Redis-commando's/uur idle = ~7,84M/maand = 15,7× Upstash Free Tier (500K/maand) — zonder ook maar één job. Regel: gebruik container-Redis (Railway, geen kosten per commando) voor ARQ workers; gebruik Upstash alleen voor sporadische serverless calls (rate-limiter, cache). Het mismatch-probleem is niet configureerbaar weg via hogere poll_delay — health_check (1/s) domineert al bij poll_delay=5s.
[2026-06-27] playlist-shared-helper: bij playlist-jobs die een gedeelde helper aanroepen (bijv. do_assemblyai_transcription), verifieer dat cache-reads/guards in de helper zelf zitten en niet in een wrapper (run_whisper_job) die de playlist overslaat — anders mist één pad de optimalisatie stil zonder fout of log.
[2026-06-27] yt-dlp-tlang: `subtitleslangs` filtert `info['automatic_captions']` NIET in `extract_info(download=False)` mode — alle ~120 YouTube-vertaaltalen komen terug ongeacht de instelling. De enige betrouwbare manier om een native track te selecteren is: zoek sleutels die eindigen op `-orig` in `info['automatic_captions']` (bv. `ja-orig`) — die hebben nooit `tlang=` in de URL. Sleutels zonder `-orig` (bv. `en` voor een Japanse video) bevatten een machinevertaals-URL. Gebruik NOOIT de taalcode uit de YouTube Data API (`videos.list language`) als enige grondslag voor track-selectie: de API geeft `'en'` terug voor niet-Engelse video's (bewezen voor iKtPI8IMuOM, Japans). Verifieer altijd URL-niveau (`'tlang=' not in url`) als safety net.
[2026-06-04] tailwind-v4-monorepo: Tailwind v4 (@tailwindcss/postcss) scant NIET automatisch packages buiten de app-directory. In een monorepo met packages/shared/ moeten alle apps een `@source "../../../../packages/shared/src/**/*.{ts,tsx}";` directive toevoegen aan hun tokens.css (pad relatief ten opzichte van het CSS-bestand). Zonder dit ontbreken alle Tailwind utility classes die uitsluitend in shared components voorkomen (bg-accent, md:flex, md:hidden, text-fg-subtle, etc.) uit de CSS output — resulterend in onzichtbare UI elementen en kapotte responsive layouts.
[2026-06-30] supabase-migrations: 8-cijferige timestamp-prefixes (bijv. 20260302_) en SQL-Editor-wijzigingen buiten de CLI zijn onzichtbaar voor supabase_migrations.schema_migrations → schema-drift zonder foutmelding. Remedie: baseline-squash via Management API introspectie (pg_get_functiondef, pg_policies, information_schema, pg_constraint voor cross-schema FKs) → één baseline .sql in migrations/, tracking-reset via DELETE + INSERT op schema_migrations, pre-baseline bestanden naar migrations_archive/. Triggers ophalen apart via pg_trigger WHERE NOT tgisinternal (pg_get_triggerdef includeert auth-schema triggers zoals on_auth_user_created).
[2026-07-01] ui-feedback: geen toasts, ooit — ook niet in admin-only UI, ook niet als "netter maken" van een bevestiging. Toast = auto-dismiss via setTimeout = verboden. Altijd inline persistente success/error state die blijft staan tot de volgende actie of expliciete sluiting door de user.
[2026-06-30] supabase-migrations: migratiebestanden MOETEN een 14-cijferige YYYYMMDDHHMMSS-prefix hebben (zoals `20260630170359_messages_archived.sql`). 8-cijferig is verboden — die vallen buiten schema_migrations-tracking. Pas altijd toe via Supabase MCP apply_migration of Management API, nooit via `supabase db push` of SQL-Editor.
[2026-07-01] docs-vs-code: CLAUDE.md (en andere docs) kunnen beschrijven hoe de code eruitzag ten tijde van schrijven, niet hoe hij nu is. "Geen OKLCH (vervangen door hex)" bleek de live staat exact omgekeerd te zijn. Patroon: elk CLAUDE.md-claim over runtime-gedrag, token-namen, of library-gebruik verifiëren tegen de bronbestanden vóór je erop vertrouwt — zeker na redesign-sprints of grote refactors. Dit is het tweede blinde-vlek-patroon na de Upstash-wiki-drift (2026-05-05).
[2026-07-03] design-library-stijltaal: Library-redesign (stijl-anker voor de dashboard-shell) vestigde deze taal, over te nemen door volgende dashboard-pagina's: titel-gedreven rijen met `line-clamp-2` (nooit line-clamp-3), haarlijn-scheidingen (`divide-border-subtle`) i.p.v. zware per-rij card-borders, twee badge-families (bron = `bg-info-subtle text-info`, AI-afgeleide outputs = `bg-violet-subtle text-violet`) plus een neutrale `border-border`-collectiebadge, metadata (duur/woorden/datum) altijd rechts-uitgelijnd en gedempt (`text-fg-muted`) nooit concurrerend met de titel. Nieuwe tokens `--info`/`--violet` (+ `-subtle`/`-fg`) toegevoegd aan beide tokens.css-bestanden — volg het bestaande `success`/`warning`/`error`-patroon exact bij het toevoegen van nieuwe badge-families, gebruik de `-subtle` bg + basis-token als tekstkleur (niet de `-fg`-variant, die staat er als optie maar wordt in de praktijk niet gebruikt).
[2026-07-03] design-hexagon-bg-uitzondering: wiki/design/research/batch-3b-ux-aesthetic.md §5.4 classificeert een hexagon-achtergrondtextuur in de Library-lijst als Israf ("competes with content") — hexagon-achtergronden zijn daar beperkt tot reflectieve oppervlakken (login, 404, welcome, footer). Op expliciet verzoek van Khidr is dit voor de Library-body toch doorgevoerd, op zeer lage opaciteit (`opacity-[0.035] dark:opacity-[0.05]`) via `HexagonPattern` uit `packages/shared/src/components/icons/`. Behandel dit als bewuste, gedocumenteerde uitzondering (zie ook §5.4-notitie in de wiki zelf) — niet als precedent om hexagon-achtergronden stilzwijgend ook op andere werk-oppervlakken (Transcribe, Settings, Admin) toe te voegen zonder dezelfde expliciete afweging.
[2026-07-03] destructive-actions-confirmatie: elke destructieve actie (delete, vooral bulk-delete) MOET een bevestigingsdialoog hebben vóór uitvoering — ook als de bestaande component die nooit had. In TranscriptList.tsx had per-rij delete géén bevestiging (direct `onDelete(t.id)` op klik) en bulk-delete gebruikte een ongestylede `window.confirm()`. Beide zijn vervangen door de gedeelde `AlertDialog`-primitive (`packages/shared/src/components/ui/alert-dialog.tsx`) die titel (per-rij) of aantal (bulk) toont vóór bevestiging. Patroon voor nieuwe destructieve acties op andere pagina's: nooit een directe delete-call op een klik-handler zetten, altijd via een `deleteTarget`-state + AlertDialog, en `window.confirm()` nooit gebruiken (onstyled, inconsistent met design-system, makkelijk over het hoofd te zien tijdens redesigns omdat de functionaliteit "al bestond").
[2026-07-04] design-badge-hue-systeem: Library badge-kleuren volgen nu een OKLCH hue-familie-systeem (tokens.css "Badge families"). Regel bij toevoegen/wijzigen van badge-families: houd L en C CONSTANT per rol (text/base én -subtle bg), verschuif ALLEEN de hue tussen families zodat ze perceptueel gelijkwaardig en dark-mode-stabiel zijn. Families: auto-captions = sky (hue 230), AI-transcription = indigo (265), AI-summary = violet (300), RAG = teal (185) — minimaal ~35° hue-afstand. "Edited"-variant = ZELFDE hue, hogere L (`-soft`/`-edit` token). LET OP: op een lichte `-subtle` bg vermindert hogere text-L het contrast; in light mode botst "hogere L" met WCAG AA, dus draag het "lichter"-signaal deels via een lichtere bg en houd de text AA-veilig (geverifieerd: light min 4.75, dark min 6.73 op de eigen -subtle bg). Verifieer ELKE nieuwe familie/edited-paar numeriek (OKLCH→sRGB→WCAG) in beide thema's vóór merge; vergroot hue-afstand als twee families te dicht liggen (teal vs sky was het krapste). Badges renderen als `bg-{familie}-subtle text-{familie}` (basis-token als tekst, niet -fg). De oude `--info`/`--violet`-twee-familie-opzet is vervangen; `--info` verwijderd.
[2026-07-04] library-optimistic-mutaties: Library UI-mutaties die één rij aanraken (mark-as-read, en soortgelijke read/unread/toggle-acties) gebruiken het React 19 `useOptimistic` + `startTransition`-patroon, NOOIT een full-list-reload via `router.refresh()`, `revalidatePath()` of een `transcripts-updated`-event dat `fetchTranscripts()` opnieuw triggert. Reden: een refetch zet `loading=true` en herbouwt de lijst → zichtbare flits/reflow en scroll-reset voor een wijziging die maar één badge betreft. Correcte opzet (TranscriptList.tsx `handleMarkAsRead`): committed state in `useState` (`readIds`), overlay via `useOptimistic(readIds, (prev,id)=>new Set(prev).add(id))`; de optimistic setter (`addOptimisticRead`) MOET binnen de `startTransition`-async-callback worden aangeroepen anders flitst de badge terug; de canonicale mutatie (supabase `.update({viewed_at})`) blijft ongewijzigd draaien; alleen bij succes `setReadIds` committen, bij fout niets doen zodat de overlay vanzelf terugrolt. Verander bij zulke taken alleen de UI-update (reload→optimistic), niet de onderliggende mutatie, en dispatch géén `transcripts-updated` voor single-row reads.
[2026-07-04] youtube-data-api-playlist-paginatie: de YouTube Data API `playlistItems.list` retourneert MAX 50 items per pagina — voor playlists >50 video's MOET je met `nextPageToken` doorpagineren tot het token weg is (of tot een bovengrens; wij gebruiken 500, gelijk aan de yt-dlp-fallback `playlist_items: '1-500'`). Zonder loop haal je stil alleen de eerste 50 op. LET OP: ook `videos.list` (voor duur/captions-details) accepteert max 50 IDs per call — na paginatie moet je de video-ID's in chunks van 50 batchen, anders resolven alleen de eerste 50. Bijkomend anti-patroon dat hierdoor ontstond: een verzonnen "X unavailable"-telling als `total_count − opgehaald` (= puur de door de cap afgekapte rest) met misleidende hardcoded copy "private, members-only, or deleted" zonder enige echte check. Regel: een "unavailable"-telling moet berekend worden uit een échte per-video-resolutie (videos.list gaf geen details terug óf titel is "Private/Deleted video"), niet uit een aftreksom tegen een (mogelijk cap-opgeblazen of inconsistente) itemCount. Zie `backend/youtube_client.py:get_playlist_items` (retourneert nu `unavailable_count`) en `PlaylistManager.tsx` (leest `playlist.unavailable_count`).
[2026-07-04] home-dashboard-credits-bron: de Home/dashboard-pagina (`apps/app/src/app/dashboard/page.tsx`) MOET het credit-saldo uit dezelfde live bron halen als topbar en sidebar: `useAuth().credits` → RPC `get_user_credits` → `user_credits.credits`. Nooit een aparte/eigen RPC. Concreet ging het mis: Home was een server-component die een niet-bestaande RPC `get_credit_balance` aanriep, die faalde en via `?? 0` stil terugviel op 0 — terwijl topbar/sidebar wél het juiste saldo toonden. Twee lessen: (a) verzin geen tweede credits-leesbron; hergebruik `useAuth().credits` (client) zodat het getal consistent is én binnen een sessie meeververs t na aftrek; (b) een server-component rendert eenmalig per request en ververst niet — voor live-veranderende waarden (credits) hoort de weergave client-side via de gedeelde context, niet een one-shot server-render. Fix: klein client-component `HomeCreditsBalance` (`apps/app/src/components/dashboard/`). Raak nooit de credit-aftrek-RPC/`user_credits`-mutatie aan bij zo'n leesbron-correctie.
[2026-07-04] caption-timedtext-proxy-rotatie: caption/timedtext-downloads (yt-dlp/httpx VTT-fetch in `youtube_utils.py:extract_with_ytdlp`) MOETEN de proxy-sessie roteren per retry — net als de audio/Whisper-tak al doet (`transcription_pipeline.py` bouwt `[get_proxy_url(session_id=f"{sid}-r{i}") for i in range(1,4)]`, `audio_utils.py` kiest `proxy_urls[attempt-1]`). Eén sticky Decodo exit-IP vastpinnen over alle 3 de retries garandeert herhaalde 429 op schaal: als YouTube's timedtext-endpoint dat IP rate-limit, geeft élke retry op datzelfde IP weer 429. Concreet fout: `dl_proxy_url` werd één keer vóór de retry-loop berekend en hergebruikt; de 30s-deferred playlist-retry-pass (`worker.py`) gebruikte bovendien exact dezelfde per-video sessie als de mislukte eerste poging. Fix: per attempt `get_proxy_url(session_id=f"{session_id}-r{attempt}")`, en de retry-pass een `-retry`-suffix zodat die op een vers IP landt. Regel: elke geproxyde YouTube-download-retry (audio én captions) roteert de sessie; pin nooit één IP over retries. Combineer met async exponentiële backoff + jitter (nooit blocking `time.sleep` op de event-loop). NB: concurrency verlagen helpt hier NIET — de playlist-verwerking is al strikt sequentieel (`_enqueue_next`), dus de 429 komt van IP-hergebruik, niet van fan-out.
[2026-07-05] marketing-vs-transactionele-email-scheiding: marketing/broadcast-e-mail is een APART kanaal van transactionele support-mail. `profiles.marketing_unsubscribed` (broadcast opt-out) ≠ `profiles.email_notifications` (support-reply-toggle). Regel: een marketing-unsubscribe mag NOOIT transactionele mail (ticket-replies via `notifyUser`) breken, en omgekeerd. Concreet in de code: `sendBroadcastEmails`/de broadcast-send-route filtert op `marketing_unsubscribed`, `notifyUser` (support) checkt uitsluitend `email_notifications` — nooit kruisverwijzen. Geverifieerd: na een marketing-unsubscribe blijft `email_notifications` ongewijzigd `true`. Bij het toevoegen van nieuwe e-mailkanalen: geef elk kanaal zijn eigen opt-out-veld en check alleen dat veld. Bijkomend patroon uit deze feature: (a) niet elke auth-user heeft een `profiles`-rij (6 users vs 5 profiles) → de unsubscribe-route MOET upserten (`onConflict:'id'`), en send-time-checks behandelen een ontbrekende rij als "niet uitgeschreven"; (b) unsubscribe-token = HMAC-signed (userId ingebed + gesigneerd), geen radbare/omwisselbare user_id, geen opslag nodig; secret valt terug op `SUPABASE_SERVICE_ROLE_KEY` als `UNSUBSCRIBE_SECRET` niet gezet is; (c) `admin.auth.admin.listUsers` is hard `perPage:1000` → altijd pagineren voor >1000 users; (d) een broadcast-send-route MOET een expliciete `confirmCount`-gate hebben (409 bij mismatch) zodat er nooit verzonden wordt zonder bevestigde recipient-count.
[2026-07-05] broadcast-service-vs-marketing-classificatie: broadcast-e-mail kent twee WETTELIJKE types en de admin-composer classificeert ze expliciet — service (legitimate interest: storingen/account-mededelingen, gaat naar iedereen, GEEN opt-out-respect, GEEN unsubscribe-footer, en MAG GEEN promotionele inhoud bevatten — promotie in een service-mail maakt het juridisch marketing) vs marketing (consent: honoreert `marketing_unsubscribed`, verplichte unsubscribe-footer + `List-Unsubscribe`-header). KRITIEK fail-safe: de classificatie moet server-side defaulten naar "marketing" — alleen een expliciete `messageType === "service"` schakelt de unsubscribe-machinerie uit (`const isService = messageType === "service"` in `api/admin/broadcast/route.ts`); elke ontbrekende/onverwachte waarde valt terug op marketing zodat een opt-out nooit per ongeluk omzeild wordt. De param loopt door drie lagen: composer (`messageType`-button-group) → send-route (`isService` stuurt filter-skip + `skippedUnsubscribed=0`) → `sendBroadcastEmails({includeUnsubscribe})` (bij service géén footer/header, `unsubscribeUrl` optioneel weggelaten). De in-app-message-insert is voor beide types identiek — alleen het e-mailkanaal verschilt. Bouwt hierop voort: [[marketing-vs-transactionele-email-scheiding]].
[2026-07-05] broadcast-email-html-template: broadcast-e-mail (`sendBroadcastEmails` in `apps/app/src/lib/mail.ts`) gebruikt een branded, e-mail-VEILIGE HTML-template — NIET web-HTML. E-mailclients (Gmail/Outlook/Apple Mail) strippen `<style>`-blokken, negeren flexbox/grid, droppen custom fonts en ondersteunen GEEN OKLCH. Regels: (a) table-based layout (`<table role="presentation" cellpadding=0 cellspacing=0 border=0>`), nooit flexbox/grid; (b) ALLE styling inline (`style="..."` per element), geen `<style>` of externe/token-CSS; (c) web-safe font-stack met fallback (`-apple-system, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`) — IBM Plex bestaat niet in e-mail; (d) merk-kleuren als HEX (OKLCH uit tokens.css vooraf omgezet: accent `#d79628`, bg `#fcfaf7`, surface `#ffffff`, fg `#27231f`, border `#dbd7d2`, fg-muted `#77726d`, accent-fg-link `#643400`); (e) max-width ~600px gecentreerd; (f) logo via ABSOLUTE URL (`${appUrl}/logo/indxr-wordmark-black-transparent.png`) — relatieve paden resolven niet in e-mail; light-variant want e-mail heeft geen dark-mode-garantie; (g) ALTIJD een plain-text-fallback (`text`) náást `html` meesturen (spam-filters/clients vereisen dat). TYPE-AFHANKELIJKE footer via de bestaande `includeUnsubscribe`-param (niet opnieuw classificeren): marketing = "Unsubscribe" als TEKSTLINK (rauwe token-URL achter het woord, nooit zichtbaar) + `List-Unsubscribe`-header; service = privacy-policy-link, GEEN unsubscribe, GEEN `List-Unsubscribe`-header. User-input (subject/body) HTML-escapen. Raak bij template-werk nooit de recipient-filtering, de `marketing_unsubscribed`-skip, de route-classificatie of de HMAC-unsubscribe-token aan — alleen de body-opmaak. Zie [[broadcast-service-vs-marketing-classificatie]] en [[marketing-vs-transactionele-email-scheiding]].
[2026-07-05] arq-jobtimeout-en-interrupted-state: TWEE lessen uit de "playlist toont Complete terwijl 'ie draait"-bug. (1) ARQ `job_timeout` is een instelbare DEFAULT (300s), GEEN plafond — je overschrijft 'm globaal op `WorkerSettings.job_timeout` of per functie (per-functie wint van de worker-default), met een waarde die de ZWAARSTE ENKELE taak moet dekken (bij een playlist: de langste enkele video, niet de som — het is een keten van per-video-jobs). In deze codebase staat `job_timeout=7200` (2u) globaal (worker.py `WorkerSettings`), wat de langste video ruim dekt (gemeten: max ~36 min/video). Timeout was hier dus NIET de oorzaak — meet de echte config vóór je 'm als oorzaak aanwijst. (2) `'interrupted'` is een TRANSIENTE, HERSTELBARE status, GEEN eindtoestand. Het poll-endpoint (`main.py`, `HEARTBEAT_STALE_SECS=300`) markeert een job `'interrupted'` bij een stale heartbeat (worker-restart of lange stall); de worker-watchdog (`watchdog_interrupted_jobs`, elke 2 min) re-enqueuet 'm binnen ~2 min (crash-recovery, `watchdog_attempts` telt op). Regel: behandel `'interrupted'` NOOIT als terminal in de frontend — anders stopt de UI met pollen en rendert 'ie "Complete" terwijl de backend nog draait (en de watchdog 'm hervat). Alleen `'complete'` en `'error'` zijn terminal (`error` = watchdog Pass 2 na uitgeputte retries). Concreet was de bug: `useJobStatus.ts` `TERMINAL`-set bevatte `'interrupted'` → poll stopte + `_handlePlaylistComplete` vuurde; en de PlaylistTab mount-resume gooide een `'interrupted'` job weg i.p.v. hervatten. Bijkomend anti-patroon: de completion-copy toonde `total = Object.keys(videoStatuses).length` (lokaal geziene count, "17 of 18") i.p.v. `total_videos` (63) — bij een vroegtijdige render geeft dat een misleidend totaal; opgelost doordat de UI nu pas bij échte `'complete'` rendert (dan bevat video_results alle 63). Keten-continuïteit bij CancelledError: `process_playlist_video` (`except Exception`) vangt GEEN `asyncio.CancelledError` (worker-restart/cancel) — de in-process enqueue-van-de-volgende sterft dan, maar de watchdog vangt dit robuust op (bewezen: job ee6e7a81 hervatte van 21→63 met `watchdog_attempts=1`). Heartbeat is adequaat: `_run_with_heartbeat` tikt elke 60s tijdens ZOWEL audio-download als AssemblyAI-transcriptie (< 300s drempel). Zie [[caption-timedtext-proxy-rotatie]].
[2026-07-05] email-darkmode-colorscheme-en-logo: transactionele/broadcast-e-mail vereist een DOCUMENT-wrapper met color-scheme-declaraties + een dark-mode-veilig logo — nooit een zwart-transparant logo op een wit table-fragment. Twee gekoppelde regels. (1) COLOR-SCHEME: wikkel de verstuurde HTML in `<!doctype html><html><head>…</head><body>…</body></html>` en zet in de `<head>` `<meta name="color-scheme" content="light dark">` + `<meta name="supported-color-schemes" content="light dark">`, plus `color-scheme:light dark;` op body-niveau. Deze vertellen Gmail/Apple Mail dat dark mode is afgehandeld en dempen agressieve volledige inversie. Een kaal `<table>`-fragment (geen `<head>`) heeft geen plek voor deze meta's → clients inverteren ongeremd. (2) LOGO: afbeeldingen worden NOOIT mee-geïnverteerd door clients, dus een transparant logo leest maar op één achtergrond. Oplossing zonder media-query-swap: WITTE wordmark (`indxr-wordmark-white-transparent.png`) op een GENUINE-DONKERE header-cel `background-color:#141414` (NIET puur `#000000` — Apple Mail auto-flipt puur zwart). Wit-op-#141414 leest in zowel light als dark mode omdat clients écht-donkere achtergronden met rust laten. Laat de donkere balk de card-hoekradius volgen (`border-top-left/right-radius` gelijk aan de card) zodat het een intentionele header is, geen los blok. Concreet: `apps/app/src/lib/mail.ts` `renderBroadcastEmailHtml`. Media-query image-swap bewust buiten scope (fragiel over clients). Beide template-varianten (broadcast met unsubscribe-footer + service met privacy-link) delen dezelfde head/logo-constructie — alleen de footer verschilt. De `List-Unsubscribe`-header blijft losstaand in de send-payload (alleen bij `includeUnsubscribe && unsubscribeUrl`). Zie [[broadcast-email-html-template]].
[2026-07-05] playlist-resume-hydratatie: bij terugkeren naar een lopende playlist-job (navigeer weg → terug) verdween de per-video-lijst — alleen de progress-balk/teller bleef. OORZAAK: de lijst-rows renderen uit PlaylistManager's INTERNE `playlist`-state (`{title, entries}`, gated door `{playlist && …}` op regel 566 + `playlist?.entries?.map` op 632); die state is vluchtig en gaat verloren bij remount/navigatie. De `videoStatuses`-prop (per-video-status-badges) wórdt wél gehydrateerd uit DB `video_results`, maar zonder `playlist.entries` zijn er geen rows om die badges aan te hangen → lege lijst, terwijl de balk/teller (aparte render, gedreven door `videoStatuses`-counts + elapsedSeconds) blijft. REGEL: een resume-path moet de VOLLEDIGE UI-state herstellen, niet alleen de teller. Split naar bron: (a) dynamische per-video-STATE (statussen) hydrateren uit DB (`video_results`) — verandert live, moet autoritatief uit de DB komen; (b) statische display-data (titel/thumbnail per video) die NIET in de DB-job-rij staat (`video_metadata` is leeg; `video_results` bevat alleen `{status, transcript_id/error_type}`) → persisteren bij job-start (sessionStorage `entries`) en op resume terugzetten in de component-state. Concreet: PlaylistTab slaat `entries` op in sessionStorage bij start, leest ze op resume en geeft ze via nieuwe prop `resumePlaylist` aan PlaylistManager, dat zijn `playlist`-state seedt (`setPlaylist(prev => prev ?? …)` + `hasExtracted=true`). Toekomstige robuustheid (cross-device/andere tab): vul `video_metadata` (backend accepteert `{video_id:{title,duration,thumbnail}}`, main.py:208/1059) bij job-start zodat de entries ook puur uit de DB kunnen komen — vereist wijziging aan de start-payload/Next.js-route (buiten de resume-path-scope, apart oppakken). Zie [[arq-jobtimeout-en-interrupted-state]].
[2026-07-05] resume-uit-db-en-lijst-windowen: twee regels voor resume/lange-lijst-render. (1) sessionStorage telt ÓÓK als vluchtige client-state — een resume-path moet display-data uit de DB hydrateren, niet uit een sessionStorage-cache. Concreet playlist: de per-video display-data (titel, duur) hoort in de DB-job-rij via `video_metadata` ({video_id:{title,duration}}), meegestuurd in de start-payload; op resume bouw je de entry-lijst uit `job.video_metadata` + `job.video_ids` (die je toch al ophaalt voor de `video_results`-statussen). GEEN sessionStorage-entry-cache. LET OP de doorgifte-keten: de backend PERSISTEERT `video_metadata` bij job-start (main.py insert), maar de Next.js start-route Zod-schema STRIPT onbekende velden (`safeParse` → `result.data`) — een nieuw veld moet expliciet aan het schema worden toegevoegd, anders bereikt het de backend nooit. Zod v4: `z.record(z.string(), valueSchema)` (2 args), niet `z.record(valueSchema)`. (2) Lange lijsten WINDOWEN, niet volledig renderen: een 200-500-video-playlist mag nooit alle rows tegelijk in de DOM zetten — render standaard ~25 met een "Load More" (+25) en een "Showing X of Y"-indicator; de balk/teller toont het échte totaal (voortgang over alle N), de lijst is gewindowd. Geldt live én bij resume. In `PlaylistManager` bestond `visibleCount`/`loadMore` al; `availableCount = playlist?.entries?.length` zodat windowing automatisch ook bij resume werkt zodra `playlist.entries` gehydrateerd is. Bijkomend: playlist-lijst is nu titel-gedreven (thumbnails uit de row verwijderd — sneller, consistent met Library-stijl). Zie [[playlist-resume-hydratatie]].
[2026-07-06] credit-transactions-sign-conventie: debits in `credit_transactions` hebben ÉÉN consistente sign-conventie — **positief amount onder `type='debit'`**; de `type`-kolom draagt de richting, NIET het teken. Schrijf NOOIT een debit met negatief amount weg. Fout die dit blootlegde: `deduct_credits_atomic` (whisper) schreef debits positief, maar `update_playlist_video_progress` (caption) schreef `-p_amount` (negatief) — beide `type='debit'`. Gevolg: `SUM(credit_transactions)` reconcilieerde NIET naar `user_credits.credits` (whisper- en caption-debits hieven elkaar deels op), en de admin-metric "Credits Consumed" (`SUM WHERE type='debit'`) was structureel onderschat (met 2× de som van de negatieve rijen). De LIVE balans klopte wel — beide paden doen `credits = credits - p_amount` op `user_credits`; alleen het LOG en de afgeleide metrics waren fout. Ook een display-bug: `{type==='credit'?'+':'-'}{amount}` rendert een negatief debit-bedrag als "--1". Regel bij elke nieuwe credit-aftrek-route: teken = altijd positief, richting via `type`; en verifieer met de reconciliatie-invariant `user_credits.credits == SUM(type=credit) - SUM(type=debit)` per user. `user_credits.credits` is en blijft de gezaghebbende balans (4 RPC's onder `FOR UPDATE`); `credit_transactions` is een audit-log, geen balans-bron. Opgelost 2026-07-06 via `20260706172045_fix_caption_debit_sign` (RPC → `+p_amount`) + `20260706172114_backfill_debit_sign` (627 negatieve rijen → `ABS`); 6/6 users reconcilieerden diff=0 na de backfill.
[2026-07-06] additieve-kolom-op-hete-rpc-tabel: een NIEUWE, ge-CONSTRAINEDE kolom toevoegen aan een tabel waar bestaande RPC's/INSERT-paden al naar schrijven MOET non-breaking zijn — anders breekt elke bestaande INSERT die de nieuwe kolom niet zet. Regels: (a) kolom NULLABLE, geen NOT NULL (bestaande paden zetten 'm niet); (b) een CHECK-constraint moet NULL EXPLICIET toestaan (`CHECK (kind IS NULL OR kind IN (...))`), nooit `CHECK (kind IN (...))` — die laatste faalt op elke insert zonder kind; (c) een UNIQUE-index bedoeld voor toekomstige idempotentie maak je PARTIEEL met een `WHERE <nieuwe-kolom> IS NOT NULL`, zodat legacy-rijen (kolom NULL) erbuiten vallen en er geen collision ontstaat vóór de nieuwe code de kolom vult. Backfill van de klassificatie-kolom (bijv. `kind`) mag, maar backfill de JOIN-sleutel (`job_id`/`playlist_id`) van legacy-rijen JUIST NIET uit metadata — dat trekt ze de partiële UNIQUE-index in met collision-risico. Concreet toegepast bij ADR-050 credit-reservering-fundering (`20260706190140`, `20260706190216`): `kind`/`job_id`/`playlist_id` op `credit_transactions` nullable + `CHECK (kind IS NULL OR ...)` + twee partiële UNIQUE-indexen — de live RPC's (`deduct_credits_atomic`, `add_credits`, `update_playlist_video_progress`) zetten geen `kind` en blijven daardoor werken. Verifieer non-breaking vóór apply: zou een INSERT-vorm van de bestaande RPC's nog aan de nieuwe CHECK voldoen? Zie [[credit-transactions-sign-conventie]].
[2026-07-06] reserveren-idempotent-insert-first: een reserverings-/idempotente-aftrek-RPC bouw je als **insert-first onder FOR UPDATE**, NIET check-balance-first. Volgorde: (1) `FOR UPDATE` op `user_credits`; (2) probeer de reservering-rij te inserten met `ON CONFLICT DO NOTHING RETURNING id` (partiële UNIQUE `(job_id,kind)`/`(playlist_id,kind)` vangt de retry); (3) **geen** rij terug = al gereserveerd → return idempotent success ZONDER aftrek; (4) wél een nieuwe rij → pas dan saldocheck; onvoldoende → de zojuist ingevoegde rij weer DELETE'n en schoon hard-falen (niets afgetrokken). WAAROM insert-first en niet check-first: bij een idempotente retry ná een geslaagde eerste reservering is de balans al verlaagd — een check-balance-first zou de retry ten onrechte op `insufficient_credits` laten vallen. De conflict-detectie moet dus vóór de saldocheck. `reserve_credits` (`20260706200207`) doet dit; bewezen met een concurrent-overspend-test (2 jobs, samen > saldo → exact 1 slaagt, balans nooit negatief) + idempotentie-asserts voor job_id én playlist_id. **Rollout van financieel-kritieke multi-fase credit-changes: build dark, activate together** — reserve is gebouwd + bedraad achter `CREDIT_RESERVATION_ENABLED` (default OFF) zodat het live gedrag nul verandert (geen dubbele aftrek met de nog-bestaande per-video-aftrek); de flag gaat pas AAN samen met settle+refund, niet los. Zie [[credit-transactions-sign-conventie]] en [[additieve-kolom-op-hete-rpc-tabel]].
[2026-07-06] reserve-settle-refund-drie-fasen: de gedrags-fase van ADR-050 (settle+refund) heeft drie harde regels. (1) SETTLE IS BALANS-NEUTRAAL: bij een gereserveerde job/playlist is de balans al bij reserve bewogen; settle registreert alléén het WERKELIJKE verbruik als `kind='settlement'`/`type='debit'`-rij ZONDER `user_credits` te muteren — het is de som-bron voor de refund, geen balans-mutatie. Gevolg voor de reconciliatie-invariant: nieuwe settlements (herkenbaar aan `job_id`/`playlist_id` NOT NULL) worden UITGESLOTEN uit `balans == Σ(credit) − Σ(debit)` — anders tel je verbruik dubbel (reserve daalt de balans al). Legacy-settlements (fase-1-backfill, `job_id`/`playlist_id` NULL) blijven wél balans-affecterend; na de launch-reset (taak 1.26) vereenvoudigt de uitsluiting tot `WHERE kind <> 'settlement'`. En: de admin-metric "Credits Consumed" telt `SUM WHERE kind='settlement'`, NIET `SUM WHERE type='debit'` — anders tel je reservering + settlement dubbel (beide `type='debit'`) zodra de flag AAN gaat; "Credits Purchased" telt `type='credit'` EXCL. `kind='refund'` (NULL-kind blijft meetellen) zodat refund-credits de aankoop-metric niet inflateren. (2) REFUND IS ÉÉN NETTO-POST aan het eind: `refund = credits_reserved − Σ(settlements)`; positief → credit; negatief (verbruik > gereserveerd) → best-effort debit GECAPT op saldo (`LEAST(-refund, balance)`) + `RAISE WARNING`, NOOIT `RAISE EXCEPTION` — een al-gedraaide transcriptie mag niet alsnog falen (ADR-050 §4). Reason zegt bij negatief "bijbetaald", niet "teruggestort". Idempotent via insert-first (`ON CONFLICT (.,'refund') DO NOTHING` VÓÓR de balans-mutatie, onder `FOR UPDATE`). (3) BRANCH OP `credits_reserved > 0` (per-job/playlist), NIET op de globale flag: een in-flight job houdt het model van zijn eigen start, ook als de flag midden in zijn leven flipt → géén window met dubbele óf nul aftrek. De flag (`CREDIT_RESERVATION_ENABLED`) gate ALLEEN of NIEUWE jobs reserveren; de oude directe aftrek blijft als `else`-tak (flag-OFF/ongereserveerd = rollback-veilig, byte-identiek aan `20260706172045`). Caption-settlement-idempotentie = applicatielogica (`v_already_done` per-video-guard onder `FOR UPDATE`), GEEN index-backstop; whisper-settlement via de bestaande `(job_id,'settlement')` UNIQUE; de `(playlist_id,kind)` UNIQUE moest daarom `kind <> 'settlement'` uitsluiten (M4) want settlements zijn meervoudig per playlist. Gebouwd in `20260706205451/205619/205835/205918` + pipeline/worker; flag OFF bij commit — activering is een aparte 1-regel-follow-up ná prod-verificatie. Zie [[reserveren-idempotent-insert-first]].
[2026-07-07] recovery-pad-boek-geld-vóór-terminal-claim: op een financieel-kritiek recovery-/crash-pad mag je de status NOOIT terminal claimen vóór de geld-boeking. Volgorde MOET zijn: (1) muteer het geld EERST (idempotent), (2) CHECK de returnwaarde, (3) claim de status pas terminal bij BEWEZEN succes. Waarom: bij claim-vóór-boeking staat de job na een transient failure (522/timeout) op de boeking al terminal → wordt nooit meer geselecteerd (recovery-queries filteren op de niet-terminale status) → de refund wordt nooit opnieuw geprobeerd = STIL, PERMANENT geldverlies. Idempotentie beschermt tegen dúbbel, niet tegen núl — dus de reorder heeft een idempotente mutatie nodig (partiële UNIQUE `(job_id/playlist_id,'refund')`; voor een niet-idempotente helper zoals `add_credits` een idempotente variant bouwen, hier `refund_credits_flat`). Faalt de boeking → laat de status niet-terminaal zodat de volgende cyclus retry't, én alarmeer (error-Sentry). Concreet: de watchdog `Pass 2`/`Pass 2b` (`worker.py`) claimde `interrupted→error` en refundde daarná; gefixt naar refund-vóór-claim via `_refund_then_claim_job`/`_refund_then_claim_playlist`. BREDER patroon (op meerdere plekken fout te maken): een GENEGEERDE returnwaarde van `refund_credits`/`add_credits`/`refund_credits_flat` op ELK pad = stil verlies — check 'm altijd. Hetzelfde antipatroon zat in `run_whisper_reservation_aware` op het whisper-success-pad (refund genegeerd, géén watchdog-vangnet want `transcript_id` gezet) → daar minimaal luid alarmeren (geen retry-pad). En: een transient SELECT-522 op een recovery-lijst-query is NIET high-priority — muteert niets, retry't vanzelf → degradeer naar warning (`scope.set_level("warning")`, sentry-sdk default = error); reserveer error-level voor de boeking-failure. RPC-tests dekken de dispatch/cron-orchestratie NIET — test de faalmodus op het pad zelf (stub de mutatie → failure, assert niet-terminaal + geen boeking; dan success → precies één boeking, terminal; retry → idempotent). **Twee lagen die je op een terminaal refund-pad allebei nodig hebt** (een pad ZONDER watchdog-vangnet, d.w.z. `transcript_id` gezet / `status='complete'` → buiten Pass 2/2b): (a) een **bounded idempotente retry** op de refund-call zelf (2-3× korte backoff vóór het alarm) — een enkele error-Sentry zónder retry laat een transient 522 alsnog stil verliezen; refund_credits is idempotent via `(.,'refund')`, dus herproberen dubbel-refundt nooit. (b) een **reconciliatie-sweep** als structureel net: retry dekt de transient NIET wanneer de worker crasht TUSSEN de terminal-status-set en de refund-retries (job is dan al terminaal → buiten elk crash-pad). Een watchdog-pass met een **anti-join** (`terminale status + credits_reserved>0 + GEEN (job/playlist_id,'refund')-rij`, via een SQL-functie want PostgREST kan geen NOT EXISTS) vindt precies die gemiste refunds en boekt ze idempotent — **zonder de status te muteren** (de job is al terminaal en correct; er valt niets te claimen, anders dan Pass 2/2b). De anti-join is meteen de idempotentie (verdwijnt zodra de refund-rij bestaat) en moet ge-`LIMIT`-d zijn zodat een achterstand de cron niet laat overlopen. Concreet: `refund_with_retry` (`transcription_pipeline.py`) + watchdog `Pass 2c` / `watchdog_unrefunded_reserved` (`worker.py`, migratie `20260707102113`). Zie [[reserve-settle-refund-drie-fasen]] en [[reserve-vóór-dispatch-splitsing-alle-paden-bedraden]].
[2026-07-07] reserve-vóór-dispatch-splitsing-alle-paden-bedraden: `reserve_credits` draait in `main.py` (`/api/transcribe/whisper`) VÓÓR de `if source_type == "youtube"`-splitsing, dus het geldt voor ÉLK dispatch-pad dat daarna de transcriptie-pipeline aanroept. Gevolg: elk zo'n pad MOET reservation-aware zijn (pipeline `reservation_mode=True` → oude aftrek onderdrukt + settle) én een refund-hook hebben — een DIRECTE `do_assemblyai_transcription`-aanroep zonder die bedrading = bij flag ON reserve + oude per-video-aftrek samen = DUBBELE afrekening, en de reservering wordt nooit teruggeboekt. Concreet gemist bij de eerste fase-2-bouw: alleen `worker.run_whisper_job` (ARQ-pad) was bedraad; het **upload-pad** (`main.py`, `source_type != youtube`, `asyncio.create_task` — permanent, geen fallback) en de **arq-loze youtube-fallback** riepen de pipeline direct aan zonder `reservation_mode`/refund → prod-bereikbare dubbele aftrek voor elke upload-transcriptie. Fix: één gedeelde dispatch-primitief `run_whisper_reservation_aware` (`transcription_pipeline.py`) die credits_deducted+credits_reserved van de eigen job-rij leest, `reservation_mode` afleidt, de pipeline draait en ná afloop (success ÉN failure) `refund_credits(job_id)` roept; alle drie de standalone-paden (worker, upload, fallback) gaan er nu doorheen zodat de bedrading niet per call-site kan driften. Playlist-whisper gebruikt 'm NIET (refundt op playlist-niveau). REGELS: (a) reserve vóór een branch → verifieer ELKE tak die daarna geld uitgeeft; (b) meerdere call-sites van hetzelfde financieel-kritieke pad = één gedeelde primitief, niet gekopieerde bedrading (kopie #2 werd hier vergeten); (c) RPC-tests dekken de `main.py`-dispatch NIET — voeg een e2e-test op het dispatch-pad zelf toe (gestubde pipeline die settle simuleert) die bewijst dat balans exact één keer beweegt. Zie [[reserve-settle-refund-drie-fasen]].
[2026-07-07] reserve-bedrag-realistisch-vóór-de-gate-betekenis-heeft: een overspend-/quota-gate is alleen echt als het GERESERVEERDE bedrag realistisch is vóór de gate draait. Een reserve die stil terugvalt op het minimum (hier `ceil(0/60)`→1 credit) is een LEGE gate: de 402 checkt tegen 1, en het werkelijke verbruik wordt pas ná de dure operatie via settle/refund verrekend → een gebruiker met te weinig credits krijgt het meerdere gratis (bijbetaald-refund gecapt op saldo, `LEAST(-refund, balance)`, RAISE WARNING nooit EXCEPTION). Concreet: het **upload-pad** (`main.py /api/transcribe/whisper`) reserveerde 1 credit omdat de audio-duur pas ín de pipeline geprobed werd (ná reserve); YouTube reserveerde wél correct want de browser stuurt de duur (metadata) vóór reserve mee. REGELS: (a) stel het reserve-bedrag **server-side + onomzeilbaar** vast VÓÓR reserve — NOOIT uit een client-waarde (bij een directe JWT-upload is die volledig client-gecontroleerd → heropent de gate), en NOOIT stil naar het minimum terugvallen; (b) kun je de exacte kosten nog niet meten, **overschat royaal** (bij een gefaalde ffprobe: schat uit bestandsgrootte met een bewust lage bitrate-aanname) — overschatten is veilig want settle+refund corrigeren naar de echte duur; onderschatten heropent de gate; (c) de meting die je vóór reserve doet is voor de GATE, niet voor de afrekening — settle blijft de waarheid (probe in de pipeline op de echte duur), dus geef een geslaagde vóór-reserve-probe door om dubbel proben te vermijden maar laat een fallback-schatting NOOIT als settle-waarde tellen. Concreet: `estimate_upload_reserve_cost` (`audio_utils.py`) + probe-vóór-reserve in `main.py` (temp-bestand één keer geschreven, hergebruikt door de pipeline; `known_duration_seconds`-passthrough). Zie [[reserve-vóór-dispatch-splitsing-alle-paden-bedraden]] en [[reserve-settle-refund-drie-fasen]].
[2026-07-08] completion-receipt-één-component-stilte-bij-succes: financiële/status-feedback die per scherm ad-hoc gebouwd wordt driftt onvermijdelijk uit elkaar (drie completion-banners hadden drie verschillende groentinten + verschillende copy + de playlist toonde géén totaal-credits). REGEL: bouw zulke feedback als ÉÉN variant-gedreven, herbruikbaar inline component (`packages/shared/src/components/ui/CompletionReceipt.tsx`, gemodelleerd op `FeedbackCard`), niet per call-site. Ontwerpprincipe "stilte bij succes, uitleg bij afwijking": een schone job = één regel (alleen wat betaald is), géén reserve/refund-ruis als er niks terugkwam; pas bij een afwijking (refund, skip, overschatting) toon je de transparantie. Data-regel (financieel-kritiek maar READ-ONLY): lees de cijfers uit de gestructureerde `credit_transactions.metadata` van de refund-rij (`{reserved, consumed, refunded, ...}`) + de settlement-rijen — NOOIT de (Nederlandse) reason-string parsen, en verzin geen tweede balans-bron; de UI muteert nooit credits. Onderscheid scherp: refund-door-mislukking (playlist met skips → normale reserved→used→refunded-transparantie) is NIET hetzelfde als overschatting (upload-fallback waar de duur-schatting te hoog was → geruststellings-strook); trigger de strook semantisch (`kind='upload' && reserved > used`), niet op een credit-drempel. Balans-refresh hoort erbij: `refreshCredits()` moet óók bij job-START draaien (de reservering is server-side al gecommit vóór `job_id` terugkomt), niet alleen bij completion — anders loopt de topbar achter. Zie [[reserve-settle-refund-drie-fasen]] en [[reserve-bedrag-realistisch-vóór-de-gate-betekenis-heeft]].

[2026-07-08] playlist-retry-collection-scoped-receipt: een playlist-retry (per-video én "Retry all") draait als een APARTE playlist-job (`POST /api/playlist/extract` met een subset) met een eigen `playlist_id` maar dezelfde `collection_id` — GEEN interne retry-pass. Gevolg 1 (weergave): een receipt/aggregatie die op één `playlist_id` leest, bevriest op de eerste-run-snapshot ("10 transcribed · 2 skipped — 30 refunded") en ziet de retry-settlements (onder een ander `playlist_id`) nooit; de eerste-run-`video_results` blijft óók bevroren (die 2 video's staan er nog als error in). Een "her-fetch op dezelfde id" lost dit dus NIET op. FIX: aggregeer read-only over `collection_id` (alle collectie-jobs mergen, per video wint success van error, settlements/refunds sommeren) + een `refreshToken`-param die her-fetcht bij retry-completion (het anker-id verandert niet). Gevolg 2 (verrekening — die WÉL klopt): omdat elke retry-job hetzelfde reserve-pad (`_compute_playlist_reservation` + `reserve_credits`) hergebruikt en de worker met identieke formule settelt (reserve==settle per job), en de refund uitgesteld is tot ná de retry-pass, blijft de ledger consistent — geen dubbele charge, geen over-refund (bewezen tegen de echte ledger van collectie de42c5d4: reserved 154, used 153, net refund 1, 12/0). NUANCE (geen ledger-bug, wél revenue): de free-tier (`idx < 3` gratis captions) is index-gebaseerd en reset per job → een retry-subset krijgt z'n eerste ≤3 caption-video's opnieuw gratis; consistent met de bestaande per-video-retry, bewust zo gelaten (Policy K) om de settle-logica niet aan te raken. Retry-all = per-video-retry met een array via één gedeelde `_startRetryJob` — géén apart reserve-pad. Zie [[reserve-settle-refund-drie-fasen]] en [[completion-receipt-één-component-stilte-bij-succes]].
[2026-07-08] active-job-filter-twee-implementaties-sync: de "telt lopende jobs" filter bestaat op TWEE plekken — backend `_count_active_jobs` (`main.py`, service-role, expliciet op `user_id`, voor de concurrency-cap VÓÓR reserve) en frontend `ActiveJobsIndicator` (browser-client onder RLS, voor de weergave). Beide MOETEN dezelfde definitie gebruiken: niet-terminale statussen `transcription_jobs∈{pending,downloading,transcribing,saving}` + `playlist_extraction_jobs∈{running,retry_pending}`, ÉN de versheidsfilter `created_at <30m OF last_heartbeat_at <10m` (mirrort de dedup-query — sluit zombie/stale jobs uit, bv. de april-crashes). `interrupted` telt NIET mee (watchdog-herstelstaat). Bij wijziging: pas BEIDE aan. Cap-plaatsing is financieel-kritiek: de check MOET vóór de job-insert én `reserve_credits` (een geweigerde job mag nooit reserveren) — whisper: ná dedup / vóór insert; playlist: vóór insert. WAAROM niet één gedeelde bron: frontend leest onder RLS (auth.uid), backend met service-role op expliciete user_id — een gedeelde SQL-functie zou beide contexten niet schoon dekken; twee kleine implementaties + deze sync-regel is eenvoudiger. Oude bug: `ActiveJobsIndicator` telde sessionStorage-sleutels (één per job-type) → twee gelijktijdige same-type jobs werden als "1" geteld.
