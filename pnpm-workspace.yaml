packages:
  - 'apps/*'
  - 'packages/*'
  # apps/video is a standalone Remotion workspace, deliberately OUTSIDE the Turborepo build
  # graph (ADR-089): it has its own node_modules + install, never builds/deploys with the apps.
  - '!apps/video'
